# 幽墟回廊 · 项目交接文档

**这个文件是给 Claude 看的。** 新开一个会话时它会被自动读取，所以用户不需要重复交代背景。
改动了下面记录的任何事实（地址、流程、决定、坑），**请同步更新这个文件**。

用户用中文交流，回复用中文。

---

## 一句话

一个用背单词打怪的地牢 Roguelike，纯静态网页，没有构建步骤，没有依赖。
答对一题砍怪一刀，答错挨一口。**五章**：一~四章各 50 层，分别出 A1 / A2 / B1 / B2；
**第五章「无尽」没有底**，B1 / B2 / B2–C1 三档词混着出，一直往下走到倒下为止。

## 三个地址

| 用途 | 地址 |
| --- | --- |
| 代码仓库（唯一源） | https://github.com/audience20060904-lang/youxu-huilang |
| 线上网站 | https://audience20060904-lang.github.io/youxu-huilang/ |
| 废弃的旧副本 | `https://claude.ai/artifact/AUmzcwqghnNKk9hGFYxhJL` |

## 已经定下来的事（别再重新讨论）

1. **GitHub 是唯一源。** 这个仓库的 `main` 分支就是全部真相。
2. **不要再碰那个 Artifact。** 游戏最早是作为 Claude Artifact 写的，代码从那里导出后推进了本仓库。
   那个 Artifact 现在是一份冻结的旧副本，用户已决定弃用它 —— **不要往那边同步，不要republish，
   也不要主动提议用它**。（用户没说要删，所以也别删。）
3. **用户不自己改代码。** 工作方式是：用户提需求 → Claude 在本仓库改 → 提交并推到 `main` → 网站自动更新。
   不要把「你去 GitHub 网页上点开文件改一下」当成交付。
4. **网站已经跑通了**，用户确认能正常打开。别再问「Pages 开了吗」。
5. **改完自动上线，不用问。**（用户 2026-09 明确授权）每次改动做完、自测过，就直接：
   提交 → 推到 `main` → 用 Actions API 确认部署 `conclusion: success` → 在回复里带一句上线结果。
   **不要再问「要不要合并到 main / 要不要上线」**，也不要停在某个开发分支上等许可。
   - 会话被要求在某个 `claude/...` 分支上开发时：照样在那个分支上提交和推送，
     然后把它合进 `main` 再推 `main`（`git checkout main && git merge --ff-only <分支> && git push origin main`）。
     分支只是工作区，**`main` 才是上线口**。
   - 例外只有两种，这时候先问：改动会**动坏现有存档**（换 localStorage 键、改存档格式），
     或者用户自己说了「先别上线」。
   - 自测跑不过、或者部署失败，就别硬推/别当成功报 —— 先修，或者如实说卡在哪。
6. **别堆提示文字。**（用户 2026-09）"这个游戏不需要太多提示"。界面上的大段说明能删就删，
   玩法说明（`veilHelp`）里集中讲一次就够。遗物页原来那块「遗物怎么来」已经整块删掉了，别加回来。
   新功能就地给一行短提示即可，不要再写一屏 note。

## 文件结构

`index.html` 按固定顺序加载脚本，**顺序不能乱**（`util` 提供全局函数 → `words`/`content` 依赖它 → `game` 依赖全部）：

| 文件 | 作用 |
| --- | --- |
| `index.html` | 页面骨架：四个标签页（冒险/信息/遗物/设置）+ 十个弹层 |
| `style.css` | 全部样式，颜色变量在 `:root` |
| `util.js` | 全局小工具（随机、localStorage 存档、语音朗读），必须最先加载 |
| `words-a1.js` | 词库 6925 词（A1 460 / A2 465 / B1 2000 / B2 2000 / **B2–C1 2000**）+ 类别名 `CAT_CN`、词性名 `POS_CN` + 派生索引 `WMAP` / `BYCAT` / `BYPOS` / `BYLV`（**`BYLV` 现在有 1~5 五个桶**，加难度就得给它加桶）。文件名是历史遗留，**装的是全部五档**。这个仓库里**只有它是 CRLF 换行**，重新生成时别改成 LF |
| `content.js` | 数值配置：共用数值 `CHAPTER`、各章 `CHAPTERS`、结算 `SCORE`、怪物 `FOES`、遗物 `RELICS`。**调平衡只改这个文件** |
| `art.js` | SVG 美术（怪物立绘 `ART`、主角 `HERO`、泉 `SPRING`、商 `SHOP`、金币 `COIN`、粒子碎屑），主体用 `currentColor`，颜色交给 CSS（金币走 `--coin`，不是 `--torch`）|
| `game.js` | 全部游戏逻辑，约 4300 行（含联机 COOP 分支，见下面三行和 `联机方案.md`） |
| `coop.html` | **联机入口**（2026-09 加，第一期）。跟 `index.html` 结构一样，多了进房间的弹层和队友状态条，在 `game.js` 之前设 `window.__COOP = true`。**`index.html` 一个字没改**，单人版零风险 |
| `net.js` | **联机客户端**（2026-09 加），只有 `coop.html` 加载。提供全局 `NET`：连房间、收发消息、自动重连 |
| `server.js` | **联机服务器**（2026-09 加），`node server.js` 启动。零依赖，只用 Node 内置模块，自己实现 WebSocket 握手/帧解析；同时是静态文件服务器 + 房间/消息转发服务器。**不上线**（发到 Pages 上也无害，没人会去执行它，但它本来就是给本地跑的） |
| `check-words.py` | **不上线**，词库体检脚本：`python3 check-words.py`（英文/同级释义查重、类别词性合法、每类每词性 ≥6、长度 ≤11）。改完词库跑一遍 |
| `遗物数据表.md` | **不上线**，只给用户改：48 件遗物的品质/文案/实际数值 + 全局数值（公式、掉率、分解价）。用户改这份，Claude 照着改 `content.js` / `game.js`。改了遗物数值就**同步更新它** |
| `联机方案.md` | **不上线**，**局域网双人联机的唯一交接文档**：当前进度、已定死的玩法规则、架构与踩过的坑。要做联机、或者用户提到联机，**先读它**；动了联机相关的任何东西，**必须回来改它** |
| `start-coop.bat` | **不上线**（2026-09-20 加，跟 `server.js` 一个待遇），用户在自己电脑上双击用的：先 `git pull` 拉最新代码，再 `node server.js` 启动联机服务器。详见 `联机方案.md`「怎么自动更新到用户电脑」 |

## 章节系统（五章，2026-09）

**前四章 = 一章一个词难度**：石廊 A1 / 锈庭 A2 / 烬渊 B1 / 墟心 B2，各 50 层，各一只章末 Boss。
**第五章「无尽」是另一种东西**：没有底、没有章末 Boss、永远不会通关，三档词混着出。

| 章 | 名字 | 词 | 层数 | 宝石倍率 | foeBonus | Boss |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 石廊 | A1 | 50 | ×1.00 | — | 石廊守卫 `warden` 200/14/3 |
| 2 | 锈庭 | A2 | 50 | ×1.25 | +4 血 +1 伤 +2 经验 | 锈庭主事 `steward` 300/18/4 |
| 3 | 烬渊 | B1 | 50 | ×1.50 | +8 血 +2 伤 +4 经验 | 烬渊祭司 `priest` 440/22/5 |
| 4 | 墟心 | B2 | 50 | ×1.75 | +12 血 +3 伤 +6 经验 | 墟心冕者 `crown` 560/26/6 |
| 5 | 无尽 | B1+B2+B2–C1 | **∞** | ×2.00 | +16 血 +4 伤 +8 经验 | **没有**（每 10 层都是层间守者） |

- `content.js` 的 **`CHAPTER`** 只装**各章共用**的数值（地图 33×25、50 层、主角基础、成长曲线）。
  ⚠️ `W / H / FLOORS` 在 `game.js` 开头就被取成 `const`，**各章不能各来一套**。
  ⚠️ **`FLOORS` 现在只是「普通章的 50 层」，别再直接读它** —— 走 **`floorMax()`**
  （无尽章返回 `Infinity`）。`Infinity` 进 `Math.min` 就是不封顶，
  `G.floor > floorMax()`（通关）/ `=== floorMax()`（最后一层、章末 Boss）在无尽章里**永远不成立**，
  所以那几条分支自动全部走不到 —— 这就是无尽章「没有底」的全部实现。
  另有 `isEndless()` 给顶栏之类的地方判当前是不是无尽章。
- 每章自己的东西在 **`CHAPTERS`** 数组里：`name` / `level` / `wordLv` / `gemMult` / `foeBonus` / `boss`。
  **`CH` 是「当前这一章」**，换章只能走 `setChapter(id)`（它同时刷顶栏和地图上的章节名）。
- **前四章一章只出一个难度的词**（`CH.wordLv`：1=A1 2=A2 3=B1 4=B2 5=B2–C1）——
  用户要求「每章词不要重复」。以前那套按层数从 A1 混到 B1 的 `wordLevels()` 已经删了，别加回来。
  ⚠️ **无尽章的 `wordLv` 是个数组 `[3,4,5]`**（用户 2026-09），B1 / B2 / B2–C1 三档一起出，
  是全游戏唯一多难度的一章。所以取词那一路全部改成了**按一组难度**算：
  **`chapterLvs()`**（单个数字会被包成一元数组，前四章行为一个字没变）、
  **`scopeToLevels(pool, 难度数组)`**、`scopeByLevel()`、`chapterPos()`、`chapterSeenPct()`。
  **`chapterLv()` 这个单数版已经没了，别找**。
  ⚠️ **`scopeToLevel(pool, 单个难度)` 还在，别删** —— 选择题的干扰项走的是它
  （干扰项跟**题目那个词本身**同难度，不是跟这一章同难度，否则无尽章会拿 B1 的干扰项配 C1 的题）。
  **每个难度的每个类别、每个词性都补到了 ≥6**，否则这一档的怪/Boss 挑不到弱点
  （`chapterPos()` 只在**这一章真的有词**的词性里挑 Boss 弱点；以前那个按类别挑的
  `chapterCats()` 已经没了，别找）。
- **各章的 Boss 写在 `CHAPTERS[].boss` 上**（`def.fixed = true` 表示数值写死不随层数长）。
  第二章的「锈庭主事」复用守卫的立绘；`MOB_ART` 的键是 `def.id`，所以 `art.js` 末尾挂了一条
  `MOB_ART.steward = MOB_ART.warden` 的别名，少了它地图上只会画个「庭」字。
  三四章的两只**各有自己的立绘和小图**（`ART.priest` / `ART.crown`、`MOB_ART.priest` / `MOB_ART.crown`），
  不用别名。
- **三四章的 Boss 在地图上单独上色**：`render()` 除了 `mob` / `boss` 还会挂一个 **`m-<怪的 id>`**，
  `style.css` 里 `.c.mob.boss.m-priest{#C2510E}`（烬色橙）/ `.c.mob.boss.m-crown{#4A3A8C}`（冕者靛），
  并且这两条也进了 `.c.mem.* svg{opacity:.5}`（它们图里有写死的金）。
  别的怪没写规则，照旧吃 `.c.mob` / `.c.mob.boss`。
- `foeBonus` 是这一章所有怪（Boss 除外）的固定加成，整数加法，**一章一档：+4 血 +1 伤害 +2 经验**。
  ⚠️ **`armor` 那一档永远留 0，别给后面的章加护甲**：玩家攻击靠等级长（第 1 层只有 3 点），
  护甲只会把全章最难的一下压在**刚进章的第一只怪**身上，中后期攻击四十几时等于没有。
  算过：第四章 armor +1 时第 1 层的灰鼠要砍 10 刀，+2 要砍 20 刀。压力全部交给 hp / dmg。
- **成长曲线实测**（50 层跑完、不带遗物，「可错几次」= 玩家血上限 ÷ 当层怪的伤害）：
  第 1 层可错 13.0 / 9.3 / 7.5 / 6.4 次，Boss 可错 11.6 / 9.6 / 8.3 / 7.4 次，
  Boss 要砍 3 / 4 / 6 / 7 刀。中后期四章都收敛到「可错 10 次、两刀一只」——
  因为经验给得多 → 等级高 → 血上限也高，**章节难度差主要体现在开头几层和 Boss 身上**。
  改 `foeBonus` 或 Boss 数值前先把这几个数算一遍。
- 续玩档里存了 `ch`，`resumeRun()` 先 `setChapter(s.ch)` 再铺场；`foeDef()` 会在**所有章的 Boss**
  和守层者里找 —— 以前它只认第一章 Boss，第 10/20/30/40 层的守层者一读档就凭空消失了（顺手修了）。
  ⚠️ `foeDef()` 里那句要 **`CHAPTERS[i].boss && ...`** —— 无尽章的 `boss` 是 `null`，不兜会点空。
  ⚠️ `readRun()` 校验层数上限时 **`CH` 还没切**，所以那里写的是
  `floorMax(chapterById(s.ch))`，按**存档里那一章**算，不是按当前章。
- **加第六章**：`CHAPTERS` 抄一条 + `ROUTES` 加一条路 + 一只 Boss（立绘 `ART` / 小图 `MOB_ART`）。
  按难度取词的地方（`chapterLvs` / `chapterPos` / `scopeToLevels`）都是按 `w.lv` 现算的，不写死档数，
  **词库那边先得有那一档的桶**（`words-a1.js` 的 `BYLV` 现在是 1~5）。

### 第五章「无尽」（用户 2026-09）

- **没有底**：`CHAPTERS[4]` 上带 **`endless:true`**、**`boss:null`**、**`wordLv:[3,4,5]`**。
  层数上限走 `floorMax()`（见上面那条），所以不会通关、没有「最后一层」、没有章末 Boss。
  **每 10 层照旧是一间 Boss 房**，里面永远是跟着层数长的「层间守者」`GATEKEEPER`
  （`genBossRoom()` 里那句 `(G.floor === floorMax() && CH.boss) ? CH.boss : GATEKEEPER`）。
  顶栏写成 `37*/∞`（`isEndless()`）；390×667 上量过，`∞` 不会把顶栏撑出滚动条。
- **词池 6000 个**（B1 2000 + B2 2000 + B2–C1 2000），是前四章加起来还多的量。
  「一趟之内答对过的词不再出第二次」那套照常走，问完 6000 个才会清空 `P.used` 开新一轮。
- **深渊压迫 `endlessRamp(floor)`**（`content.js` 的 `ENDLESS_FROM` 50 / `ENDLESS_EVERY` 10 /
  `ENDLESS_RAMP` 0.1）：第 50 层往下每 10 层给怪的**伤害**再加一档 10%，
  在 `foeNums()` 末尾跟 `FOE_BANDS` 一起乘（别的章恒为 1，等于不存在）。
  ⚠️ **只压伤害，别去压血量**（两样都压的那一版算完就毙掉了）：
  血量也乘的话第 200 层一只普通怪要砍 50 刀 —— 那不是难，是熬。
  ⚠️ **为什么非得有它**：不加的话深层会**稳定在「四五刀一只、能错四次」再也不变**
  （经验多 → 等级高 → 血和攻击跟着线性长，正好抵消怪的线性成长），300 层跟 60 层一样好打。
  实测（裸装、不带遗物，「容错」= 血上限 ÷ 怪一口；刀数是普通怪）：

  | 层数 | 50 | 75 | 100 | 150 | 200 | 250 | 300 |
  | --- | --- | --- | --- | --- | --- | --- | --- |
  | 容错 | 4.7 | 4.0 | 3.5 | 2.5 | 2.0 | 1.6 | 1.4 |
  | 刀数 | 4 | 4 | 4 | 5 | 5 | 5 | 5 |
  | Boss 层容错 | 3.2 | 2.9 | 2.3 | 1.7 | 1.3 | 1.1 | 0.9 |

  一百层往后就得靠遗物顶着走，顶不住的那一层就是这一趟的终点 —— 这是有意的。
  改这三个数之前先把这张表重算一遍。
- **宝石**：难度倍率 ×2.00，而层数倍率（`层数 ÷ 10`）**没有上限** ——
  无尽就是刷宝石的地方，走得越深给得越多。它永远拿不到 `SCORE.clear` 那 50 分（不会通关）。
- **美术一个字没加**：层间守者本来就有自己的小图 `MOB_ART.gate` 和立绘（借 `ART.warden`）。

## 结算与宝石

- 一趟结束（倒下 / 通关）走 `endRun()` → `runScore()`。
  ⚠️ **到达的层数是主倍率，不是一项加分**（用户 2026-09 改的）：
  ```
  宝石 = （最大连击 + 击杀 + 正确率 + 没花完的金币 + 通关）× (到达层数 ÷ SCORE.floorDiv，最低 SCORE.floorMin) × CH.gemMult
  ```
  **其余每一项都只是相加**（分值在 `content.js` 的 `SCORE` 里，`perFloor` 已经删了，别找）。
  跟伤害公式一个规矩：**只有「层数」和「难度」两个乘区，别再加第三个**。
  结算界面把每一行、两个倍率都单列出来，玩家能自己对账。
  ⚠️ **层数倍率没有上限**，无尽章走到第 200 层就是 ×20 —— 这是有意的，
  无尽就是刷宝石的地方（难度倍率还另有 ×2.00）。它永远拿不到「通关」那 50 分。
- **金币不再带回镇上**，它是纯局内货币（买遗物 / 合成）。带回去的只有宝石。
  主动「放弃本次探索」照旧不结算。
- **宝石 = 以前的「镇上存款」**：`localStorage` 的键没变（`youxu.town.v1`，老档照读），
  字段从 `gold` 改叫 `gem`，读的时候兜一下老档。**改宝石只能走 `addGems(n)`** ——
  它会立刻 `commitPerm()` 落盘（用户要求「每次宝石数额变动存一次档」）。
- 宝石的唯一去处是主城的**「祝福」**（原来那个灰着的铁匠铺）。**祝福系统本身还没定**，
  现在只是改了名字和文案，点不动。
- `P.maxCombo` 是结算用的最大连击，在 `answer()` 里跟着 `P.combo` 更新（拼对的 +10 也算进去）。
- **结算顶上有一块「本局正确率 vs 历史平均正确率（该层）」的强调**（用户 2026-09，`#endAcc`）：
  左边是这一趟的总正确率，右边是**这一层**历史上的平均正确率，底下一行写差了几个百分点
  （高标绿 `.up`、低标红 `.down`）。
  数据存在 **`MET.accF`** 里：`{"23":{r:340,w:80}}`，按**答题当时所在的层**累计，跨存档一直加。
  ⚠️ 本局的那一笔先记在 **`P.accF`** 上，`endRun()` 里先算完 `histAcc()` **再** `foldAcc()` 并进去 ——
  顺序反了就变成跟自己比。`accF` 是 `MET` 的一个字段，跟着四个永久键落盘，**不用新开 localStorage 键**；
  导入存档时 `mergeData()` 里是**相加**（它是答题次数，不是进度，取大值会白丢一边）。

## 常见改动

- **加单词** → `words-a1.js` 的 `WORDS` 数组，格式 `["英文","中文","类别",难度,"词性"]`，**五个字段都要给**。
  难度 1=A1 2=A2 3=B1 4=B2 **5=B2–C1**（第五章「无尽」把 3/4/5 三档混着出）。
  类别必须在同文件的 `CAT_CN` 里有中文名（**现在 14 类**，
  虚词 `func` 那一类连同 A1 的 40 个、A2 的 35 个虚词已经整类删掉了，用户 2026-09 定的，别加回来）。
  词性只有五种：`n` / `v` / `adj` / `adv` / `num`（中文名在同文件的 `POS_CN` 里）。
  大部分词性能从类别推出来（animal/food/body/people/thing/nature/place→n，verb→v，adj→adj，
  num→num，adv→adv），**只有 `time`、`feel`、`color` 三类是混的**（`afternoon` 是 n 但 `early` 是 adj，
  `love` 是 v 但 `angry` 是 adj，`shade` 是 n 但 `pale` 是 adj），这三类得逐个核。
  ⚠️ **英文不能重复** —— `WMAP` 按英文做键，重了会被静默覆盖，不报错。
  ⚠️ **同一难度里中文释义也不能重复** —— 选择题的干扰项是在同难度里挑的，
  两个词共用一个中文（比如 `start 开始` 和 `begin 开始`）就会出现「两个选项都对」。
  跨难度重复没事（干扰项走 `scopeToLevel(pool, 这个词自己的难度)`，只在**同一档**里挑，
  所以哪怕无尽章把 3/4/5 三档混着出，也碰不到面），
  所以 B1 的 `certain 确定的` 和 A2 的 `sure 确信的` 可以并存。
  ⚠️ **每个难度的每个类别、每个词性都要 ≥6 个词** —— 普通怪按类别出题，Boss 弱点按词性挑，
  不够 6 个那一档就挑不到。
  ⚠️ **英文最长 11 个字母** —— 拼写题的字格只量到 11（`grandmother` 那一批），再长会在 390×667 上折行。
  加完词**跑一遍 `python3 check-words.py`**（仓库根目录，不上线）—— 上面这七条它全查，别靠肉眼。
  ⚠️ 难度 5 那 2000 词是 2026-09 一次加进去的（B2–C1 高频）：
  生成时是**分批写候选 → 脚本过一遍老库去重 → 再按类别配额裁到 2000**，
  单是「英文跟老库撞了」就刷掉了两成多（thing 类撞掉四成）。下次再批量加词照这个路子来，
  **别指望一次写对**。
- **调数值**（血量/伤害/掉率/成长曲线）→ 全在 `content.js` 的 `CHAPTER` 和 `FOES` 里。
  50 层的成长曲线很敏感，改之前先算一遍（文件里有注释说明为什么经验是每 3 层 +1）。
- **加遗物** → `content.js` 的 `RELICS` 加一条，**然后必须在 `game.js` 里加对应的 `hasRelic("id")` 分支**，
  两边不同步的话遗物会存在但没效果。改完**同步更新 `遗物数据表.md`**（用户就是照那份文档提需求的）。
  伤害类的进 `answer()` 的四层桶；**防御类的进 `mitigate()`**；要存东西就往 `P` 上挂字段
  （自动进续玩档，读的地方记得 `|| 0`）；每层清零的挂 `G` 上并在 `nextFloor()` 里重置。
- **伤害按品质分四层**（用户 2026-09 定的阶梯，替换了原来「只有暴击一个乘区」的老规矩）：
  **基础点伤 ＜ 伤害百分比 ＜ 点伤 ＜ 额外伤害 ＝ 暴击率／暴击伤害**
  ```
  伤害 =（攻击 + 基础点伤）×（1 + 百分比合计）+ 点伤，再 ×暴击倍率，最后 −护甲，最低 1
  额外伤害不进这条式子：无视护甲、不吃暴击，单独从怪血里扣（`m.hp -= dmg + extra`）
  ```
  ① 基础点伤进 `stats().atk` 或 answer() 里的 `base`（普通品质给这个）；② 百分比全部**相加**进 `pct`，
  只乘一次；③ 点伤进 `flat`，在百分比之后加；④ 额外伤害进 `extra`。
  **连击本身就是②层的百分比**（用户 2026-09 从「连对 3/5 次 +1/+2 点伤」改过来的）：
  `comboPct()` = `⌊连击 ÷ comboStep()⌋ × CHAPTER.comboPct`，即**每 5 连击 +2%，可叠加不封顶**。
  `comboStep()` 会被遗物「火星」减 1（每 4 连击一档）。`CHAPTER.comboAt3 / comboAt5` 已经删了，别找。
  **全局只有「×(1+pct)」和「×暴击倍率」两个乘区，别再加第三个** —— 玩家要能心算还差几刀。
  暴击率超过 100 的部分每 5 点自动转 +10% 暴击伤害（`critMult`），别让它白费。

## 地图生成（2026-09 换成「房间图」）

- **地图 33×25**（`CHAPTER.W/H`）= **4×4 = 16 个槽位，每个槽位 8 宽 6 高**，外面再留一圈边墙。
  ⚠️ **`view` 9×13 / `viewMax` 11×15 不许跟着改** —— 格子大小只看 `view`，
  地图变大不会让格子变小（四档屏幕上量过，还是 37–46px）。`camera()` 照旧不夹边界。
  **没被房间用到的槽位就是实心墙**，画不出来，玩家看不出地图大了一圈。
- **墙只画贴着地板的那一圈**（用户 2026-09：「墙体只要一格」）：`render()` 里的 `wallEdge(x,y)`
  —— 八邻里有地板才画成 `.c.wall`，再往里的实心墙画成 **`.c.void`**（背景 `--unseen`，
  跟 `.stage` 的底同色，等于不画）。于是每间房、每条走廊都只剩一格厚的墙。
  ⚠️ 这只是**画法**，`G.map` 一点没动 —— 寻路、生成、续玩档看的还是原来那张实心图。
  ⚠️ 八邻（带斜角）**不能改成四邻**，房间的四个角会漏出缺口。
  ⚠️ `.c.void` 的底色必须跟 `.stage` 的背景一样，不然走到地图边缘会看出接缝。
- **房间图怎么长出来**（`slotChain()` + `genFloor()`）：随机挑两个槽位 A、B，
  用曼哈顿最短路径（随机决定先横后竖还是先竖后横）把它们串成一条**链**，链上经过的槽位就是房间。
  房间数卡在 **4–6**：不足 4 就从已选槽位的四邻里随机挑空槽补，超过 6 就重选 A、B（`ROOM_MIN/ROOM_MAX`）。
  每个槽位里放一间 `w=ri(4,7) h=ri(3,5)` 的房，在槽位内随机偏移（夹一下保住外圈边墙）。
- **只有网格上相邻的两间房才连通**，`links` 记着哪两间要开走廊。走廊是**两间房边缘之间的一小段**：
  投影对得上就是直的一条，错开了就在中间的空档里拐一下（Z 形）。
  整条通道只待在这两个槽位里，碰不到第三间房。
  ⚠️ **别再改回原来那种「房间中心连房间中心」的连线** —— 那个会横穿半张地图。
- **人在链的一端，`G.stair` 的占位在另一端**。（清空后阶梯照旧改写到最后一只怪倒下的地方，那套不动。）
- **每层怪 10–13 只**（`CHAPTER.mobsPerFloor` 现在是 `{min,max}` 区间，不再是个数字）。
  **Boss 层（每 10 层）走的是另一套生成**（见下面「Boss 房」），不经过这段。**金币堆 `ri(7,10)` 堆**，
  每堆 `(ri(2,6) + 层数) × CHAPTER.goldMult`（**0.4**，用户 2026-09 把地上的金币减了 60%），最少 1 枚。
- **`G.rooms` 存着这一层每间房的矩形**（跟着续玩档走）——
  自动寻路要「先把当前这间屋子扫干净」。老续玩档没有这个字段，读处一律 `|| null` 兜底。
- ⚠️ **改地图尺寸就得作废续玩档**：`youxu.run.v1` 里地图是按行压成字符串的，行宽一变就对不上。
  所以 `RUN_V` 跟着 +1（这次 3 → 4，跟当年改 15×21 一个道理）。
  作废的只有「没走完的那一趟」，宝石/熟练度/图鉴/统计四个永久键都不受影响。

## Boss 房与怪物分段（2026-09）

- **每 10 层就是一层 Boss 房**（`BOSS_EVERY`，第 10/20/30/40/50 层；**无尽章是每 10 层一直下去**）：`genFloor()` 一进来就分岔到
  `genBossRoom()` —— **一间 9×7 的屋子，屋里只有一只 Boss**，没有别的怪，也没有金币/泉/箱/坛/商。
  人在门这一侧、Boss 在屋子正中，隔三格，**取景框里一进来就看得见**（别把屋子放大回 11 宽，
  那样 Boss 会掉到取景框外面）。阶梯照旧在它倒下的地方裂开，那套不动。
  ⚠️ 原来「第 10/20/30/40 层在普通层里塞一只守层者」的写法**已经删了**，别加回来。
- **顶栏把 Boss 层写成 `10*` 并染成血色**：`renderHud()` 里 `isBossFloor()` + `style.css` 的
  `.hi b.bossfloor{color:var(--blood)}`。
- **Boss 数值参照上一层的小怪**（`makeBossFoe()` → `refFoe(G.floor-1)`，取那一层所有会出的怪的平均值）：
  **血量 ×5（`BOSS_HP_X`）、伤害 ×1.5（`BOSS_DMG_X`）**。
  ⚠️ 一整层只有一只怪，**经验 ×8、金币 ×12**（`BOSS_XP_X` / `BOSS_GOLD_X`）——
  不补这一笔，等级曲线和金币会在 Boss 层断一截。金币记在怪身上的 **`m.loot`**，
  `closeBattleWin()` 里跟着掉落一起结算（进续玩档，字段名 `lt`，老档 `|| 0`）。
  ⚠️ **第 50 层是章末 Boss**（`CH.boss`，`fixed:true`）：只借这间屋子的布局，
  数值还是它自己写死的那一套，不吃上面这几个倍率。
  ⚠️ **无尽章没有章末 Boss**（`CH.boss` 是 `null`）：每一间 Boss 房里都是跟着层数长的层间守者，
  所以上面这几个倍率一路吃到底，另外还要再吃一层深渊压迫（见「章节系统」里第五章那一段）。
- **怪物按层段再乘一次倍率**（`content.js` 的 `FOE_BANDS`，用户 2026-09 定的）：
  基础值 + 层数成长 + 章节 `foeBonus` **全部算完之后**，在 `foeNums()` 末尾乘一次、取整、最低 1。

  | 层段 | 血量 | 伤害 |
  | --- | --- | --- |
  | 1–10 | ×1 | **×0.67** |
  | 11–19 | ×1 | **×1.33** |
  | 20–29 | ×1 | **×1.33** |
  | 30–39 | **×2** | **×1.4** |
  | 40–50 | **×2.5** | **×2** |

  ⚠️ 最后一档的 `to` 是 999，所以**无尽章第 50 层往下也一直吃这一档** ——
  再往下的压力交给 `endlessRamp()`（只加伤害，见「章节系统」里第五章那一段）。

  ⚠️ **11–19 那一档的 ×1.33 是用户 2026-09 说的「10–20 层伤害 +33%」** ——
  第 10 / 20 层本来就是 Boss 层，不走普通怪这条线（第 20 层那只 Boss 参照第 19 层的怪，吃得到）。
  ⚠️ **只作用在会成长的怪身上**（普通怪 + Boss 房那只），章末 Boss（`fixed`）不吃。
  改这张表前先跑一遍 `refFoe()` 看看各层的血/伤害，50 层的曲线很敏感。

## 洞窟那张卡片（2026-09）

- **`ROUTES[].desc` 只写到「50 层」为止**（用户 2026-09 要求把后面那半句删掉）——
  宝石倍率、章末 Boss 是谁，都不在卡片上写了。
- **每张卡片背后铺一条淡蓝，宽度 = 这一章的词遇见过多少**，右上角再写一遍「遇见 N%」。
  数在 `chapterSeenPct(章号)` 里现算：分母是这一档难度的全部词（`BYLV[wordLv]`），
  分子是**熟练度表 `LEX` 里已经有记录**的那些（过一遍 `WMAP`，老档里删掉的词不算）。
  ⚠️ 淡蓝是 `.route .rfill`，**是背景** —— `.rt` / `.rn` / `.rd` / `.rgo` 都得 `position:relative`
  才顶得到它上面；颜色也要够淡，铺到 100% 时卡片上的字还得看得清（量过）。
  ⚠️ `.route` 右边留了 64px 的 `padding` 给右上角那个百分数，别收掉。

## 练习模式（2026-09）

- **在洞窟弹层里选路线之前勾**（`#btnPractice` → 模块级 `practiceOn`，`openCave()` 里 `renderPractice()`）。
  它是**纯局前选项**，不进 `OPT`、不落盘；`newRun()` 把它写进 **`P.practice`**，所以跟着续玩档走
  （`resumeRun()` 里兜了一句 `typeof P.practice !== "boolean"`）。
- 效果全在 **`stats()` 的最后一步**：`def += CHAPTER.practiceDef`(50)、
  `atk = round(atk × CHAPTER.practiceAtkMult)`(×0.5)。
  护甲那条链最低仍掉 1 点，所以练习模式等于「每下只掉 1 血、砍人慢一倍」。
  ⚠️ **放在 `stats()` 最后** —— 等级和所有遗物都算完了再压这一刀。
- 结算界面上会多一行「练习模式」，免得回头看不懂这一趟为什么这么轻松。宝石照常给。

## 动效（2026-09 加的两条）

- **掉血 / 回血各播一次 0.65s 的反馈**（`HP_FX_MS`）：`paintHp()` 把上一次的血量记在血条自己的
  **`data-hp`** 上，一发现变了就挂 `.hurt` / `.heal`，0.65s 后摘掉。两条血条（地图 `#hpBar`、
  战斗 `#bHpBar`）各记各的，所以不用去每个改血的地方挂钩子。
  ⚠️ **新的一趟 / 读档 / 回主城都要 `resetHpFx()`** —— 不然一进门就白播一下（跟 `comboShown` 同理）。
  ⚠️ CSS 里的闪光走 **`.hpbar::after`**，**不能用 `inset box-shadow`** ——
  `.fill` / `.sh` / `.txt` 都是定位子元素，会把父元素的内阴影整个盖住。
- **升级是一圈光晕 + 一圈金色粒子，1.5s**（`LEVEL_FX_MS`，`fxLevelUp()` 在 `gainXp()` 里调）：
  起点优先取地图上主角那一格，战斗中地图在弹层底下就退到 `#bHpBar`，都量不到就用 `#stageBox`。
  `.lvring` / `.lvmote` 挂在 body 上、**z-index 61**（要压过 veil 的 30），1.5s 后一起删。
- 两条都吃 `REDUCE_MOTION`：系统开了「减少动态效果」就整段跳过。
- **经验条加粗了一倍并改成金色**（用户 2026-09）：`.xpbar` 5 → **10px**、`.mapui .xpbar` 3 → **6px**，
  填充色从绿换成 `--coin`。加粗的是条子本身的高度 —— 它横向本来就铺满，没有别的「宽度」可翻。

## 界面上几条已经定下来的规矩

- **地牢里没有光源／雾**（用户 2026-09：「不需要暗黑这样的光源效果」）：`fov()` 现在把
  `G.vis` / `G.seen` 整张铺成 true，取景框里的一切都是亮的 —— 没有黑下去的未探索格（`.c.dark`），
  也没有「走出视野淡一档」（`.c.mem`）。**看得见多少只由镜头决定**（`CHAPTER.view` / `viewMax`），
  不再由半径决定。**别再把「半径 R + 视线遮挡 `los()`」那一套加回来**，那是用户明确不要的。
  两张表和 `style.css` 里的 `.c.dark` / `.c.mem.*` 规则都留着（`render()`、`findPath()`、
  续玩档都按它们写的，抹掉要动一片地方），现在只是永远走不到那几条分支。
- **镜头永远居中。** `camera()` **不夹地图边界**（以前夹了，走到边缘人就跑到画面边上）。
  走到边缘时取景窗会露出地图外面，所以 `.stage` 的背景色跟未探索格子 `--unseen` 一样，看不出接缝。
  **别再把 clamp 加回来。**
- **走一格 144ms**（`CHAPTER.stepMs`，用户 2026-09 把移动速度降到原来的 75%，原本 108ms）——
  `stepWalk()` 末尾那个 `setTimeout` 读它，别再写死数字。
- **1~20 级的升级经验 ×1.5**（`CHAPTER.xpEarlyTo` / `xpEarlyMult`，用户 2026-09）：
  `xpNeed()` = `ceil((5 + 等级×3) × 1.5)`，21 级往上照旧。「速成」的 −20% 还是在最后乘。
- **自动寻路会绕开泉/坛/箱/商/楼梯**（`findPath` 里的 `isStop()`）—— 它们是「互动」不是「路过」，
  顺路踩进去很烦。金币不算，路过顺手捡了正好。想用它们就**直接点它那一格**（终点不受限制）。
  真被堵死时 `goTo()` 会用 `findPath(tx,ty,true)` 再找一次不绕的路，不会走不了。
  ⚠️ **楼梯不在 `G.things` 里**（它是 `G.stair`），所以 `isStop()` 里单开了一条分支；
  而且只在**这一层清空之后**才算障碍 —— 清空前那一格只是个看不见的占位，踩上去什么都不会发生。
- **取景框左下角的「寻路」是个开关**（`#btnPathfind` → `autoToggle()`，2026-09 用户要求，
  不是点一下走一段）：开着就一路走下去，**下楼照旧要确认**，它不会替玩家按「下去」。
  ⚠️ **每下一层都要重新手动开**（用户 2026-09 改的，以前是自动接着走）——
  `nextFloor()` 里一句 `autoOff()`，新一层的怪在哪都还不知道，别让它自己就冲出去了。
  目标是**怪和金币放一起比、谁近去谁**（用户 2026-09 改的，以前是「先把怪清完再捡钱」——
  顺路的一堆金币不该等你横穿半张地图回来捡）；
  ⚠️ **先把当前这间屋子扫干净**（用户 2026-09）：`autoPath()` 先用 `roomOf(P.x,P.y)` 找出脚下这间房
  （`G.rooms`，genFloor 存的矩形），屋里还有怪或金币就**只在屋里挑最近的**，屋里空了才去下一间。
  老续玩档没存 `G.rooms`，`roomOf()` 返回 null，自然退回原来的「全图最近」；
  **两样都没有了才去楼梯**；
  再没有就**自己关掉**（**不去泉/箱/坛/商**，要不要进那些是玩家自己决定的）。
  「最近」按 BFS 的真实脚程算，不是直线距离。
  实现是 `autoTick()` 的 160ms 轮询：弹层开着（战斗 / 三选一 / 商店 / 下楼确认）就等，
  还在走就不打扰，停下来了才挑下一个目标 —— 一个轮询把所有「暂停」的情况都吃掉了，
  别再去每个弹层的关闭处挂钩子。
  **玩家一动手就关**（`autoOff()`）：方向键、点地图、撤退、放弃、回主城。
  ⚠️ 这个按钮的 click **不过 `gate()`** —— 战斗刚结束那 260ms 的输入锁会把「停下」那一下吃掉。
  ⚠️ `goTo()` 返回的是「有没有找到路」，`autoPath()` 就看这个。**别改成看 `walkPath`** ——
  目标就在脚边时 `stepWalk()` 会同步开战并把 `walkPath` 清空，看它等于刚开打就把自己关了（踩过）。
  它用的是 `findPath(tx,ty,loose,blind)` 的 **`blind` 档：不管探没探索过** ——
  当年是因为刚进一层雾还没开，按了等于没按。**现在地图全亮（见下面「没有光源／雾」那条），
  `G.seen` 恒为 true，blind 和普通档等价**，那个参数留着是为了不动一片调用点。
- **下楼要确认。** 踩上 ▼ 不再直接掉下去，先弹 `veilStair`（`askStair()`）。
  选「再待一会儿」会**自动从阶梯上退开一格**（用户 2026-09，`stepOffStair()`）：
  **先往下，下面是墙就往上**，上下都不行才左右兜一下，四个方向全堵死才留在原地。
  ⚠️ 只落到**空地板**上（跳过怪、泉、坛、箱、商、金币）—— 刚说了「再待一会儿」，
  不该顺手把人推进另一个弹层里，也不该替他开一场仗。
  ⚠️ 同时 `autoOff()` **关掉自动寻路** —— 不关的话它下一拍就把人领回阶梯上、再弹一次窗，死循环。
  退开之后想走，**点一下那个 ▼** 就重新弹（`#map` 的 click 里有这一分支）。
- **「押注」这个词在界面上已经全改成「冒险」**（代码里的标识符还是 `wager` / `B.wager`，没改）。
  「锁定冒险」= `OPT.lock`，开着就每题自动 `B.wager = true`（拼写题除外）。
  它在**取景框右下角**（`.mapacts.right` 里的 `#btnLockWager`，用户 2026-09 从地图下面那一行挪上来的），
  跟左下角的「放弃」「寻路」是同一种按钮、同一种 `.on` 开关状态。
  ⚠️ 按钮上**只写「锁定冒险」四个字**，开关状态靠亮起来表示 —— 别把「· 开 / · 关」写回去，那儿放不下。
  ⚠️ 地图下面那一行 `#mapTools` / `.maptools` / `.mapbtn` **已经整块删了**，空出来的纵向空间归日志栏，
  别再加回来。
  ⚠️ **冒险的效果就是按钮上写的那两句**（用户 2026-09 报「开了跟没开一样」，已修）：
  答对 **`pct += 100`**（②层的百分比，跟「开场」「见面礼」同一个写法，**不许给它开第三个乘区**），
  答错 **`dmg *= 2`**。以前是「+3 点伤 / +2 掉血」—— 攻击四十几的时候 +3 点伤等于没有，所以才像没生效。
  「赌徒」也跟着回到它自己的 **+2 点伤**（以前是把冒险的 +3 顶成 +5，两件事混在一行里）。
  冒险按钮上的两行字必须走 `setWagerLabel()` —— **直接 `textContent =` 会把里面的 `<em>` 干掉**（踩过）。
- **设置页的「本地存档文件」和「跨设备导出码」两个面板是 `hidden` 的**（`#panelSaveFile` / `#panelCode`）。
  用户要求藏起来，代码全留着，去掉 `hidden` 就回来 —— 别因为"没用到"就删掉那套逻辑。
- **战斗四个选项是 2×2 的正方块**（`.opts` / `.opt`，`aspect-ratio:1`，宽度 `min(100%, 40vh)` 卡住高度），
  序号 1234 是左上角的小方块。⚠️ 设置页的复选框以前也叫 `.opt` 且定义在后面，偷偷覆盖了战斗选项的
  `display/padding` —— 已改名 `.setopt`，**别再让这两个重名**。
  ⚠️ **`.opt` / `.lbtn` 的 `:hover` 必须关在 `@media (hover:hover)` 里**（用户 2026-09 报的 bug：
  「选完之后会留下阴影，下一题同一个位置还是暗的」）。触屏上的 hover 是**粘住**的：手指点完那一格，
  hover 底色会留到下次点别处，而下一题的新按钮又长在同一个位置上，看着就像上一题在这儿留了块阴影。
  选项按钮本身每题都是重建的（`nextQuestion()` 里 `innerHTML = ""`），DOM 上没有任何残留类名 ——
  **所以别再去 answer()/nextQuestion() 里找「忘了清的 class」，那是 CSS 的事。**
  同一条还顺手关了 `-webkit-tap-highlight-color`（iOS 点完留的那块灰罩子）。
- **捡东西有粒子**：`fxGold()` 从那一格飞到顶上的「金」，`fxKill()` 在怪倒下那一格同时甩出
  金币（飞「金」）和经验（绿色，飞「等级」），`fxRelic()` 从当前弹层飞到底部「遗物」标签，
  落点自己跳一下（`.pop`）。都走 `fxFly()`，纯装饰、挂在 body 上、飞完就删；
  系统开了「减少动态效果」（`REDUCE_MOTION`）就整段跳过。
- **复读者是「先欠着 + 5 秒补救」**（用户选的方案）：答错时伤害先记在 `B.pend` 上不扣，
  底下弹 `#btnRescue`（`openRescue()` 里 1 秒一跳的倒计时），**这五秒里 `#btnNextQ` 和「撤退」都收起来**。
  点了 → `B.rescue = true`，同一个词强制出成拼写题（`nextQuestion()` 里第一个分支）；
  拼对免伤、拼错结清；没点 → `closeRescue(true)` 自动结清。
  （`B.toChoice`「拼错退回选择题」那一套 2026-09 已经整个删了，别找。）
  补救按钮在四档屏幕上都量过，不用额外收起别的行（战斗窗已经答题前后同高）。
  （顺带记一笔：`answer()` 里那个 `note` 变量**从来没被渲染过**，是老代码留下的死变量，别指望它显示。）
- **「放弃」和「寻路」并排在取景框左下角**（`.mapacts` 里两个 `.giveup`，压在 `.stage` 上，z-index 3，
  2026-09 按用户要求放大了一倍：字号 16px、内边距 6/14）。「放弃」是：
  两步确认，第二下直接 `giveUpRun()` → `endRun(false, true)` —— **主动放弃现在也照常结算发宝石**
  （用户 2026-09 改的，以前是 `goTown()` 一颗不给），只是不计入死亡次数。
  它跟着 `stageBox` 一起显隐，所以主城里自然看不到，别再按存档状态去禁用它。
- **拼写率两个口子**：`spellChance()` = 底子 `SPELL_RATE`（5%）+ `spellBonusPct()`（刻字 +15、默诵 +30），
  盲斗直接锁 100%。**`spellBonusPct()` 就是盲斗吃的那个数**（每 1% 额外拼写率换 1% 伤害），
  加新的拼写遗物要同时改这两个函数。默诵的免伤是**每层 `RECITE_FREE` 次**（`G.reciteFree` 计数，
  `nextFloor()` 里清零；老续玩档没这个字段，判断处都写了 `|| 0`）。
- **泉水回复最大生命的 15%**（`CHAPTER.springPct`，向上取整、最少 1 点 —— 走 `springHeal()`，
  用户 2026-09 从「回满血」改的）。仍然**要先问**（`veilSpring` / `openSpring()`）：不喝就留在原地，
  回头还能来；满血时「喝下」按钮禁用。
- **连击存在 `P.combo` 上，跨怪物保留**（以前是 `B.combo`，每场清零）。只有答错、倒下、回主城才清。
  **答错分两档**（`answer()` 里那三行）：选择题答错**清零**，**拼写题拼错只减半**
  （用户 2026-09：拼写比选择难，错一次不该把长链清零）；遗物「长链」让任何答错都只减半，
  「铁胆」让冒险失手不断连击。它跟着 `P` 进续玩档，`resumeRun` 有老档兜底。
- **连击那条在怪物血条下面**（用户 2026-09 从战斗窗底下挪上来的，`index.html` 里 `#combo`
  就夹在 `.foe` 和 `.qcard` 中间，`.mybar` 现在只剩血条）。三段常驻（`renderCombo()`）：
  `.cmb-n` 连了几次 / `.cmb-p` 现在加了百分之几（金色小药丸）/ `.cmb-next` 再对几个进下一档。
  **三段永远都在，只有数字变** —— 少一段窗高就会跳。
  ⚠️ 这三个类名一开始写的是 `.cx/.cb/.cn`，**跟遗物图鉴卡片的 `.cx`/`.cn` 撞了**
  （跟当年 `.opt` 撞设置页复选框一模一样的坑），已改名带 `cmb-` 前缀，别再改回短名字。
  **涨了放大回弹（`.up`）、掉了缩一下淡一下（`.down`）**：`renderCombo()` 拿模块级的
  `comboShown` 跟这次的数比，460ms 后把类摘掉；`REDUCE_MOTION` 开着就整段跳过。
  动效只用 transform/opacity，**不许动布局**（一动就把底下的题面顶开）。
  `newRun()` 里要把 `comboShown` 清成 `null`，不然新一趟第一场会白播一次「掉了」。
  ⚠️ 连击条独占一行之后比原来（挤在 `.mybar` 里）高十来像素，
  **所以 `.opts` 的宽度上限从 33vh 再收到 31vh**（方块是正方形，那条宽度就是选项区的高度上限）——
  两处是一起的，390×667 上不收就出滚动条。
- **合成要花 500 金（`FUSE_COST`，用户 2026-09 从 300 上调），并且先弹窗二选一**：
  点「合成」走的是 `askFuse()`（开 `veilFuse`），确认了才走 `fuseGo()` 扣钱。
  钱不够时确认钮显示「金币不够」并禁用。
- **合成出来的那一件是「三件里挑一件」**（`FUSE_PICK`，2026-09 从「随机给一件」→「两件里挑」
  →「三件里挑」改了两轮）：`fuseGo()` **先把材料和钱结清**，再抽 `FUSE_PICK` 件高一档的开
  `veilFuseGot`（`openFusePick()`），点哪张 `fuseTake()` 就给哪件。那一档剩下的没拿过的件数
  不够 `FUSE_PICK` 就把有的都摆出来，一件都没有才直接给，不弹窗。
  ⚠️ `veilFuseGot` **故意不进 `hideAll()`**（材料已经砸了，窗一被顺手藏掉那件就没了），
  只进 `anyVeil()` 挡键盘；它也没有关闭钮 —— 必须挑一件。
- **合成是玩家自己挑的**：遗物页的合成面板只有两个按钮 ——「选择」（`fuseToggleMode`，进/退挑选状态）
  和「合成」（`fuseGo`）。挑选状态下整张遗物卡可点（`fusePick`），只能挑同一品质、最多 3 件，
  神圣不能当材料；分解按钮这时收起来免得误触。换到哪一件高品质**由玩家在三件里挑**（见上一条）。
  以前那套「按品质列四行、自动吃掉前三件」的 `fuserow` / `fuseRelics(rar)` 已经删了。
- **阶梯开在最后一只怪倒下的地方**（`closeBattleWin()` 里清空时改写 `G.stair`）——
  清完层不用再满地图找那个 ▼。怪站的一定是地板，所以位置永远合法。
  **清空之前地图上根本不画阶梯**（`render()` 里那个分支要 `cleared` 才走），
  「上锁的楼梯」那个淡色状态和踩上去的「石门纹丝不动」提示都已经删了 ——
  玩家看不见的东西不该给反馈。`G.stair` 在清空前只是个占位（续玩档也存它）。
- **遗物图鉴按品质从「普通」排到「神圣」**（用户要的顺序）。`content.js` 里的 `RELICS` 是按搭配线
  分组的，所以 `openCodex()` 里做了一次带下标的稳定排序 —— 同品质仍保持原顺序。
- **图鉴默认全解锁**：遗物和词汇都直接显示名字/效果/释义，没拿过、没遇到的只是**没有计数**
  （显示「还没拿到过」「还没遇到」）。以前那套 `▨▨` 遮挡已经删了，别加回来。
- **没进冒险时，信息页不显示「本局战绩」**（用户 2026-09，`#panelRun`）：
  `renderSheets()` 里按 `SCENE === "run" && G && !G.over` 决定显隐 —— 在镇上「本局」没有意义。
  「属性」和「词汇记录」两块照旧一直在。
- **图鉴标题只写「词库」两个字**（用户 2026-09）：原来那串「120 掌握 / 1382 遇到 / 4925 总数」
  搬到了**信息页那块「词汇记录」**上（用户 2026-09 从「本章词汇」改的名），摆成四格：
  **已掌握 / 已遇见 / 总数 / 累计学词**（`renderSheets()` 里）。
  「累计学词」= `LEX` 里所有词 `seen` 之和，也就是**这个存档一共答过多少道题**，跨局一直累加。
  遗物那一页的标题照旧是「遗物 N / M」。**别在图鉴标题上再摆一遍数字。**
- **图鉴有搜索框**（`#codexFind`，类名 `.findbox`，用户 2026-09）：词库按**英文 / 中文 / 类别名**找，
  ⚠️ 这个类**以前叫 `.find`，跟取景框里的寻路按钮 `.giveup.find` 撞了**，而且定义在后面 ——
  `width:100%` 被套到了按钮身上，把「放弃」挤成上下两行的「放/弃」。
  跟当年 `.opt` 撞设置页复选框、`.cx` 撞连击条是同一个坑，**别改回 `.find`**。
  （顺带给 `.giveup` 加了 `flex:none;white-space:nowrap`，那一组按钮的容器是绝对定位的，宽度会被压。）
  遗物按**名字 / 效果 / 铭文 / 品质**找。`openCodex()` 每次重画时现读这个框（`codexFind()`），
  所以两个标签页共用同一个搜索词；整类一个都没命中就连类名都不画。
  ⚠️ 输入是 **150ms 防抖**的 —— 词库有四千多张卡，每敲一下重画会卡。
  ⚠️ 从按钮进图鉴时**先把框清空**，不然上次的搜索词会把图鉴筛成空的。
  ⚠️ 键盘那条全局监听里加了一句「target 是 input/textarea 就整个放行」——
  不然在框里按回车会去点弹层的主按钮（把图鉴关掉）。
- **遗物上限 `RELIC_MAX = 15`**。带满时遗物页很长，所以**整页不滚、滚的是遗物列表**
  （用户 2026-09）：`#viewRelic{overflow:hidden}`，`.panel.grow` 吃满剩余高度、里面的 `.relics`
  自己 `overflow-y:auto`，`.panel.fusebar`（合成面板）`flex:none` **钉在标签页下方**。
  ⚠️ 别再把 `#viewRelic` 塞回那条 `overflow-y:auto` 的规则里 —— 那样合成面板又要滚半天才看得见。
  ⚠️ `.relics` 那个滚动容器留了一圈 `padding` + 负 `margin`，是给「new」小红点让位的（它探出卡片外）。
- **刚拿到的遗物在遗物页卡片左上角挂一个「new」小红点**（`newRelics` 数组 + `.relic.own.fresh`
  上的 `.newdot`），**点一下那张卡就消失**。它是**纯界面状态、不进存档** ——
  `newRun()` / `goTown()` 里清空，别给它加落盘（存档点就那四个）。
  统一入口在 `noteRelicFound()` 里 `markNewRelic()`，所以五选一 / 祭坛 / 宝箱 / 游商 / 合成 / 换装都覆盖到了。
- **换装时被换下来的那件当场分解成金币**（`doSwap()` 里按 `sellPrice()` 给钱，用户 2026-09 改的，
  以前是白扔）。遗物页上的手动「分解」按钮照旧保留。⚠️ 照例包在 `withMaxHp()` 里。
- **带满了的取舍窗里，点最上面那件新的 = 直接卖掉它**（用户 2026-09）：`#swapNew` 上挂了 click →
  `doSwap(null)`，跟底下那个「不要了，折成金币」是同一条路，卡片上顺手写了能换多少金。
- **怪倒下没有中间画面**：`finishBattle(true)` 直接 `closeBattleWin()` ——
  既没有「收取战利品」按钮，也不再弹「XX 倒下了」那一屏。看清最后一题释义的时间
  留在 `answer()` 那边（最后一击后延时 760ms 再结束）。`B.won` 是防重入的闸。
- **听音辨词已经删掉了**（用户 2026-09）。题型只剩三种：英译中 / 中译英 / 拼写。
  🔊 按钮留着 —— 答完题出现，设置里的「答完自动朗读」（`OPT.speak`）开着就自动念一遍。
- **一趟之内答对过的词不再出第二次**（用户 2026-09：「单词一局内不要出现重复的，除了答错」）：
  答对就记进 **`P.used`**（`answer()` 里），答错、或者先对后错就从里面删掉 —— 所以**错过的词照样会再来找你**。
  `pickQuizWord()` 里用 `unused()` 过滤候选池；**心魔那条分支故意不过滤**（它本来就是「这趟答错过的词」），
  「复读者」的重考也一样，是答错的路。
  ⚠️ 一章的词是问得完的（A1/A2 各 500 上下，B1/B2 各 2000）—— **挑空了就清空 `P.used` 开新一轮**；
  弱点类先挑空的话先退回全池，别直接重置。
  `P.used` 跟着 `P` 进续玩档（老档在 `resumeRun()` 里兜了一句 `if(!P.used) P.used = {}`）。
- **心魔要熬 10 题才现身**（`HAUNT_DELAY`，用户 2026-09：刚错完下一题就重考太黏人）：
  答错时 `addHaunt()` 把当时的答题数记进 **`P.hauntAt[en]`**，`hauntReady()` 要
  `P.right + P.wrong` 比它大 `HAUNT_DELAY` 才算数（跨战斗照样在走）。
  **拼写题拼错也走这条路**（用户 2026-09）—— 以前它是下一题就退回成选择题，现在一视同仁。
  战斗窗那条「心魔」标签、答错额外 1 点、驱散回血**全看 `hauntReady()`**；
  抽题只从 **`readyHaunts()`** 里挑。答对了不论熬到没熬到都从名单里拿掉
  （`dropHaunt()`），**回血和文案只给真的现身过的**。
  `P.hauntAt` 跟着 `P` 进续玩档（`RUN_V` 不用动，老档在 `resumeRun()` 里兜一句
  `if(!P.hauntAt) P.hauntAt = {}` —— 没记时间的一律当成已经熬到）。
- **出题不再往怪的弱点类偏**（用户 2026-09）：`pickQuizWord()` 里的 `catRate` 默认是 **0**
  （原来 70%），词照全池抽。**「嗅迹」是唯一的例外** —— 拿到它才拉到 90%。
  弱点本身照旧生效（打中了有点伤、猎手/破绽/追猎都还在），只是不再刻意多出弱点类的词。
- **没学过的新词权重 90%**（用户 2026-09 从 80% 上调）：`pickQuizWord()` 在过完 `unused()` 之后，
  按 `NEW_WORD_RATE`（`content.js`，0.9）掷一次 —— 掷中就**只在「熟练度表 `LEX` 里还没有记录」
  的词**里挑，也就是这个存档从没遇到过的生词。这一章的生词问完了（`fresh` 空）自然落回
  下面那个按熟练度加权的袋子，所以复习不会彻底断掉。
- **Boss 的弱点按词性挑，普通怪仍然按类别**（用户 2026-09）：`makeFoe()` 里
  Boss 走 `pick(chapterPos())`，普通怪还是 `def.cat`；哪一种写在怪身上的 **`weakPos`** 上，
  `showWeak()`（文案走 `POS_CN` 还是 `CAT_CN`）和 `answer()` 里的 `hitWeak`
  （比 `word.pos` 还是 `word.cat`）都看它。战斗窗顶上的「遭遇 · X类词」没变，还是类别。
  ⚠️ 弱点**不进存档**，`resumeRun()` 里照 `makeFoe` 的规矩补一个（Boss 读档会重掷一个词性）——
  以前这里整个漏了，读档后 Boss 就没弱点了，顺手修的。
- **拼写题是掷骰子出的**：每题独立 `Math.random() < spellChance()`，底子 `SPELL_RATE`（10%），
  「刻字」「默诵」各再 +`SPELL_RELIC_RATE`（15%），「盲斗」仍然硬锁 100%。
  **拼对的奖励是默认规则**（不靠遗物）：连击 `+SPELL_COMBO`（10，在算伤害之前加，这一刀就吃得到）、
  本场经验 ×`SPELL_XPX`（存在 `B.xpx` 上，`closeBattleWin` 里结算）。
  **拼错不再当场退回成选择题**（用户 2026-09，`B.toChoice` 那一套整个删了）——
  跟别的答错一样记进心魔，熬满 `HAUNT_DELAY`（10）题才回来找你。
  ⚠️ `answer()` 里的 `note` 变量是**死的**（verdict 只显示 head + 释义），
  想让玩家看见新反馈就写进 `head` 或 `say()`，别往 `note` 上加。
- **选择题有 7 秒读条**（`QUIZ_TIME`，用户 2026-09）：`nextQuestion()` 末尾 `startQTimer()`，
  走完还没作答走 `timeUp()` —— **超时只是「怪咬你一口」，不是判错，也不换题**（用户 2026-09 改的）：
  `LEX`、心魔、`P.used`、**连击**、赌骰全都不动，题面／四个选项／冒险按钮原样留着、答案也不揭晓，
  **读条从头再走一遍**，下一次超时就再咬一口，直到答出来（或者被咬死）为止。
  挨的那一下照旧走 `mitigate()` 那条减伤链。
  ⚠️ 别再往 `timeUp()` 里加「揭晓答案 / 禁用选项 / 露出继续钮 / 扣连击」那一套 —— 那是判错的做法，
  `TIMEOUT_COMBO_KEEP` 已经跟着删了，别找。
  ⚠️ **拼写题不限时**（字母要一个一个点），复读者的补救题也不限时 —— `startQTimer()` 自己会放过它们。
  ⚠️ 计时器存在模块级的 `qTimer` 上，**不要挂到 `B` 上** —— `B` 随时会被清成 `null`，挂上去就关不掉了。
  `answer()` / `flee()` / `finishBattle()` / `openRescue()` / `hideAll()` 都会 `clearQTimer()`。
  ⚠️ 条子 `.qtimer` 是**绝对定位压在 `.qcard` 顶边**的 3px 细条，不占纵向空间（最后 2 秒变红 `.hot`）——
  放回文档流的话战斗窗会在答完那一瞬间缩一下，「答题前后一样高」那条就破了。
- **战斗里撤退要掉一半生命上限**（`FLEE_HP_PCT`，用户 2026-09，以前是白撤）：`flee()` 里扣，
  **最低留 1 点**（撤退不会把人撤死）。怪身上掉的血照旧留着，回头还能接着打。
  按钮上写着「撤退 · 掉一半血」，别改回光秃秃的「撤退」。
- **冒险答对记 2 点连击**（用户 2026-09）：`answer()` 里 `P.combo += B.wager ? 2 : 1`。
  拼对那 `+SPELL_COMBO` 是在这之后另加的（拼写题本来就不给冒险）。
- **祭坛第 10 层之后才生成**（`ALTAR_FROM`，`genFloor()` 里那一行），
  而且有 `ALTAR_FORGE_RATE`（20%）的概率长成**熔炉**（用户 2026-09）：
  不献血换新遗物，而是**献祭身上的一件**，1/3 升一档、1/3 同档换一件、1/3 降一档。
  ⚠️ 是哪一种**第一次踩上去就定死、记在物件上**（`th.forge`，跟着 `G.things` 进续玩档）——
  不然玩家可以走开再回来反复刷。身上一件遗物都没有时退回成寻常祭坛。
  那一档没货了就按 想要的 → 原档 → 更高 → 更低 找个有货的档，全空才折成金币。
  动 `P.relics` 照例包在 `withMaxHp()` 里。
- **游商标价 = 这件的分解价 × `SHOP_MARKUP`（1.3）**（用户 2026-09 定的：商店价必须比出售价高 30%）——
  `openShop()` 里 `Math.ceil(sellPrice(r) * SHOP_MARKUP)`，进店那一刻定死存在 `th.stock[].price` 上。
  ⚠️ 老公式 `(10 + 品质×12 + 层数) × 5`（`SHOP_MULT`）已经删了，别改回去 ——
  那套在浅层比分解价还便宜，「买进来再拆掉」就是白赚金币。
  「熟客」的 −15% 是**结账时**在 `shopPrice(row)` 里现算的，1.3 × 0.85 = 1.105，仍然高于分解价；
  再动这两个数之前先把这一步算一遍。
- **游商货架 5 件**（用户 2026-09 从 3 件加到 5 件），按**三选一那种遗物卡**摆
  （`.shopit` 在 `style.css` 里，品质/名字/效果/传说各一行，买的钮压在右边）——
  以前是一行小字挤一个按钮。五件会把弹层撑到要滚，这是有意的。
- **货架能刷新 `SHOP_REROLL_N`（1）次**（2026-09 加，用户要求"商店可以刷新一次"）：
  `rollShopStock()` 是唯一的生成口（`openShop()` 首次进店、`shopReroll()` 刷新都调它），
  `th.rerollsLeft` 记这家店还能刷几次（老续玩档没有这个字段，`renderShop()` 读处 `|| 0`）。
  ⚠️ **这不是遗物效果**，是每次进店都有的基础功能，跟遗物「重掷」（三选一/五选一的重抽）
  是平行的两套，按钮 `#btnShopReroll` 独立于遗物系统。已买过的货位也会被换掉——刷新是换整批货架，
  不退钱。
- **战斗窗答题前后必须一样高**（用户要求"选错之后不要出滚动条，保持一致性"）：
  答完题会多出两块东西 —— 判定文字和喇叭钮。所以
  ① `.verdict` 的 `min-height` 提前把两行的高度留够；
  ② 喇叭钮 `.spk` **绝对定位浮在题面卡片右上角**（只有一个 🔊 图标，别给它加文字）；
  ③ 心魔标签 `.haunt[hidden]` 用 `visibility:hidden` 藏，**照样占位**。
  别把 ②③ 改回 `display:none`，一改窗就在答题瞬间长高、veil 立刻出滚动条。
- **答错之后四个选项各自摊开中英**（用户 2026-09）：`answer()` 给每个 `.opt` 追加一个
  `.sub`（英译中的题填英文、中译英的题填中文）并挂上 `.opt.two` 改成竖排两行。
  ⚠️ 字写在**方块里面**（方块是 `aspect-ratio:1`，第二行有余量），
  **`verdict` 一行都不加** —— 上面那条「答题前后一样高」不许破。
  四档屏幕量过：390×667 / 430×740 / 430×900 / 1280×720 都不出滚动条，
  最长的 `grandmother` 在方块里折成两行也还装得下。
  选项对号现在按 **`B.q.opts`（`nextQuestion()` 里排序后存的）的下标**比，
  不再去解析按钮文字 —— 按钮里多了一行字，原来那套 `textContent.replace(/^\d/,"")` 就不准了。
- **拼写题左边有个 🔊**（`#btnSpellSpeak`，在 `#spellBar` 里跟字格并排）：**进拼写题自动念一遍**，
  之后随时能再点。它**不看设置里的「答完自动朗读」**（`OPT.speak` 管的是答完那一次）。
  ⚠️ 这次自动朗读走 `util.js` 的 **`speakQueued()`**：**等上一段读音念完了再念**
  （用户 2026-09）—— `speak()` 自己会先 `speechSynthesis.cancel()`，直接用它会把
  上一题答完那次朗读掐断、两个词叠在一起听，**别改回 `speak()`**。
  它按 120ms 轮询 `speaking / pending`，**最多等 4 秒**（有的浏览器 `speaking` 会卡住不落）；
  等待期间有人手动点朗读就把这一段掐掉（`speak()` 开头会清掉那个轮询）。
  ⚠️ 它占掉一截横向宽度，所以 `.spellrow.long .sbox` 又收窄了一号 ——
  最长的词是 11 个字母（grandmother 那一批），四档屏幕上都量过是**一行**，折行就会顶高战斗窗。
- **拼写题的字母键是 5 列的方块**（`.letters` 用 grid，`.lbtn` 是 `aspect-ratio:1`）——
  原来是 36px 的定宽小键，太小了。现在 390×667 上是 62px 见方。
  字母键和字格 `.sbox` 的尺寸也全是 `clamp(最小, xvh, 最大)`，跟战斗窗一个规矩。
- **战斗窗必须整屏放得下**（用户要求"能在屏幕里读完所有信息"）：`.battle` 里立绘、题面字号、
  选项方块、内外间距**全部用 `clamp(最小, xvh, 最大)`**，选项容器还有个 318px 的像素上限 ——
  高屏上方块再长大反而会把窗顶出去。改战斗界面的任何尺寸，都要回头量一遍
  390×667 / 430×740 / 430×900 / 1280×720 这几档还装不装得下。
- **页面缩放整个钉死**（用户要求"固定住"）：两个 viewport meta 都带
  `maximum-scale=1, user-scalable=no`，CSS 里 `html{touch-action:manipulation}`，
  index.html 顶部还有一段 JS 兜底（iOS 不认 user-scalable）：`gesturestart/change/end` 全 preventDefault，
  再加「同一位置 300ms 内第二次触摸」。
  ⚠️ 那段 touchend 兜底**必须带位置判断并放过 button/input/label/a/.c** ——
  见到第二次触摸就无脑 preventDefault 会把第二次的 click 一起吃掉，连点两下按钮就有一下不算数。
- **生命上限涨了要补当前血**：所有改 `P.relics` 的地方都包在 `withMaxHp()` 里 ——
  上限 +8 时当前血也 +8（20/30 → 28/38），换掉/拆掉加血遗物时把当前血压回新上限（至少留 1 点）。
  新增任何会动遗物的入口，**必须也从 `withMaxHp()` 过**。

- **美术的明暗一律是黑白半透明叠在 `currentColor` 上**（暗 `#000` .15 左右、亮 `#fff` .2 左右），
  **不要写死灰色** —— 同一张图要在深灰褐（普通怪）、红（BOSS `#8A3223`）、蓝（主角 `--hero`）下都成立。
  约定颜色：眼白 `#FBF6E7`、深孔/瞳仁 `#2A2620`、发光的眼金 `#E0A83A` 或血 `#D8412F`。
  ⚠️ **不要用 `clipPath` / `mask`** —— 地图格子和战斗立绘可能同屏，重名 id 会互相串；
  明暗块的坐标是照着主体轮廓手算的，**挪了主体就得跟着改明暗**，否则会漏色到轮廓外。
  几个踩过的：BOSS 的角画成竖直叶片会变成一对兔耳（要粗根细尖、朝外上方横着弯）；
  斧刃别抬到 y<36，那是角的地盘。
- **地图上一个文字都没有**（用户 2026-09 要求），全是画出来的：人 `HERO`、泉 `SPRING`、
  商 `SHOP`、金币 `COIN`、箱 `CHEST`、坛 `ALTAR`、阶梯 `STAIR`，怪走 `MOB_ART`
  （键是 `def.id`，跟 `content.js` 的 FOES/BOSS/GATEKEEPER 对齐）。
  空地板上那个点是 CSS 画的（`.c.dot::before`）——
  **别把它改成 SVG**，空地板占了整张地图九成的格子，每帧塞 innerHTML 会拖垮走路；
  也别改用 `::after`，那个被目标格的脉冲圈占了，而一格可能同时是空地板和目标格。
  `render()` 里用一个 `art` 变量收口：谁设了就 `innerHTML`，没设才 `textContent = glyph`。
  **加新怪不画图也不会开天窗** —— `MOB_ART` 里找不到 id 就自动退回原来那个汉字，
  `.c` 上的 `font-*` 那几条就是留给这个兜底的，别因为"地图上没字了"把它们删了。
  再加这类图要动三处：`render()` 里对应的分支、`style.css` 里给它一个 `color`、
  以及 `.c.mem.* svg{opacity:.5}` 那条 —— 图里有写死的固定色（石头、描边、金锁），
  走出视野光调 `color` 淡不下来。（阶梯故意不在那条里：清空后才出现，淡了反而找不着。）
  ⚠️ `.c.mem.*` 那几条**现在已经走不到了**（地图全亮，没有「走出视野」这回事），留着是备用。
  ⚠️ **`MOB_ART` 跟战斗立绘 `ART` 是两套东西，别混**：格子只有二三十像素，
  `MOB_ART` 里只有轮廓 + 一对眼睛，没有地影和明暗，**别往里加细节**，加了只会糊成一团。
  区分全靠外形差异 —— 游影是尖顶、幽魂是圆顶，画成一样的就在小格子里分不出来了。
- **阶梯就是一个倒三角**（用户 2026-09 要求）。中间改成过「四级往下退的台阶」，用户要回退，
  别再改。它是 SVG 不是 ▼ 字符，只为跟别的东西走同一条路，看到的还是那个倒三角。
- **十一只怪各有各的立绘**：`FOES` 里的 `art` 字段现在跟 `id` 一一对应，没有再借用的了
  （以前锈钟怪借石像、回廊游影借幽魂、吞惧者借泥怪、碎色棱借长足蛛，名字和形象对不上）。
  加新怪时 `art` 记得配一张，`ART` 里缺了会画不出立绘。
- **怪和物件不许堵路**（`genFloor()` 里的 `spots` / `doors` / `rest` 三档 + 一道连通性守卫）：
  走廊只有一格宽，怪、泉、箱、坛、商、金币站上去就把路卡死，门口那一格同理。
  所以候选位置**只取房间内部、且不挨着走廊的格子**；后两档是极端小地图的兜底，正常一层用不到。
  ⚠️ 一层现在站着十来只怪，光挑「房间内部」已经不够 —— 四只怪在 4 宽的屋里排成一行就把屋子切两半。
  所以 `take()` 里加了**连通性守卫**：每放一个先把它当墙做一次 flood fill，
  剩下的地板必须仍然从脚下连成一片，不成就丢掉这一格换一个。
  改生成逻辑后回归的办法就是这一条（对全部怪 + 物件做一次）。
  最近一次跑 500 层：**堵点 0**，怪 10–13、金币 7–10、占用槽位 4–6。

## 存档系统

全在浏览器 localStorage 里，六个键（`util.js` 的 `load` / `save` 负责读写）：

| 键 | 存什么 | 什么时候清 |
| --- | --- | --- |
| `youxu.a1lex.v1` | 每个词的熟练度 `{str, seen, wrong}` | 永久 |
| `youxu.codex.v1` | 遗物图鉴（初见层数、拿过几次） | 永久 |
| `youxu.meta2.v1` | 最深层 / 探索次数 / 通关 / 死亡 / 最后写入时间 `t` | 永久 |
| `youxu.town.v1` | 镇上宝石（字段 `gem`，老档的 `gold` 会自动接过来） | 永久 |
| `youxu.opt.v1` | 设置项（发音、自动下一题、锁定冒险 `lock`） | 永久 |
| `youxu.run.v1` | 没走完的那一趟（地图压成字符串，怪只存 defId + 状态） | 死透 / 通关 / 放弃就删 |

> 续玩档里 2026-09 又多了两样：**`rooms`**（这一层每间房的矩形，自动寻路按它分屋子）和怪身上的
> **`lt`**（Boss 房那只压着的一层份金币，读回 `m.loot`）。两样都是**加字段**，
> **`RUN_V` 不用动** —— 老档没有就 `|| null` / `|| 0` 兜底。

**读是自动的，存只有三下 + 宝石一变就存**（别再加别的存档时机）：
- 读：`boot()` 里 `readRun()` 有档就直接 `resumeRun()`，**不弹窗不询问**。
  以前那个「继续 / 重新开始」弹层（`veilResume`）已经删掉了，别再加回来。
- 存：**只有这三个时刻**，全部走 `game.js` 的 `commit(keepRun)`：
  1. **进入关卡** —— `enterRoute` → `newRun` → `nextFloor` 里那一次 `commit(true)`
  2. **下一层** —— `nextFloor()` 末尾 `commit(true)`
  3. **回到主城** —— `goTown()` 的 `commit(false)`；死亡/通关的结算 `endRun()` 也算这一档（也是 `commit(false)`）
  4. **宝石变动** —— `addGems()` 里的 `commitPerm()`（用户 2026-09 加的第四个存档点，只写永久键，不碰续玩档）
- 所以续玩档存的是「**刚踏进这一层时**的样子」：中途关页面 = 退回本层开头重来，
  这一层里打的怪、捡的金币、拿的遗物都不算数。`resumeRun` 的文案已经这么写了。
- 以前挂在这儿的 `pagehide` / `freeze` / `blur` / `visibilitychange` 四个监听和
  `setInterval(saveRun, 15000)` **全删了**，走路/战斗/拿遗物/买卖里的 `saveRun()` 也全删了。
  别再加回来。

代码上的规矩：
- `LEX`（熟练度）、`CODEX`（图鉴）、`MET`（统计）、`TOWN`（存款）四份**常驻内存**，
  游戏过程中只改内存对象，落盘交给 `commit()`。别在别处直接 `put(某个_KEY, …)`。
- `commitPerm()` 只写这四个永久键；`commit(keepRun)` = `commitPerm()` + 写/删续玩档。
  导入存档走 `commitPerm()`（它不是游戏里的存档点，是存档管理，且不该动层存档）。
- `meta()` 返回的就是内存里的 `MET`，改完等 `commit()`，不要再单独写 localStorage。
- 设置项 `OPT` 是例外，改了立刻 `saveOpt()` 落盘 —— 它不是游戏进度。
- **设置页的「清除全部存档」按钮已经删了**（用户 2026-09），连同它两步确认的那一段监听。
  以后要再开一个抹档的口子：删完 localStorage **必须同时把内存里的 LEX/CODEX/MET/TOWN 清空**，
  否则紧接着的 `goTown()` 会把旧数据原样写回去。
- 首次打开**不再自动弹玩法说明**（用户后面要做新手教程关卡替代它）。
  `veilHelp` 本身留着，设置页的「玩法说明」按钮还能打开。
- 每一次实际写 localStorage 都过 `game.js` 的 `put()`：`util.js` 的 `save()` 现在返回布尔，
  写不进去（无痕模式、存储被禁、配额满）会顶到页面顶部的报错横幅上（`window.showErr`），
  不再静默丢档。**别直接用 `save()`；也别直接用 `put()` —— 走 `commit()`。**

搬家有两条路，都在设置页：

1. **本地存档文件**（`snapshot()` → 一个 `.json`，上面六个键全在里面）。
   选中或拖进来会**立刻读出来**，先把信息摊在卡片上（层数、等级、掌握词数、存款、存档时间），
   再让玩家选「合并导入」还是「整档覆盖」。覆盖会 `location.reload()` —— 页面上到处是旧数字，
   重载最干净，而且开局流程本来就会问要不要接着走那一趟。
2. **导出码**（`YX1.` + base64），只带永久数据，不带那一趟。两条路共用 `mergeData()`。

合并规矩（`mergeData`）：熟练度取高的、图鉴取并集、统计取大值、**存款取大值不相加**（免得来回导两次就富了）、
续玩档只在本地没有在进行的探索时才接过来。

⚠️ **以后新增任何 localStorage 键，必须同时改四个地方**：`commit()` / `commitPerm()`（落盘）、
`snapshot()`（导出）、`overwriteAll()`（覆盖）、`mergeData()`（合并）。
（第五处「清除全部存档」的键列表已经随按钮一起删了。）
漏了就会出现「导出了但没带过去」的静默 bug。

## 部署机制

`.github/workflows/deploy.yml`：推到 `main` 就自动部署，也可以手动触发（`workflow_dispatch`）。
流程是 checkout → configure-pages → 把游戏文件拷进 `_site` → upload-pages-artifact → deploy-pages。
通常 30～60 秒上线。

仓库 Settings → Pages 的 Source 已经设成 **GitHub Actions**，这是一次性设置，已完成。

## 踩过的坑（别再踩）

- **不要用 `actions/configure-pages` 的 `enablement: true`。** 第一次部署时它会失败：
  `Create Pages site failed. Error: Resource not accessible by integration`。
  Actions 的 GITHUB_TOKEN 没有创建 Pages 站点的权限，无论 workflow 的 `permissions` 块写什么都不行。
  必须由仓库主人在 Settings 里手动开一次（已经开好了）。
- **Claude 的 GitHub App 不能创建仓库**（`POST /user/repos` 返回 403）。需要新仓库时让用户自己建。
- **workflow 是显式列举要发布哪些文件的**（`cp index.html style.css *.js .nojekyll _site/`）。
  新增任何需要上线的文件，**必须同时加进这个 cp 列表**，否则不会出现在网站上。
  ⚠️ 自定义域名的 `CNAME` 文件是例外：cp 那一步已经加了 `if [ -f CNAME ]` 的分支，
  仓库根有就自动带上、没有就跳过，绑域名时不用再改 workflow。
- **Claude 的沙盒访问不了 `*.github.io`**（代理返回 `403 to CONNECT`）。
  所以没法用 curl 自测线上网站，只能看 Actions 日志判断部署是否成功，或者请用户帮忙打开确认。
  本地自测可以：`python3 -m http.server` 然后用 Playwright 打开（Chromium 在 `/opt/pw-browsers`）。
- 页面顶部内置了报错横幅（`#errbar`），JS 一出错会直接显示在页面上，不用开 F12。排查问题时先问用户看没看到它。

## 遗物系统（2026-09 大改）

- 数据和数值的**唯一说明书是 `遗物数据表.md`** —— 用户改那份，Claude 照着改 `content.js` / `game.js`。
  代码改了数值，那份文档必须同步。
- 「窄视」「清算」**已删**。删遗物要连 `hasRelic()` 分支一起删，并且靠 `resumeRun()` 里那两句兜底：
  老续玩档的 `P.relics` 和游商货架 `th.stock` 会被过滤掉已经不存在的 id，别把这两句删了。
- **现在 84 件**：第二批 25 件（防御线 / 成长线 / 局内资源线 / 4 件神圣）、
  史诗「续弦」1 件（答对 5% 把连击接回这一趟的最大值，
  判定必须在「这次答对计入 `maxCombo` 之前」，否则等于接回自己）、
  第三批 12 件（弱点 / 暴击 / 房间 / 节奏 / 拼写）、第四批 8 件（防御端：超时 / 撤退 / 护盾 / 连击换免伤）、
  第五批 13 件（护盾流补完）、第六批 10 件（防御端「单件就有体感」）。
  （2026-09 用户把**铁鳞**和**药膏**从普通提到了稀有，药膏顺带改成「75% 概率回 4 点」。）
  **现在 152 件**（第七批 37 件：防御 29 + 机制 8，见下面单独一节），
  品质分布：普通 22 · 稀有 45 · 史诗 45 · 传奇 29 · 神圣 11。
  **攻防比**：进攻 42 · 防御 **87** · 经济 10 · 工具 13（第五批之前是 42 : 31）。
  2026-09 另外重做了三件（**数值和机制看 `遗物数据表.md`**）：
  **盲斗** = 全拼写 + **答对一击必杀**（`answer()` 里砍完把 `m.hp` 清零）+ **每下一层楼梯走 `BLIND_STEP`(4) 层**
  （`nextFloor()` 开头，⚠️ 撞到章末那层就停下，不能跳过章末 Boss）；原来那套「额外伤害 +3、
  拼写率换伤害」已经删了。
  **割裂** = **每答一题**（对错都算）自伤 1 点，累计割到 `REND_CAP`(100) 点就停手（记在 `P.rend`），
  **额外伤害 +`REND_EXTRA`(25)**。
  **钥匙** = 除了「拼错也开箱」，**拼对**时箱底那件变成**二选一**（借 `veilFuseGot` 那个窗，
  `openFusePick(picks, rar, {via:"grant", ...})` → `fuseTake()` 走 `grantRelic`，所以带满了照样弹取舍窗；
  撬开的那一次不给）。
  ⚠️ **神圣从 3 件变成 11 件**，合成出来的那一件现在是**三件里自己挑**（`FUSE_PICK`，
  2026-09 从「两件里挑」再上调一次）。

### 第七批（2026-09）· 防御 29 件 + 机制 8 件 —— 用户先要清单、自己挑的这一批

- **先出候选清单再选**：这次不是用户直接改 `遗物数据表.md`，是先在对话里让 Claude 想了
  "30 件防御 + 30 件机制"的候选清单（按流派分组、品质打散），用户从里面挑了 37 件（29 防御 +
  8 机制），品质/效果文案是挑的时候定的，**数值是 Claude 对照现有机制写的**（挂哪一层、
  具体公式，写在 `遗物数据表.md` 第七批那一节）。
- **同一批用户还要求"先只做被动类的"**——候选清单里原本有三件要点按钮的主动技能
  （护身符 / 喘口气 / 典当，每层/每场限一次的手动格挡/跳题/花钱抵伤），**这一批没做**，
  留着等用户明确要主动技能再补。
- **新的机制全是"熟悉度"和"资源会变化"**，跟前六批的"再叠一层百分比"不一样：
  - `m.hitsLanded`（挂在怪身上）/ `G.catSeen`（这层同类别遇到几只）/ `P.bossSeen`（这趟遇到
    几次同名 Boss，跟着续玩档不清零）—— 记仇 / 面熟 / 老对手三件的底层
  - `B.corrodeLoss`（护甲会被磨掉、答对又长回来）—— 蚀甲，⚠️ 放在 `stats()` 的 `defGear`
    快照**之前**减，铁壁的伤害加成才会跟着甲一起掉
  - **护盾从"有"变成"没有"是一个共享事件**：`takeHit()` 里判一次 `hadShield && 现在没了`，
    借甲 / 破盾余威 / 久经三件都挂在这同一个判定上，别各写一份
  - **轻装上阵的 `P.everSwapped`**：只要有一处会让你**失去一件已拥有的**遗物就要置真——
    手动分解 (`sellRelic`) / 换件的旧件 (`doSwap` 有 `dropId` 那支) / 熔炉 (`forgePick`)，
    三处都要覆盖到；`doSwap(null)`（带满不换、直接卖新捡的）不算，那件没真正进过背包
  - **`sellRelicGold(r)` 收口了寄存的护盾转化**：`sellPrice()` 本身**不能**带副作用
    （它还给分解按钮价签、游商标价这些"只是显示"的地方用），真的把遗物变钱的四处
    （手动分解 / 换件旧的 / 不换直接卖新的 / 熔炉吃不到新遗物）都过 `sellRelicGold()`
  - **重掷按钮的次数从布尔变成了计数**：`G.rerollUsed` 以前是 `true/false`（拿了「重掷」
    就一层一次），现在是数字，`rerollBudget()` = 拿了重掷 1 次 + 集齐条件满足再 1 次。
    老档存的布尔值 JS 比较时会自动当 `1/0`，不用迁移
- **8 件"机制"归到了攻防比的哪一类**：集齐/博闻算工具，惯性/计数器/归位/循迹算防御
  （都是保命/续航向），施粥/均富算经济——这条分类是粗略估的，供参考，不是精确统计。

### 第二批（2026-09）新加的几个口子 —— 加遗物前先看这一段

- **数值口全部函数化了，别再直接读常量**：
  `relicCap()`（= `RELIC_MAX` + 行囊 3）、`fuseN()` / `fuseCost()`（配方：2 件 / 250 金）、
  `shopPrice(row)`（熟客 −15%，**结账时现算**，所以进店之后拿到它也算数）。
  货架上的 `row.price` 存的永远是原价。
- **挨打这一侧现在有一条减伤链 `mitigate(dmg, s)`**，顺序写死：
  减伤百分比（软甲 10 + 皮甲 5，**相加后只乘一次、向下取整**）→ 屏息每层前 `HOLD_FREE`(2) 次减半
  → 钝痛封到上限 10% → 错身 25% 归零。**链算完之后才落到身上：先扣护盾、再扣血**（见第四批那一段）。
  ⚠️ 它**只在真的要掉血时才调**（默诵/回声先挡，挡掉了就别进来）——
  否则会白白吃掉屏息的次数、白掷一次错身。新增防御遗物就往这个函数里加，别散在 `answer()` 里。
  ⚠️ 这条线里**故意没有「固定减伤」**：第 1 层的怪只打 1~2 点，「每次少挨 3 点」等于开局无敌，
  后期又等于没有 —— 跟「不给后面的章加护甲」是同一个数学。要加就加百分比或概率。
- **`P` 上多了四个字段**（跟着 `P` 进续玩档，所有读的地方都写了 `|| 0`，**不用动 `RUN_V`**）：
  `spent`（这趟花掉的金，散财用）、`bonusAtk`（烙印）、`bonusHp`（铭心）、`chew`（反刍欠着的那一次）。
  烙印/铭心是**累积**的：只有拿到遗物之后升的级才算，换掉遗物也不还回去。
- **`G` 上多了两个每层清零的计数**（`nextFloor()` 里）：`holdUsed`（屏息）、`rerollUsed`（重掷）。
- **新遗物的反馈一律写 `relicLog`**（`answer()` 里，接在底下那条 `say()` 后面）。
  ⚠️ 别往 `note` 上加 —— 那个变量从来没被渲染过（老代码留的死变量，前面也记过一笔）。
  想让玩家在战斗窗里**当场**看见，只能写进 `head`（错身就是这么做的），但别加行，
  「答题前后一样高」那条规矩还在。
- **「重掷」在三选一弹层底下多了一个按钮** `#btnRelicRedraw`：`offerRelics()` 里按
  `hasRelic("redraw") && !G.rerollUsed` 决定显隐，点了就地重抽、窗不关。
- **「课业」是普通品质里唯一带机制的一件**（用户指定）。其余普通件照旧只写数值。

### 第六批（2026-09）· 防御端「单件就有体感」—— 调平衡之前先看这张表

用户说「体感上防御端还是太弱」，用真实公式把第四章的容错（血上限 ÷ 怪一口）算了一遍，
**不是错觉**：

| 层 | 血上限 | 怪一口 | 裸装能挨 | 旧防御 5 件 | 新防御 3 件（不动+深潜+石胎） | 八件全上 |
| --- | --- | --- | --- | --- | --- | --- |
| 20/30/40（Boss） | 98/128/156 | 20/26/33 | 4.9 / 4.9 / 4.7 | 9.8 | 18~20 | 19~22 |
| 45 | 170 | 35 | 4.9 | 10.0 | 18.9 | 21.3 |
| 50（章末 Boss） | 182 | 56 | **3.3** | 9.6 | 18.2 | 18.2 |

（1~10 层是 16~24 下。CLAUDE.md 里那句「中后期可错 10 次」是 `FOE_BANDS` 之前的旧数，现在腰斩了。）

- **两条结论，别再往回走**：① 防御的**单件太小**（软甲 −10%、皮甲 −5%）要凑五件才翻一倍，
  而进攻单件就有感觉（开场 ×2、见面礼 ×1.5）—— 所以第六批全是高品质大数字；
  ② **定额回血在深层等于零**（45 层血上限 170、怪一口 35，而收割 +5、吞噬 +2）——
  所以新的回血件（归血 8% / 搏动 2%）全按百分比。**以后再加回血件也按百分比写。**
- **减伤总和封在 `MIT_CUT_MAX`(75%)**：不封的话八件全上是「每次只掉 1 点」（能挨 128 下），
  那是练习模式不是构筑。封了之后八件比三件只多两三下，边际收益递减是故意的。
- ⚠️ **减伤那一步要「先乘后除」**：`Math.floor(out * (100 - cut) / 100)`。
  写成 `out * (1 - cut/100)` 的话 55% 会算成 `0.44999999999999996`，floor 之后白多掉 1 点
  —— 一共有 16 种 (减伤%, 伤害) 的组合会中招，已经修了，**别改回去**。
- **`deathSave()` 是「打完发现血 ≤ 0」的唯一入口**（答错 / 超时 / 补救失败结清三处都调它）：
  不灭薪火（每层一次、钉在 1 点）排在回魂（整趟一次、回 50%）前面。
  ⚠️ 以前这三处各写了一遍薪火的判断，加第二道保险时很容易漏一处 —— 现在收成一个函数了，
  再加第三道保险就往它里面加。`P.revived` 跟着 P 进续玩档。
- **`stats()` 里的顺序又长了两件**：加血遗物 → **铁躯 ×1.8** → 献身 ÷2 → 背水 →
  **重装 ×3（护甲）** → `s.defGear` → 练习模式。两件乘法都必须待在这个位置上。
- **「石胎」和「钝痛」都是单次封顶**，一起带**按低的那个算**（`capPct` 取 min），不叠乘。

### 第五批（2026-09）· 护盾流补完 —— 动回血这条路之前先看这一段

- **`healUp(n, s, force)` 是回血的唯一入口**（跟挨打那边的 `takeHit()` 是一对）。
  「泉涌」要把**溢出上限的那部分**按 `SPILL_RATE`(2 点血 → 1 点盾)转成护盾，
  散在各处直接写 `P.hp = Math.min(maxHp, ...)` 就收不到那笔溢出。
  ⚠️ **以后任何回血都必须走 `healUp()`** —— 油灯/吞噬/饮血/收割/药膏/收尾/反刍/缚魂/孤注/无常/
  升级/泉水/喘息/不死鸟现在全在里面。返回 `{hp, sh}`（真回了多少血、转了多少盾），调用方拿去写日志。
  `force = true` 时不看有没有泉涌也转 —— 「水」自己那条词条。
- **带「泉涌」时满血也能喝泉**：`openSpring()` 里按钮不再无脑禁用，卡片上会写能换多少盾。
  别把那个 `disabled = room <= 0` 改回去。
- **「水」必须在 `genFloor()` 之后调**（泉是 genFloor 摆上去的），`nextFloor()` 里那一行就在它后面。
- **`mitigate()` 多了第三个参数 `opt`**：`{wrong:true}` 表示「这一下是答错挨的」。
  **「粗布」只认答错**（超时不触发），所以 `timeUp()` 那边**不要**传这个标记。
  粗布排在屏息**前面** —— 它每场都有，先花它才不会白吃屏息那两次「每层」的额度。
- **`P.recoil`**（反震攒了几层）跟着 P 进续玩档，读处 `|| 0`。
  ⚠️ 它在 `takeHit()` 里**只在真的掉了血时才 +1** —— 护盾全吃掉的那一下不算。
- **「镜盾」是③点伤层**（`Math.floor(护盾 / MIRROR_PER)`）。
  ⚠️ 这是全表最大的一个数：凝盾能把盾堆到 300 → +150 点伤，暴击后 +300 以上。
  要收就给它加上限或挪到①层，别忘了同步 `遗物数据表.md`。
- **「铁壁」把护甲那条死线盘活了**（每 1 点护甲 +3% 伤害，②层）——
  以后再动护甲的数值，记得它现在同时是伤害来源。
  ⚠️ 它读的是 `stats()` 里的 **`s.defGear`**（装备和等级来的护甲），**不是 `s.def`** ——
  练习模式那 +50 护甲要是也算进去，就是 +150% 伤害，「纯背词的简单模式」反而成了输出最高的玩法。
  以后再往 `s.def` 上加「不该变成伤害」的东西，记得加在 `s.defGear` 之后。

### 第四批（2026-09）· 防御端 —— 动挨打这条路之前先看这一段

- **`takeHit()` 是「真的要掉血」的唯一入口**，护盾就扣在这儿（先扣盾、剩下的才扣血）。
  答错、超时、复读者补救失败结清，三条路都从这儿过。
  ⚠️ **不走 `takeHit()` 的两处是故意的**：`flee()`（撤退掉的是一半生命上限，不是挨打，
  由「脱壳」管）和「割裂」的自伤。以后新增任何「怪打你」的路，**必须接到 `takeHit()` 上**，
  否则护盾会被绕过去。
- **护盾存在 `P.shield` 上**（跟着 P 进续玩档，`resumeRun()` 里有兜底）：
  不回血、不进结算、不吃减伤链，可以**比生命上限还高**（凝盾能攒到 `AEGIS_MAX`=300）。
  凝盾的计数是 `P.aegisN`，答错不清零。
  ⚠️ 血条上盾只画**上半条**（`.hpbar .sh` 是 `height:42%`）—— 铺满整条的话红色的血就整个被盖住，
  `.low` / `.crit` 那两条低血告警等于失效。别改成 `inset:0`。
- **读条秒数走 `qSeconds()`**（`QUIZ_TIME` 减掉「沙漏」这一层削掉的 `G.glassCut`，最低 `QUIZ_TIME_MIN`），
  别再直接读 `QUIZ_TIME`。沙漏在 `timeUp()` 里**提前返回**，不进减伤链 ——
  不然会白吃屏息的次数、白掷一次错身。
- **`G` 上又多了两个每层清零的**（`nextFloor()` 里）：`fleeFree`（脱壳）、`glassCut`（沙漏）。
  「盾誓」也挂在 `nextFloor()`：**取大值**补到 15%，不是直接赋值（否则会把凝盾攒的大盾按回去）。
- **「断链」必须在 `P.combo` 被清零/减半之前判**，不然条件永远不成立。
  连击的处理优先级：铁胆 ＞ 断链（−`UNCHAIN_CUT`）＞ 长链/拼写（减半）＞ 清零。
  它的免伤排在**所有免伤的最前面**（拿连击换的，不该消耗默诵/回声/屏息的次数）。
- **减伤百分比这一档现在是「先相加、再乘一次、向下取整」**（软甲 10 + 皮甲 5 = 15%）。
  ⚠️ 取整从 `ceil` 改成了 `floor`：向上取整的话 −5% 在小数字上等于没有
  （早期怪打 5~6 点，`ceil(6×0.95)=6`），皮甲就成了骗人的遗物。最低仍然掉 1 点。

### 第三批（2026-09）要知道的几件事

- **「通感」让 `hitWeak` 恒为真**（所有题都算打中弱点）。以后凡是读 `hitWeak` 的分支
  （弱点点伤、猎手、破绽、追猎）都要想一下「它可能一直是真的」。
- **无视护甲现在有两个来源**，收在 `answer()` 的 `noArmor` 一个变量里：
  破绽（打中弱点）和碎颅（暴击）。再加第三个就往这个变量上并，别各写各的。
- **`critMult` 现在有人直接加了**（重锤 +0.4）。它跟「暴击率超 100% 每 5 点换 +10%」是**相加**的，
  仍然只有这一个乘区。
- **每层一次 / 每场一次的标记**：`G.openUsed`（开场，`nextFloor()` 里清零）、
  `B.greetUsed`（见面礼，跟着 B 一场一清）。两件都是②层的百分比（+100% / +50%），
  同时触发就是 +150% —— **相加不相乘**，别改成乘。
- **`P.charge`**（蓄势攒了几层没暴击）跟着 `P` 进续玩档，读处 `|| 0`。
- **祭坛代价走 `altarCost()`**（祭余 5 → 1），别直接读 `ALTAR_COST`。
- **宝箱的「开没开」是 `judgeChest()` 里的 `opened`**（钥匙让拼错也开）。
  ⚠️ 熟练度和心魔仍然按真实对错记 —— 撬开的是锁，不是那个词。
- **拼写题的输入拆成了三个小函数**：`spellStep()`（走一步：补笔顺 → 重画 → 满了就判）、
  `giftFill()`（笔顺送的那一格自己填上，并按掉对应的字母键）、`spellPop()`（退一格、把字母还回键盘）。
  ⚠️ 退格时要**连笔顺那一格一起退**，否则它下一拍又被填回来，前面那个字母永远改不了（踩过）。
  ⚠️ **大写干扰项那一行只能改干扰项**（`letters[letters.length-1]` 是最后一个干扰字母），
  所以整段包在「没有净写」的判断里 —— 带净写时没有干扰项，那行会把词本身的字母改成大写，
  拼出来永远是错的（差点踩）。
- **掉率整体左移了一档**（用户 2026-09：高品质太容易白捡）：`rarityWeights()` 里
  **神圣权重归零、不再掉落**，传奇接了原来神圣的份，史诗接了原来传奇的份，空出来的按当时
  普通:稀有的比例回填。现在是 1–10 层 87.5/12.5、11–20 层 75/25、21–30 层 60/35/5、
  31–40 层 50/40/10、41–50 层 35/45/15/5（普通/稀有/史诗/传奇）。
  **神圣不删，只是改成靠合成拿**（三件传奇 + 500 金），游商也还卖。

## 待办

- [ ] **局域网双人合作模式**（用户 2026-09-18 提的）：**第一期「骨架通」+ 第二期「双血条战斗」
      代码都做完并上线了**（2026-09-20）——组队、房主选路线队友确认、世界同步、移动锁、
      队友状态条、怪的两条独立血条、打空自己那条转去帮队友砍、倒地不出局／队友清层原地复活，
      都能用了。**真人两台真机实测还没做**（两期都需要用户帮忙这一步，Claude 的沙盒只能自己
      跟自己联机测）。**全部细节、当前进度、协议、踩过的坑都在 `联机方案.md` 里，做之前先读那一份**，
      别在这儿重复记。

- [ ] **祝福系统**（主城那个灰着的「祝福」）：用宝石换永久的好处，**具体做什么用户还没定**。
      结算和宝石这一套已经通了，缺的只是花出去的地方。
- [ ] **后期遗物偏弱**（用户 2026-09 提的）：等基本表现做完再统一做数值/机制调整。
      顺带想做的：**其它遗物也给「答对成功时的额外奖励」词条**（现在只有拼写自带双倍经验+连击）。
- [x] ~~**第三章（B1）／第四章（B2）**~~ —— 2026-09 做完了，见上面「章节系统」那一节。
      四条路都在洞窟里开着，词库一个字没动。
- [ ] **B1 里压着一批 A1/A2 高频词**（`path` `coin` `hope` `fear` `trust` `guard` `attack` `shield` 这些）。
      补 1000 核心词、以及后来把 B1 补到 1000 时，为了不动已有的词**都没有把它们挪上来**，是绕开重新选的词。
      以后要么把它们挪进 A1/A2，要么就这么放着 —— 等用户定。

- [ ] **网站图标**：现在没有 favicon，浏览器标签页空白，请求 `/favicon.ico` 会 404。原 Artifact 的图标是 🗡🔥
- [ ] **自定义域名**：用户想绑 `www.xxx.com`，还没买域名。买了之后：Settings → Pages 填域名 →
      DNS 里 `www` 加 CNAME 指向 `audience20060904-lang.github.io` → 勾 Enforce HTTPS。
      （workflow 已经会自动带上仓库根的 `CNAME` 文件，这一步不用再动。）

## 新会话怎么接手

1. 如果这个仓库不在会话的 GitHub scope 里，先用 `add_repo` 加上 `audience20060904-lang/youxu-huilang`，然后 clone。
2. 改完**自动上线**（见上面「已经定下来的事」第 5 条，不用问用户）：提交 → 推到 `main` →
   用 Actions 的 API 确认部署成功（`list_workflow_runs` 看 conclusion）。
   **不要用 curl 去验证网站** —— 沙盒访问不了 github.io，见上面「踩过的坑」。
3. 部署失败的话，用 `get_job_logs` 带 `failed_only: true` 看日志，修好再推一次，别把失败晾着。
