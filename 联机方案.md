# 幽墟回廊 · 局域网双人合作模式 · 方案与进度

**这个文件是给 Claude 看的，也是联机这件事唯一的交接文档。**
`CLAUDE.md` 管的是游戏本体，这一份只管联机。做联机的新会话，先读 `CLAUDE.md`，再读这一份。

> ## ⚠️ 维护规矩（最重要的一条）
>
> **只要动了联机相关的任何东西，就必须回来改这个文件。** 包括但不限于：
> - 做完了一步 → 更新下面的「当前进度」，把方块从 `[ ]` 划成 `[x]`，并在「变更记录」里加一行
> - 用户改了规则 → 就地改「玩法规则」那一节，别在别处另记一份
> - 踩了新坑 → 写进「踩过的坑」
> - 加了新文件 / 新的 localStorage 键 / 新的消息类型 → 写进对应小节
>
> 这个文件的目的就是：**任何一个新开的会话，读完它就知道联机现在做到哪了、下一步该干什么、
> 哪些事已经定死不许重新讨论。** 写得不够新，下一个会话就会白做或做反。

---

## 当前进度

**第一期「骨架通」+ 第二期「双血条战斗」都做完并上线了。
2026-09-20 用户和朋友在两台真机上跨网络打完了一整层，没有任何问题。
2026-09-21 换成 Tailscale Funnel 的**固定地址**，同样真机实测通过（cloudflared 那套已删）。**

已经能用的：组队（4 位数字房间号 + 邀请链接）、房主选路线／队友确认、世界同步、
移动锁（怪清完之前只能靠两人都点的共享寻路走）、队友状态条、怪的两条独立血条、
打空自己那条转去帮队友砍、倒地不出局、队友清层原地复活、跨网络穿透入口。

还没做的：

- [ ] **二维码进房**（用户问过「有没有更简单的方法」）。没有摄像头没法实测扫描，
      手搓零依赖二维码编码器风险高 —— 做之前先问用户接不接受引入一个现成的库
      （CDN 或本地 vendor 一份，两种都不再是「零依赖」了，都要用户点头）。
- [ ] **房主倒下时把算路的活儿交给队友**（见下面那条已知的不对称限制）。

---

## 实现跟设计稿的出入（以这一节为准，上面那几节的协议表是草案）

**第一期**

- **战斗那一套第一期没做**：地图/怪的初始状态/物件/楼梯是共享的（房主生成、广播给队友），
  但战斗完全不联网，两人各打各的。双血条是第二期补的。
- **移动锁是「房主算路 + 广播 + 队友照抄」**：只有房主的 `autoPath()` 真的跑 BFS，
  算完一段就广播，队友收到 `path` 原样走（`stepWalk()` 一个字没改）。
  房主会等队友「不忙」（没在战斗/没在弹层，靠 500ms 心跳判断 `coopMateBusy`）才算下一段。
  不是严格同格锁步 —— **走同一条路线、各按各的节奏**，两人坐标经常差一两步，实测可接受。
- **移动锁只看怪**（`coopLocked()`）：怪没清完之前**手动移动整个禁用**，寻路目标也只挑怪不挑金币；
  怪一清完立刻整体解锁（`coopUnlockMove()`），两人从此各走各的。
- **协议表实际多出来的几条消息**（草案里没有，以这里为准）：
  - `route {id}` —— 房主点路线只是**提议**（高亮，可以反悔重选），不会自动进。
  - `enterConfirm {id}` —— 队友点「确认，一起下去」才两边真的进。
  - `practice {on}` —— 练习模式开关，房主定、队友跟着变。
  - `used` —— 踩到泉/坛/箱/商时通知队友，队友日志里写一句。
  - `go` 多一个 `on` 字段：寻路是常驻开关，任一人再点一次两边都停，
    所以 `ready{on:false}` 时服务器也会广播 `go{on:false}`。

**第二期**

- **怪的身份用 `cid`，不是数组下标**：下标会因为 `splice` 移动 —— A 杀掉第 3 只之后
  后面的怪全部前挪，而 B 可能还没收到那条 `dead`，两边的「下标 3」已经不是同一只了。
  所以铺场时（`coopPrepMobs()`）就给每只怪挂一个固定不变的 `m.cid`，
  `dmg`/`hp`/`dead` 三条消息全部按 `cid` 找怪（`mobByCid()`）。
  服务器那边 `room.mobs` 也拿 `cid` 当 key，每次收到 `world` 整个重建（新一层，血条全满）。
- **`m.hp` 的语义照设计稿第 3 节实现**：它永远是「我现在正在砍的那条」，
  `answer()` 里扣血全部换成 `coopDealDamage(m, n)` —— 本地先乐观减（手感不等网络），
  再把数值和 `m.curSide` 发给服务器，权威值靠 `hp` 广播校正回来。
  `m.hpMate` 是另一条的缓存，纯显示 + 判断能不能切换目标，不参与伤害计算。
- **判怪死那一处**（全文件唯一一处 `m.hp <= 0`）分三条路：
  ① 我这条空了、队友那条还有血 → `coopTrySwitchTarget()` 把 `m.hp` 换成 `m.hpMate`，
  战斗不结束，继续答题（这就是「转去帮队友砍」）；
  ② 两条看着都空了 → **不在本地判定**，锁住战斗窗等服务器广播的 `dead`
  （避免两人同时补最后一刀的竞态）；③ 单人版完全没变。
  `dead` 到了走 `coopHandleDead(cid)`：正在打它就等 300ms 再走**跟单人版一样的**
  `finishBattle(true)`；没在打它就直接从 `G.mobs` 摘掉，**不结算经验金币** ——
  奖励只给当时真的在这场战斗里的那一边。
- **倒地不是新状态机**，是 `finishBattle(false)` 的一个分支：COOP 里走 `coopGoDown()`，
  `P.down=true`、`P.hp=0`，不碰 `G.over`。两人都倒下才各自走 `gameOver()`
  （`coopCheckBothDown()`，各算各的宝石，不由服务器统一喊停）。
- **倒地状态没走服务器**：复用已有的 500ms 心跳（`me`/`mate`）多带一个 `down` 字段，
  两边各自本地判断。代价是最多 500ms 的感知延迟，可以接受 ——
  血条必须服务器定序是因为有真实竞态（两人同时打同一条），倒地判断没有。
- ⚠️ **已知的不对称限制：房主倒下会连寻路一起停住**。算路永远照着**房主自己的坐标**跑
  （`findPath` 用模块级的 `P.x/P.y`），队友照抄这条绝对坐标。房主倒下后人不动但算路循环
  还在跑的话，队友会收到一堆从「房主冻结的位置」出发的路径，越走越错位。
  所以 `coopGoDown()` 里**房主倒下会额外 `autoOff()` 并广播 `ready:move on:false`**，
  两边寻路一起停，队友只能手动点地图清完剩下的怪（手动移动一直可用）。
  **队友倒下没有这个问题**（房主还醒着，正常算路，队友只是自己不挪）。
- **战斗窗里的队友血条**：`#foeMateBar`（队友正在砍的那条）和 `#bMateHpBar`（队友本人）
  都是 `coop.html` 独有，`index.html` 没有这两个元素 ——
  `renderBattleBars()` 里 `$()` 拿到 `null` 会自动跳过，所以单人版一个字没改。

---

## 架构：同一仓库，开关隔离

用户拍板过：**不另开仓库**。8300 行里有 2900 多行是纯数据（`words-a1.js` 1765 行、
`content.js` 623 行、`art.js` 584 行），复制一份就会永久分叉——以后加词、调遗物平衡
要做两遍，漏一次两边就对不上了。所以：

- **`index.html` 一个字都不改** —— 线上 GitHub Pages 永远是现在这个单人版，零风险
- 新增 **`coop.html`**：联机入口，加载同样的 `util.js` / `words-a1.js` / `content.js` /
  `art.js` / `style.css`，多加载一个 `net.js`，并在 `game.js` 之前设 `window.__COOP = true`
- `game.js` 里所有联机代码写成 **`if(COOP){...}`** 分支，`COOP` 在单人版恒为 false，
  **单人的执行路径一行都不能变**
- 新增 **`server.js`**：同时当静态文件服务器和房间服务器。
  用户在电脑上 `node server.js` 启动，手机开 `http://<局域网IP>:8080/coop.html`
- ⚠️ **`server.js` 必须零依赖**（2026-09-18 定）：只用 Node 自带的 `http` / `crypto` / `fs` 等内置模块，
  **自己实现 WebSocket 握手和帧解析**（大概 100 行），**不许 `npm install ws`**。
  理由：用户装完 Node 就能直接跑，不用等下载包、不会碰到国内 npm 超时；
  也跟这个项目「没有构建步骤、没有依赖」的一贯风格一致。仓库里**不要出现
  `package.json` / `node_modules`**
- 词库、遗物配置、美术**只有一份**，以后加词/调平衡改一次两边都生效

**线上和联机是两个入口**：`https://audience20060904-lang.github.io/youxu-huilang/`
永远是单人版，不受影响；联机必须开着电脑。

## 同步模型

- ⚠️ **「服务器权威」这个说法 2026-09-19 修正过，别照字面理解**（见下面「实现设计」第 1 节）：
  服务器是 Node，**没有 DOM，跑不了 `game.js`**，所以它不算游戏逻辑。
  它只维护几个权威数字（每只怪的两条血条、两人的 ready 和倒地状态）并负责**定序**；
  世界由**房主客户端**生成后整包广播；伤害仍然在各自客户端算（要用各自的遗物）
- **每台手机只跑自己那份 `P`** —— `game.js` 里 `P.` 出现 357 次、`G.` 240 次、`B.` 147 次，
  **绝不要在一份代码里塞两份角色状态**，那等于重写。每人看自己的第一视角，
  屏幕上多一条「队友状态条」（血、连击、遗物数、准备状态）
- **世界状态同步直接复用 `writeRun()` 那套序列化**（`game.js` 约 3117 行）：
  地图按行压成字符串、怪只存 defId + 状态，整包几 KB，正好当同步载荷
- ⚠️ **不要试图同步随机种子。** `Math.random()` 在 `game.js` 里直接调了 29 处、
  `util.js` 2 处，加上 38 处 `ri()` / `pick()`。两个人答的题不一样，随机序列立刻漂移。
  **地图和怪由权威端单点生成、整包广播给对方**，这是唯一可靠的做法

## 实现设计（2026-09-19 第二轮构思，读过代码之后）

这一节是**动手前必须看完的**。下面每条都对着 `game.js` 的真实代码核过，不是设想。

### 1. 三方分工（修正了第一轮的「服务器权威」）

第一轮写的「伤害、掉落、世界状态由服务器裁决」**做不到**：服务器是 Node，没有 DOM，
而 `game.js` 里到处是 `$()` 和 `render()`，整个文件在 Node 里跑不起来。正确的分工是：

| 谁 | 负责什么 |
| --- | --- |
| **服务器** | 房间、消息转发、**定序**，外加维护极少量权威数字 |
| **房主客户端** | 世界生成：跑 `genFloor()`，把结果整包广播 |
| **各自客户端** | 自己的 `P`、自己的伤害计算（要用自己的遗物）、自己的渲染 |

**服务器要维护的权威状态少得惊人**，一共只有这些：

- 每只怪的两条血条 `hpA` / `hpB`
- 两人的 ready 状态（寻路 ready / 下一层 ready）
- 两人的倒地状态
- 当前层号 + 世界包的版本号

⚠️ **服务器不 `require` 任何游戏文件**，它连 `content.js` 都不用读。
客户端把**算好的伤害数值**发上来，服务器只做减法、判死、广播。
这样 `server.js` 能保持在几百行，也不会跟游戏平衡的改动纠缠在一起。

### 2. 世界状态的分层：**布局共享，消耗独立**

这是整个联机设计的核心原则，想不清楚这条就会到处打补丁：

| 类别 | 例子 | 怎么同步 |
| --- | --- | --- |
| **共享且必须一致** | 地图、怪的位置、金币位置、阶梯位置、怪的两条血条 | 房主生成 → 整包广播；血条走服务器 |
| **每人独立消耗** | 泉、宝箱、祭坛、游商 | **各开各的**，各自从自己的 `G.things` 里删 |
| **天然就一致** | 金币 | 共享移动 → 两人同时踩到 → 各拿一份 → 各自 splice，结果必然相同 |

- 「每人独立消耗」意味着 A 开过的宝箱在 A 的地图上没了、在 B 的地图上还在。
  **这是有意的**，不是 bug —— 两个人本来就各看各的第一视角，而且遗物就是各抽各的
- ⚠️ 这一条**不影响移动限制的判定**：解除条件只看「怪 + 金币」，而这两样恰好都是必然一致的那一类。
  泉/箱/坛/商不进判定，所以两人的 `G.things` 不完全相同也不会让两边对「能不能走了」产生分歧

### 3. 关键技巧：`m.hp` 永远是「我眼前这条血条」

怪要有两条独立血条，最笨的改法是把 `m.hp` 换成 `m.hpA / m.hpB`，然后把 `game.js` 里
每一处读写 `m.hp` 的地方都改一遍。**不要这么做。** 正确的做法是：

> **每个客户端本地的 `m.hp` 始终指向「我现在正在砍的那条」**，另一条存在 `m.hpMate` 上只供显示。

这样 `answer()` 里那行扣血、回响之厅的 `m.hp -= hall` **一个字都不用改**。只需要在三个点上挂钩：

1. **砍完之后**：把这次的伤害数值上报服务器（`dmg` 消息），服务器扣权威血条并广播
2. **`m.hp <= 0` 那一处判定**（`game.js` 约 1833 行，全文件**只有这一处**判怪死）：
   联机模式下要先问一句「对方那条还有血吗」——
   有就**切换目标**（`m.hp = m.hpMate`，继续答题，这就是「转去帮队友砍」），
   没有才走 `finishBattle(true)`
3. **收到对方扣血的广播**：更新 `m.hpMate` 并重画血条

⚠️ **唯一有竞态的地方就是「帮打」**：A 帮砍 B 那条时，A 和 B 可能同时把它砍到 0，
两边都以为是自己补的最后一刀。所以**所有伤害都走服务器定序**（自己那条也走，保持一条路），
局域网往返 1~5ms，手感上察觉不到。本地可以先乐观扣血、收到权威值再校正。

### 4. 只有 **2 件**遗物会碰世界状态

把 `genFloor()` / `nextFloor()` 里所有 `hasRelic()` 调用逐个核过了，全表只有这两件
会改变**共享的**世界，其余（油灯回血、钱袋给钱、盾誓、厚盾…）全是各自结算，不用管：

| 遗物 | 做什么 | 冲突点 | 处理 |
| --- | --- | --- | --- |
| **掘金 `dig`** | `genFloor()` 里金币堆 +3 | 只有一个人有时算不算 | **任一人有就生效**（合作模式好处共享，金币本来两人各拿一份） |
| **水 `water`** | `drinkAll()` 喝光**这一层所有的泉** | 泉是共享布局 | 按第 2 节：**泉的消耗是每人独立的**，A 的「水」只喝光 A 那份，B 的泉还在 |

⚠️ 以后**新增任何遗物，都要回来问一句「它碰不碰共享世界」** —— 碰的话就得在这张表上加一行。
（原来还有一件「盲斗」会跳层，2026-09 已经删掉了。）

### 5. `game.js` 的钩子点清单

联机代码全部写成 `if(COOP){...}`，单人路径一行不变。按要改的地方从上到下：

| 函数（行号是 2026-09-19 的） | 要加什么 |
| --- | --- |
| `newRun()` 64 | 房主生成世界；非房主等世界包到了再铺场 |
| `nextFloor()` 140 | 双确认才进；`genFloor()` 只有房主跑 |
| `genFloor()` 234 | **完全不碰 DOM，可以原样复用** —— 只有房主跑，跑完把 `G` 整包广播 |
| `autoToggle()` 854 | 不再直接开，改成**上报「我 ready 了」**，等服务器的 `go` |
| `autoTick()` 838 | 移动锁阶段：只有收到 `go` 才走；160ms 轮询本身照旧 |
| `autoPath()` 810 | ⚠️ **只有房主算目标**，算完广播路径；否则两人各算各的会分叉 |
| `tryMove()` 875 / `#map` 的 click | 移动锁阶段 + 寻路开着时禁用手动（用户要求） |
| `onEnter()` 894 | 金币各拿一份（天然一致）；泉/箱/坛/商各自独立消耗 |
| `startBattle()` 971 | 两人同时进；初始化 `m.hp` / `m.hpMate` 两条血条 |
| `answer()` 1490 | 扣完血上报服务器（`m.hp` 本身不用改，见第 3 节） |
| 判怪死 1833 | **全文件唯一一处** `m.hp <= 0` —— 加「切换目标 or 真死」的分岔 |
| `P.hp <= 0` 三处 1179 / 1834 / 1993 | 联机模式下走**倒地**，不是 `gameOver()` |
| `finishBattle()` 1997 | 怪死是共识事件，等服务器的 `dead` 再收 |
| `closeBattleWin()` 2017 | 经验/金币/回血各自独立；`G.stair` 改写位置天然一致（怪死在哪是共识） |
| `takeHit()` 1937 | 各自独立，不用同步；只是血见底时转倒地 |
| `deathSave()` 1844 | 薪火/回魂都用完了 → 倒地而不是结束 |
| `endRun()` 3249 | **两人都倒下**才结算；各算各的宝石 |
| `flee()` 2068 | **联机里整个禁用**，战斗窗的「撤退」按钮隐藏（见第 8 节） |
| `render()` 577 | 画两个棋子（见第 7 节） |
| `renderBattleBars()` 1000 附近 | 画两条怪血条 + 队友状态条 |
| `commit()` 3110 | 各自存各自的，**localStorage 的键一个都不用新增**；重连时以房主广播的世界为准（第 10 节） |
| `enterRoute()` 3071 | 选路线由房主定，两人到齐才一起进（第 10 节） |
| `offerRelics()` 2792 + 宝箱/祭坛/游商 | 联机里把「脱壳」滤出掉落池（第 9 节） |

### 6. 消息协议草案（`server.js` ↔ `net.js` 的契约）

JSON 文本帧，每条带一个 `t`（type）。**先照这张表实现，不够了再加，别临时发明字段。**

**客户端 → 服务器**

| `t` | 带什么 | 谁发 | 什么时候 |
| --- | --- | --- | --- |
| `join` | `room`, `name` | 两人 | 进页面时 |
| `world` | `floor`, `pack`（`writeRun()` 那套序列化） | **只有房主** | 每次 `genFloor()` 跑完 |
| `ready` | `what:"move"\|"floor"`, `on:bool` | 两人 | 点寻路 / 点下一层 |
| `path` | `steps:[{x,y}...]` | **只有房主** | `autoPath()` 算出目标之后 |
| `dmg` | `mob`（**cid**，不是数组下标）, `side:"a"\|"b"`, `n` | 两人 | 砍完一刀（`side` 指砍的是谁那条血条） |
| `me` | `hp`, `maxHp`, `combo`, `relics`, `down` | 两人 | 状态一变（给队友状态条用） |
| `used` | `kind`, `x`, `y` | 两人 | 用掉了泉/箱/坛/商（**只是通知对方，不改对方的世界**） |

**服务器 → 客户端**

| `t` | 带什么 | 意思 |
| --- | --- | --- |
| `joined` | `host:bool`, `mate:bool` | 你是不是房主、对方在不在 |
| `world` | 原样转发 | 非房主收到就铺场 |
| `go` | `what:"move"\|"floor"` | **两人都 ready 了**，可以走 / 可以下一层 |
| `path` | 原样转发 | 非房主照着这条路走 |
| `hp` | `mob`（cid）, `a`, `b` | **权威血量**，客户端照这个校正 |
| `dead` | `mob`（cid） | 这只怪两条血条都空了，收掉它 |
| `mate` | 对方那条 `me` | 刷队友状态条 |
| `pause` | `why:"disconnect"` | 对方掉线，整局暂停 |
| `resume` | — | 对方回来了 |

⚠️ 几条要点：
- **`dmg` 只发「伤害数值」，不发血量** —— 血量是服务器算的，客户端算血量就会各说各话
- **`world` 只有房主发**，服务器不校验内容，只管转发和记版本号
- `used` 是**纯通知**（让对方的日志里能看到「队友开了宝箱」），**收到它不要去改自己的 `G.things`**
  —— 消耗是独立的，见第 2 节
- 断线检测靠 WebSocket 的 close 事件 + 30 秒心跳，两条都要有：
  手机锁屏时连接常常不会干净地关掉

### 7. 两个棋子：**逻辑同格，渲染错开**

用户要「地图上画两个棋子，一前一后走同一条路」。实现上**不要**让两个人真的占不同格子 ——
`autoPath()` 里的 `roomOf(P.x, P.y)` 和 `nearestBy()` 都按位置算，位置一差目标就可能不同，
路线立刻分叉。正确做法：

> **两人逻辑上共享同一个 `P.x / P.y`**，渲染时把自己画在格子偏左、队友画在偏右并缩小一号。
> 纯视觉偏移，不占格子、不进寻路。

这样 `findPath` / `goTo` / `stepWalk` / `onEnter` **一个字都不用改**。

### 8. 第二轮定下的两条规则（用户 2026-09-19 确认）

- **联机里禁用撤退**：碰上了就必须打完。`flee()` 整个走不到，战斗窗里的「撤退」按钮直接隐藏
- **房主 = 先连上的那台手机**：电脑只跑服务器、不开游戏页面。
  ⚠️ **房主掉线时服务器要让另一个人自动接管**（发一条 `joined` 更新 `host:true`），
  否则世界就没人生成了，整局卡死

### 9. 禁用撤退的连带影响：**「脱壳」要从掉落池里剔除**

把所有跟 `flee()` 相关的遗物核了一遍，受影响的**只有一件**：

- **脱壳 `shed`**（史诗，「每层第一次撤退不掉血」）—— 撤退都没了，它是**纯废牌**。
  联机模式下要把它从掉落池里滤掉：三选一 `offerRelics()`、宝箱、祭坛、游商货架，
  四个出货口都要过滤，别让玩家白高兴一次抽到史诗
- ⚠️ 这是一条**通用规矩**：以后凡是在联机里改动或禁用某个系统，都要回头问一句
  「有没有遗物是专门吃这个系统的」。`content.js` 里按 `pw`（效果文案）全文搜一遍最快

### 10. 进洞之前也要同步（第二轮发现的遗漏）

前两轮的设计全在讲「进了地牢之后怎么办」，**漏了进洞这一步**。`enterRoute(id)`
（`game.js` 约 3071 行）是进洞的唯一入口，它 `setChapter(r.ch)` 决定这一趟是哪一章
—— 两个人要是选了不同的路，世界从第一秒就是两个，后面全白搭。所以：

- **选哪条路（哪一章）由房主定**，另一台手机上的洞窟卡片只显示、不可点，
  写一行「等房主选路线」
- **练习模式也由房主定**（第一轮就定了「两人一致」，这里补上实现位置）：
  `practiceOn` 是模块级变量，非房主那台要禁用 `#btnPractice` 并跟着房主的值刷新
- **`enterRoute()` 要等两人都到齐**再一起进，不能房主点了就自己先走

**续玩档怎么办**：照常各写各的（`commit()` 一个字不用改），但
⚠️ **重连或重开页面时，一律由房主重新广播一次世界包，非房主用它覆盖自己的 `G`**。
不要试图让两边的续玩档「自己对上」—— 只要有一次不一致，后面全是错位，
而重新广播一次只花几 KB。

## 玩法规则（用户 2026-09-18 逐条确认，别再重新讨论）

**角色完全独立**：遗物、金币、等级、血、连击、心魔、`P.used`、出题全部各是各的。
两人的熟练度不同，各抽各的题。

### 移动限制

⚠️ **2026-09-20 又续改窄了一次**：解除条件从「怪清完 `且` 金币全捡完」改成**只看怪**——
金币是两人各自生成、各自捡的（`onEnter()` 里各自 `splice` 自己的 `G.things`，不共享一份计数），
拿它当解锁条件的话，一个人手快先把地上那几堆都捡了，另一个人还没碰到，这一层反而先解锁了，
条件本身就说不清楚。现在是：

- **解除条件 = 这一层的怪清完，不再管金币**（`coopLocked()` = `COOP && G.mobs.length > 0`）
- **解除之前：手动移动整个禁用**（不是"建议用寻路"，是方向键 / 点地图**直接不生效**，
  `tryMove()`/`#map` 的 click 里挡死，只提示一句「这一层的怪还没清完」，1.5 秒节流别刷屏）——
  唯一能动的办法就是**两人都点寻路**，走同一条共享路线、打同一只怪
- **寻路只认怪，不认金币**（`autoPath()` 里 `coopLocked()` 时目标只挑 `G.mobs`，
  金币过滤掉）——顺路踩过的金币照样白捡（`onEnter()` 没变），只是不会特地为了一堆金币绕路，
  也不会在还有怪的时候把两人的路带岔
- 任一人再点一次寻路 = 两人一起停下（没变）
- **怪一清完，移动锁立刻整个解除**（`coopUnlockMove()`，两边各自检测到"我的 G.mobs 空了"
  都会调用一次）：寻路自动停一下、还给玩家自己决定接下来干嘛，manual 移动同时解禁
- **解除之后两人完全独立**，不再有"一前一后走同一条路"这回事——各自决定要不要用寻路
  （找的是**各自的**金币，因为地上剩下的金币两人捡去的份数早就不一样了）、要不要手动逛、
  要不要用泉 / 箱 / 坛 / 商，最后双确认下楼
- 所以一层的节奏还是三段：**同步清怪（手动锁死，只能走共享寻路）→ 各自独立行动 → 双确认下楼**
- **清怪阶段**地图上画**两个棋子**，一前一后走同一条路；**解锁之后**队友棋子就是它本来的样子——
  按队友最后一次上报的坐标画（`coopMateX/Y`），不再跟自己捆在同一条路上
- **一场战斗打完、这一层还没清完的话，寻路状态保留、自动接着走下一只怪**
  （一层十几只怪，每只都要两人重新点太烦）——这条只在**清怪阶段内部**有效，跟"清完之后
  还继不继续走"是两回事
- 每下一层仍然要重新手动开（现有规矩，`nextFloor()` 里的 `autoOff()`；新一层怪没清完，
  `coopLocked()` 又是 true，移动锁自动重新扣上）
- ⚠️ **`coopUnlockMove()` 里那句 `NET.send({t:"ready",what:"move",on:false})` 是幂等地发**，
  不管 `autoOn` 现在是不是已经是 `false` 都发一次——`server.js` 的 `room.ready.move` 是
  常驻状态、不会自动复位（"floor"/"enter" 这类一次性事件触发后会自动清空，但"move"不会，
  见"实现设计"第 6 节的协议要点）。踩过一次：如果只有一边真的发了这条 off，下一层"两人都点
  寻路才走"的判定会被这一层残留的 `[true,true]` 提前凑成 `go`，变成"其中一个人一点寻路，
  两人就直接一起走了，另一个人根本没点"。两边各自检测到清完都调用一次 `coopUnlockMove()`，
  这条才能真的清干净

### 战斗

- **两人同时各答各的题**，互不等待。各自的连击、遗物、暴击、7 秒读条都独立
- 怪的反击各打各的（A 答错 A 掉血）
- **怪有两条独立血条，每条都是原血量的满血**（实际总血量 ×2，但两人同时砍，
  单人体感节奏不变）。`m.hp` 要从单值改成两条
- **A 把自己那条清空后，可以转去帮 B 砍 B 那条**。强的人能带弱的人
- 两条都清空，怪才死
- **联机里禁用撤退**（用户 2026-09-19）：碰上了就必须打完，战斗窗里没有「撤退」按钮
- **战斗窗里两条队友血条，都是半透明 50%**（用户 2026-09-20）：
  ① 怪血条 `.fbar` 下面再画一条 `#foeMateBar`——队友正在打的那条（`m.hpMate`）；
  ② 自己血条 `#bHpBar` 上面再画一条 `#bMateHpBar`——队友本人剩多少血（数据是
  `coopMateInfo.hp/maxHp`，跟顶栏那条「队友状态条」`#mateRow` 同一份心跳，最多 500ms 延迟）。
  两条都只在 `coop.html` 里有对应元素（`index.html` 没有，`$()` 拿 `null` 就跳过），
  纯显示，不参与伤害计算。⚠️ `#bMateHpBar` **不走 `paintHp()`**——那个函数会顺手把
  `P.shield`（自己的护盾）画上去，套在队友那条血条上会显示错的盾数，所以专门写了
  `paintMateHp()`，没有护盾层、没有掉血/回血的闪光动效。半透明是在 CSS 里用 id 选择器
  （`#bMateHpBar`/`.fbar.mate`）写 `opacity:.5`，不是加一个新 class——`paintHp()` 里
  `bar.className = "hpbar" + ...` 是整块重写，加在 class 上的自定义类每次刷新都会被冲掉。

### 倒地

- 血掉光**不出局**：停止答题，怪也不再打他
- 队友把这一层的怪清完，他原地复活（回 1 点血）
- **两人同时倒下才算这趟结束**
- **倒地的人不阻塞移动** —— 视为自动同意，队友一个人点寻路就能走
- ⚠️ **A 倒地后不做任何难度缓解**：怪的两条血条都还在，B 必须独自砍完。
  这是用户明确选的硬核档（已经提醒过后果，用户坚持），**不要自作主张减半或让血条消失**。
  实战下来觉得劝退的话由用户来提，改起来是一行的事

### 局内资源

- 地上一堆金币，**两人各拿一份**（不是抢，合作模式不该为捡钱吵架）
- 怪掉遗物，**两人各自抽各自的三选一**
- 泉 / 宝箱 / 祭坛 / 游商：走到那一格后各自决定用不用，互不阻塞，各花各的金币

### 结算

- 各算各的宝石，存进各自手机的 localStorage。熟练度、图鉴、统计也各存各的

### 练习模式

- 局前由房主统一定，两人一致（它直接改难度平衡，只有一个人开会很怪）

### 断线

- 谁掉线就整局暂停，屏幕上写「等待队友重连」，重连回来接着走
- 最坏情况退回本层开头（续玩档本来就存「刚踏进这一层的样子」，这条规矩不变）

## 用户的运行环境

- **用户用 Windows**（2026-09-18 确认）。装 Node 的步骤已经给过他了：
  nodejs.org 下 LTS 的 .msi，一路 Next，「Tools for Native Modules」那个复选框保持不勾
- 查局域网 IP 用 `ipconfig` 看「IPv4 地址」
- ⚠️ **第一次跑 `node server.js` 会弹 Windows 安全中心警报，必须勾「专用网络」再允许访问**，
  不勾手机连不上 —— 这是最常见的坑
- **用户电脑上那份代码最早是用网页上的「Download ZIP」下载的**（2026-09-20 问出来的），
  跟仓库没有关联，Claude 这边改完推到 `main`，用户电脑上那份不会跟着变，
  每次都要重新整个下载很麻烦。**已经引导用户改成 `git clone`**，见下面「怎么自动更新」。

### 怎么自动更新到用户电脑（2026-09-20 加）

- **单人版网站不用管**：推到 `main` 之后 GitHub Pages 自动重新部署，用户打开网页看到的
  永远是最新版，这条路径本来就是自动的。
- **联机版是另一回事**：`node server.js` 跑的是**用户电脑本地磁盘上**的一份代码，
  跟 GitHub 仓库是两份独立的东西 —— 仓库更新了，本地这份不会自动跟着变。
  真正做到"自动"的前提是**用户电脑上那份必须是 `git clone` 下来的**（不能是解压的 ZIP，
  ZIP 没法 `git pull`）。
- **新增了 `start-coop.bat`**（仓库根目录，2026-09-20 加，不上线 —— 只在用户本地跑，
  跟 `server.js`/`check-words.py` 一个待遇）：双击就会先 `git pull` 拉一遍最新代码，
  再 `node server.js` 启动服务器；`git pull` 失败（没装 Git、没联网、这个文件夹不是
  git clone 建的）就打印一句提示，然后照样用本地现有代码启动，不会卡死。
  ⚠️ **用了 `chcp 65001` 切到 UTF-8 控制台**，不然中文提示在默认 GBK 代码页下会变成乱码。
  ⚠️ 末尾有 `pause` —— `node server.js` 万一直接崩了，窗口不会一闪而过看不清错误。
- **以后用户要联机，标准流程是**：进这个 git clone 出来的文件夹，双击 `start-coop.bat`。
  它会自动确保是最新代码，不用再手动 `git pull` 或重新下载 ZIP。
- **怎么确认「联机端真的是新版本」**（2026-09-21 加，起因见「踩过的坑」第一条）：
  - bat 在启动服务器之前会打印一行 **`Now running: <短 commit>  <日期>`**，
    跟 GitHub 上 `main` 的最新提交一对就知道本地这份新不新。
  - `git pull` 失败时打印的是 **`*** UPDATE FAILED - the server will run the OLD code.`** ——
    这一行必须醒目，原来那句轻飘飘的 `Update failed. Using the local copy.` 很容易被划过去。
  - **浏览器那一侧不用再操心**：`server.js` 现在发 `no-cache` + `ETag`，
    HTML 里的脚本还带 `?v=<mtime>`，改完代码刷新一下就是最新的（见「踩过的坑」第一条）。
- ⚠️ 这条只解决"联机服务器代码是不是最新"，**不会自动帮用户重新触发那两步防火墙授权**——
  防火墙那个警报只在 Windows 认为"这是一个新版本的程序"时才可能再弹一次，正常改代码不动
  `server.js` 监听的端口/协议的话，一般不会重新弹。

### 怎么跑起来（第一期，2026-09-20）

**电脑上**（只有电脑跑服务器，手机不用装任何东西）：

1. 打开这个仓库所在的文件夹（用文件资源管理器地址栏输入 `cmd` 回车，或者用 PowerShell
   `cd` 过去），确认目录里能看到 `server.js`
2. **双击 `start-coop.bat`**（推荐，会自动先拉最新代码再启动 —— 前提是这个文件夹是
   `git clone` 出来的，见上面「怎么自动更新」），或者手动跑 `node server.js`。
   黑窗口里会打出两行地址，形如：
   ```
   电脑上打开： http://localhost:8080/coop.html
   手机（同一个 WiFi）打开：
     http://192.168.x.x:8080/coop.html
   ```
   第一次跑，Windows 安全中心会弹一个「Windows Defender 防火墙已阻止此应用的某些功能」——
   **勾上「专用网络」，点「允许访问」**（公用网络不用勾，勾了也没坏处）。不允许的话手机连不上。
3. 这个黑窗口**不能关**，关了服务器就没了（两人都会掉线）。想停止就在窗口里按 `Ctrl+C`。

**两台手机（或一台手机一台电脑）**（2026-09-20 又续：房间号改数字 + 邀请链接）：

1. 确认两台设备跟**跑服务器的那台电脑**连的是**同一个 WiFi**（手机流量、访客网络不行）
2. 浏览器打开黑窗口里打出的那个 `http://192.168.x.x:8080/coop.html` 地址（照抄，不要打 https）
   —— 打开就是正常的主城画面，**不用先组队**，图鉴、设置都能先逛
3. 一个人点主城里的**「组队」**（在「洞窟」下面），点**「创建房间」**——会自动给一个
   **4 位数字房间号**（比如 `6148`），界面上会显示出来
4. 另一个人有两种加入方式：
   - **报数字**：也点「组队」→「加入房间」，把那 4 位数字输进去（手机上会自动弹数字键盘）
   - **发链接**（更省事）：创建房间那边点**「复制邀请链接」**，把链接发给对方（微信、短信都行），
     对方**直接打开那个链接就自动加入了**，不用再点任何按钮、也不用手输数字
5. 两边都连上之后，「组队」那一块的副标题会变成「房间 6148 · 房主/队友 · 已连接」
6. **先创建房间的是房主**：点「洞窟」能选去哪一章；队友点「洞窟」只能看，写着「等待房主选路线」
7. **房主选一条路线**（点一下），这时候还**不会**进去——卡片会高亮、下面写「等队友确认」。
   房主反悔了可以重新点别的路线，会覆盖掉上一次的选择
8. **队友点「确认，一起下去」**，两边才真的一起进洞
9. 清怪捡金阶段要**两人都点「寻路」**才会一起走；下楼前要**两人都点「下去」**才会真的下去
10. 如果中途有人掉线（切后台太久、信号不好），屏幕上会显示「等待队友…」，
    服务器还开着的话重新打开页面、走一遍第 3~4 步（用同一个房间号，或者再点一次那条邀请链接）
    就能接回来

### 不同网络怎么联机（公网穿透，2026-09-20 加）

用户问「我电脑当服务器的话能不能实现不同网络的联机」，**能，而且代码一个字都不用改**，
原因见下面「为什么不用改代码」。做法是：服务器照跑，另外拿一个**内网穿透**工具把本机的
8080 端口映射到一个公网地址上，两人都打开那个公网地址。

> ⚠️ **用户没有中国大陆身份证，做不了实名认证**（2026-09-20 问出来的）。
> **cpolar / 花生壳 / natapp 这类国内服务一律要实名，全部排除** —— 一开始推荐的是 cpolar，
> 已经作废，**别再推荐它们**。现在走的是 **Tailscale Funnel**（Google / GitHub 账号登录，
> 不实名、不花钱、地址固定），见下面「固定地址路线」。

#### ⚠️ cloudflared 那条路已经整套删掉了（2026-09-20）

用户嫌入口太多，说「现在有 3 个 bat 了，删掉没用的」。快速隧道的地址每次重启都变、
存档跟着 origin 换一份，固定地址那条路把它完全取代了，所以
**`start-coop-online.bat` / `coop-msg-url.txt` / `coop-msg-install.txt` 三个文件一起删了**，
连同里面那套「cloudflared → ngrok → cpolar 按优先级挑隧道工具」的跳转链。

**别凭记忆重写它** —— 真要找回来，去 git 历史里翻提交
「联机加固定地址入口：Tailscale Funnel」的上一版，整份 checkout 出来就行。

剩下两个入口，别再合成一个：

| 什么时候用 | 双击哪个 |
| --- | --- |
| 两人在同一个 WiFi | `start-coop.bat` —— 更快更稳，不经过任何第三方 |
| 两人不在同一个网络 | `start-coop-fixed.bat` —— Tailscale Funnel，地址固定 |

#### ⚠️ 写 bat 的规矩（两个 bat 都适用，全是踩出来的）

- **找 exe 是五段兜底**（`:findexe` 子程序）：PATH → **仓库目录本身** → `%ProgramFiles%` /
  `%ProgramFiles(x86)%` / `%LOCALAPPDATA%` 下的同名文件夹。
  Tailscale 装在 `%ProgramFiles%\Tailscale\` 下，第三段正好命中；
  「丢进仓库目录」那一段是留给「单文件 exe、不用装」那类工具的，别删。
- 工具没找到就**只起游戏服务器**（局域网照样能玩）、打印安装步骤、顺手打开下载页，
  **不会因为没装工具就整个启动失败**。
- ⚠️ **两个 bat 里一个中文都不许有**（2026-09-20 用户实测炸了，详见下面「踩过的坑」）——
  中文说明全部放进 `coop-msg-lan.txt` / `coop-msg-tailscale.txt` / `coop-msg-fixed.txt` /
  `coop-msg-funnel-off.txt`
  （UTF-8 无 BOM、CRLF），bat 里用 **`type`** 打出来。改提示文字改那三个 txt，别往 bat 里写。
- ⚠️ **两个 bat 必须是 CRLF**——里面用了 `goto` / 标签 / `call :findexe` 子程序，
  **LF-only 会让 `call :label` 直接失效**（2026-09-20 真踩了）：
  `The system cannot find the batch label specified - findexe`，然后带着空变量往下跑，
  表现成「No tunnel tool found」这种**看着像别的 bug 的假象**。
  ⚠️ 根因是改文件的方式：Python 文本模式读（universal newlines 把 `\r\n` 吃成 `\n`）
  再 `newline=""` 写回，会**静默**转成 LF。改完这两个 bat 一律用二进制收尾并验：
  `tr -cd '\r' < x.bat | wc -c` 要等于 `wc -l`。
- ⚠️ 里面的 `echo` **一律不写半角括号** —— 批处理的 `if (...)` 块里出现裸的 `)` 会提前闭合块，
  报「此时不应有 xxx」。
- ⚠️ `:findexe` 这个子程序**必须放在所有 `goto done` 之后、`:done` 之前**，
  不然主流程会一路掉进它里面。
- ⚠️ **挑工具那段不能写成 `if ... ( set "TOOL=%_p%" )` 那种块** ——
  块里的 `%_p%` 在**整块被解析时**就展开了，那会儿 `call :findexe` 还没跑，永远是空的。
  写成 `if not defined _p goto no_ts` 这样的跳转链，就是为了绕开这个。
- ⚠️ `start` 开出来的**新窗口不继承父窗口的代码页**，所以服务器那一行是
  `start "Youxu Server" cmd /k chcp 65001 ^>nul ^&^& node server.js` —— 不加的话
  `server.js` 打出来的中文在新窗口里是乱码。`^` 是批处理转义，别删；
  也**别改成** `cmd /k "chcp 65001 >nul && node server.js"`，串里有 `>` `&` 时 cmd 不脱外层引号。

#### 固定地址路线：`start-coop-fixed.bat`（Tailscale Funnel，2026-09-20 加）

用户 2026-09-20 问「怎么做固定域名」，挑的是**先用 Tailscale Funnel** ——
不花钱、不买域名、**队友什么都不用装**。cloudflared 的快速隧道地址每次重启都变，
Funnel 给的是一个**永远不变的** `https://<机器名>.<tailnet>.ts.net`，
所以**存档也跟着固定下来了**（origin 不再变，见下面那一节）。

⚠️ **跨网络就只有这一个入口了**（cloudflared 那套已经删干净，见上面那一节）。

脚本做的事：`git pull` → `call :findexe tailscale.exe`（跟 `start-coop.bat` 共用的那套
五段兜底子程序，Tailscale 装在 `%ProgramFiles%\Tailscale\` 下，第三段正好命中）→
开服务器窗口 → `tailscale funnel --bg 8080` → 把 `tailscale funnel status` 重定向到
`%TEMP%\youxu_funnel.txt`，用 `findstr /b /c:"https://"` 抠出地址，
在窗口里框着打出 `<地址>/coop.html`。

- ⚠️ **Funnel 是后台跑的，没有隧道窗口**（`--bg`）—— 跟 cloudflared 那条路不一样，
  从头到尾只有服务器一个窗口。配置写在 tailscaled 里，**重启电脑也还在**，
  所以第二次之后双击这个 bat 基本就是「起个服务器 + 把地址打出来」。
- ⚠️ **地址抠不出来要退回原样打印**（`:no_url` 分支 `type` 那个临时文件）——
  `funnel status` 的输出格式会随版本变，别让解析失败变成「窗口里什么都没有」。
- 🔥 **踩过：地址打出来了，但手机打不开**（2026-09-21 用户实测）。
  根因是 **serve 和 funnel 两种状态的域名长得一模一样**，都是 `https://<机器名>.<tailnet>.ts.net`：
  **serve 只给同一个 tailnet 里的设备看**（公网 DNS 根本没有这条记录，手机解析不到），
  **funnel 才是对公网开放**。所以「窗口里有 ts.net 地址」≠「队友能打开」。
  ⚠️ 修法是在 `:not_public` 分支里查一句 **`findstr /c:"Funnel on"`** ——
  `funnel status` 里没有这两个字就当没开，照样把地址打出来但**大写警告 + 原样 dump 状态**，
  再 `type coop-msg-funnel-off.txt` 教用户去 admin 页点 **Enable HTTPS**、
  以及点那条 `login.tailscale.com/f/funnel?node=...` 的批准链接。
  ⚠️ **别把这个检查去掉** —— 没有它的那一版（第一版）只会把地址框出来，
  用户拿去发给队友，然后两头都莫名其妙。
- ⚠️ **`--bg` 是较新版本才有的 flag**：老版本报 `unknown flag`、errorlevel 非 0，
  走 `:ts_failed` 打印 `coop-msg-tailscale.txt`（里面写了「去官网覆盖安装一次」）。
- ⚠️ 中文照旧一个字都不许进 bat，两段提示放在 `coop-msg-tailscale.txt`（第一次怎么装）和
  `coop-msg-fixed.txt`（拿到地址之后）里，**UTF-8 无 BOM、CRLF**；
  bat 本身**必须 CRLF + 全 ASCII**，理由见上面「写 bat 的规矩」那一节，改完照样要验
  `tr -cd '\r' < start-coop-fixed.bat | wc -c` 等于 `wc -l`。

**用户那一侧第一次要做的事**（以后都不用再做）：

1. https://tailscale.com/download/windows 下载安装，用 Google / GitHub / Microsoft 登录
   —— **不要手机号、不要实名、不花钱**
2. https://login.tailscale.com/admin/dns 里确认 MagicDNS 开着，点 **Enable HTTPS**
3. 双击 `start-coop-fixed.bat`；第一次开 Funnel 时命令行可能打出一个授权链接，
   点开在网页上同意（给这台机器加 `funnel` 属性）
4. 再双击一次，框出来的 `https://xxx.ts.net/coop.html` 就是**以后永远的那个地址**

**队友什么都不用装** —— Funnel 是对公网开放的，跟「Tailscale 组网、两边都装 App」
那套不是一回事，直接把链接发过去点开就能进。

✅ **2026-09-21 真机实测通过**：用户按上面四步设置完，手机（不在同一个网络）
直接打开 `https://<机器名>.<tailnet>.ts.net/coop.html` 进得去。
中间卡了一次，是 serve/funnel 那个坑（见下面那条 🔥）。

⚠️ 电脑还是得开着别休眠、服务器窗口还是不能关 —— 这两条换哪条路都躲不掉。
⚠️ **`ts.net` 在中国大陆同样可能打不开**（跟 `trycloudflare.com` 一个处境）。
真被墙了就退回「两边都装 Tailscale 走组网」那条，地址是固定的 `100.x.x.x`。

#### 为什么不用改代码

- `server.js` 的 `server.listen(PORT, cb)` **没绑 `127.0.0.1`**，本来就监听所有网卡，
  隧道工具从本机连 `localhost:8080` 当然连得上
- `net.js` 的 `wsUrl()` 是 `location.protocol === "https:" ? "wss:" : "ws:"` +
  `location.host` —— 页面从哪个域名发出来的，WebSocket 就连回哪个域名，
  **公网地址是 https 时会自动升级成 `wss://`**，不用手改
- `game.js` 里生成邀请链接那句是 `location.origin + location.pathname + "?room=" + ...`，
  同理自动跟着公网域名走
- **自测验证过**（2026-09-20）：带伪造 `Host: fake.r3.cpolar.top` / `Origin:` 的 WS 升级请求，
  `server.js` 照样返回 `101` 且 `Sec-WebSocket-Accept` 正确 —— **没有 Origin 校验挡住穿透**。
  以后要是给服务器加鉴权/防跨站，记得这条会被打破。

⚠️ **所以唯一的硬性要求是：必须打开服务器（经隧道）发出来的那个地址**，
不能用 GitHub Pages 那个网址 —— Pages 上没有 WS 服务器，而且 https 页面连不上 `ws://`。

#### 已知的限制和坑

- **电脑必须一直开着别休眠**，关了两人都掉线。
- **服务器那个黑窗口不能关。** Funnel 在后台跑，没有第二个窗口；
  但服务器没开的时候那个公网地址会打不开或者报 502。
- 延迟：协议是「房主算路广播 + 伤害上报定序」，不是逐帧同步，**一两百毫秒完全吃得住**，
  跨网络的延迟不会破坏玩法。真要出问题，症状会是「队友棋子跳格」而不是「打不动」。
- ⚠️ **如果人在中国大陆，`ts.net` 可能打不开或很慢** —— 这是网络环境问题，不是代码问题。
  这时退到 **Tailscale 组网**（见下面备选表，两边都装 App，地址是固定的 `100.x.x.x`）。
- **顺带解决了一个老坑**：公网地址是 `https`，所以「iOS Safari 在非加密页面上语音朗读
  可能不出声」那条（见「踩过的坑」）在这条路径下自动消失了。

#### ⚠️ 地址一变，存档就换了一份（origin 隔离）

**localStorage 是按 origin（协议+域名+端口）隔离的**，所以：

| 打开的地址 | 存档 |
| --- | --- |
| `https://audience20060904-lang.github.io/...`（单人版） | 一份 |
| `http://192.168.x.x:8080/coop.html`（局域网联机） | 另一份 |
| `https://xxx.ts.net/coop.html`（Tailscale Funnel） | 又一份，但**地址固定，所以这一份是稳的** |

这**不是 bug，也不是代码能修的** —— 浏览器的安全模型就是这样。后果是宝石、词汇熟练度、
图鉴、续玩档全部看不见。想让联机存档留得住，只有两条路：

1. **固定地址**（2026-09-20 **已经做了**，见上面 `start-coop-fixed.bat` 那一节）：
   **Tailscale Funnel**，地址恒定、队友什么都不用装、不花钱 —— 这是用户挑的那条。
   备选是 cloudflared 的**命名隧道**（要 Cloudflare 账号 + 一个**自己买的域名**，不要实名），
   好处是地址好看（`play.你的域名.com`）、还能顺带把 `www` 指给 GitHub Pages。
   地址不变 = 存档不变。
2. **把存档搬过去**：设置页那两个面板（「本地存档文件」/「跨设备导出码」）现在是 `hidden` 的，
   去掉 `hidden` 就能手动导出再导入。**用户没要求做，先别自作主张放出来**。

#### 备选方案（记一笔免得下次重新讨论）

| 方案 | 要不要实名 | 为什么没做成首选 |
| --- | --- | --- |
| cloudflared 快速隧道 | 不要（连账号都不用） | **2026-09-20 用过、真人实测通过，同日删掉** —— 地址每次重启都变，队友要重新拿链接、存档还跟着换。固定地址那条路取代了它 |
| **Tailscale Funnel** | 不要（Google/GitHub 登录） | **2026-09-20 已经做成 `start-coop-fixed.bat`** —— 地址固定、队友不用装东西。当初被排掉是因为误当成「组网要两边装 App」，Funnel 不是那样 |
| Tailscale 组网 / ZeroTier | 不要（Google/GitHub 登录） | 很稳、没有流量限制、地址固定，但**队友也得装 App 并加进同一个网络**，不如发个链接省事。**ts.net 和 cloudflared 一起被墙时的兜底** |
| IPv6 直连 | 不要 | 免费、不经第三方，但要家宽真给公网 IPv6 且路由器放行入站，运营商差异太大 |
| 路由器端口转发 | 不要 | 需要公网 IPv4，现在家宽基本都是大内网，多半申请不到 |
| 把 `server.js` 部署到云服务器 | 国内机器要实名+备案，国外机器不用 | 电脑不用开着了，但要花钱买机器，用户没这个需求 |
| cpolar / 花生壳 / natapp | **要，且要中国大陆身份证** | **用户做不了，已排除** |

## 验收

- **单人版必须完全不变**：用 Playwright（Chromium 在 `/opt/pw-browsers`，
  **别跑 `playwright install`**）打开 `index.html`，跑一遍进洞 → 走路 → 打怪 → 下楼，
  确认和改动前一模一样。**2026-09-20 跑过**：进洞/寻路/战斗/答题/翻标签页/练习模式开关，
  `#errbar` 全程没出现、`pageerror` 全程是空数组
- 联机：本机开两个浏览器窗口连同一个房间，走完一整层。**2026-09-20 跑过部分**：
  两个 Playwright context 连同一个房间号，验证了房主/队友分配、路线选择的房主独占、
  移动锁的双人 ready、世界同步、独立战斗不报错、队友状态条。
  **没有自动验证到"清完一层→下楼双确认→进第二层"这一段**（自动化脚本乱点选项，
  正确率只有随机水平，角色会先倒下）—— 这一步交给用户在真机上实际玩一遍确认
- ⚠️ 沙盒访问不了 `*.github.io`，本地自测用 `python3 -m http.server`（联机的静态文件服务
  现在由 `server.js` 自己提供，不用再单独起 `http.server`）

## 踩过的坑

- **联机端一直在跑旧代码：`server.js` 原来一个缓存头都不发**（2026-09-21 用户报
  「新版本无法正确同步至联机端」，已修）。`serveStatic()` 原来就是
  `readFile` → `writeHead(200, {Content-Type})` → `end`，**没有 `Cache-Control`、
  没有 `ETag`、没有 `Last-Modified`**。HTTP 规范允许浏览器对这种响应自己拍脑袋定一个
  新鲜期（启发式缓存），手机浏览器尤其激进 —— 于是 `git pull` 明明拉到了新代码、
  服务器读的也确实是新文件，**手机上加载的还是缓存里那份旧的 `game.js` / `content.js`**。
  ⚠️ **最难查的地方是它长得不像缓存问题**：电脑上看文件是新的、bat 也说 pull 成功了，
  两边控制台干干净净，只是游戏里没有新东西。
  ⚠️ **单人版没这个毛病** —— GitHub Pages 自己会发 `ETag` / `Last-Modified`，
  所以「网站是新的、联机端是旧的」这个组合本身就是这条坑的指纹。
  修法（`serveStatic()` 里，三条一起上）：
  `.html` 发 **`no-store`**（入口永不缓存）；其余静态文件发 **`no-cache` + `ETag`**
  （mtime-size，没变回 304 省流量）；HTML 里本地的 `<script src>` / `<link href>`
  由 `stampHtml()` 自动加上 **`?v=<那个文件的 mtime>`** —— 代码一改 URL 就变，
  浏览器必定重新拉，连「修复之前就已经存下的那份旧缓存」也绕不过去。
  ⚠️ **别把这一段删回原来那三行**，删了这个坑立刻回来。
- **新增消息类型忘了在 `server.js` 里加转发的 `case`**（踩过一次）。服务器 `onMessage` 的
  `switch(msg.t)` 遇到不认识的 `t` 会**安安静静地跳过**，不转发也不报错 ——
  表现是「队友点了确认，房主那边毫无反应，两边控制台还都干净」。
  ⚠️ **写完任何要靠服务器转发的新消息，回头检查一遍那个 `switch`**，
  这类坑比报错难查得多。
- **退出之后回不到原来那一趟：主因是「房主的地址栏里没有房间号」**
  （2026-09-20 用户真机实测报的，已修）。自动重连只认 URL 上的 `?room=`，
  而那个参数只有**点邀请链接进来的队友**才有 —— **房主自己创建房间时地址栏是干净的**。
  于是房主一刷新（或手机切后台被杀掉重开）就变成「读回了自己的续玩档、但不在任何房间里」，
  一个人在那儿走；队友那边反而自动连回去了，看到的是「等待队友…」。两头都像是存档丢了。
  ⚠️ **续玩档本身一直是好的** —— 实测 `youxu.run.v1` 写进去了、`resumeRun()` 也正常跑。
  别往存档那边查。修法是 `afterJoined()` 里 `history.replaceState` 把房间号写进地址栏，
  `btnTeamLeave` 里再摘掉。
- **`writeRun()` 不存怪身上的联机字段**（`cid` / `hpMate` / `mySide` / `curSide`）——
  存档里怪只有 `{d,x,y,hp,max,dmg,armor,xp,lt,s}`。所以 **`resumeRun()` 里必须补一句
  `if(COOP) coopPrepMobs()`**，否则读档回来 `m.cid` 是 `undefined`，
  `coopDealDamage()` 那条 `if(... m.cid != null)` 直接不发 `dmg` 消息 ——
  **伤害不上报、队友血条没数据，而且一点报错都不会有**。
  cid 是按数组下标发的，两人的续玩档来自同一份广播世界、顺序一致，所以对得上。
- ⚠️ **重连之后角色可能翻转**：房主掉线时 `server.js` 的 `onDisconnect()` 会把剩下那个人
  扶正成房主，原房主回来就成了队友。而「我砍的是哪条血条」（`mySide`）是按角色定的，
  所以 `afterJoined()` 里要再 `coopPrepMobs()` 绑一次。翻转本身不致命 —— 只要两边对
  「谁是 a 谁是 b」的认知一致（都来自服务器），就仍然自洽。
- **服务器缓存着 `room.world`** 并在有队友在场时补发给新连接的人，但那是**上一次广播**的那份。
  队友中途退出再回来时，房主可能已经清了半层，所以 `NET.on("resume")` 里加了一句：
  房主在局内就 `coopBroadcastWorld()` 重发一次**当前**状态。
  ⚠️ **两个人都退出时 `rooms.delete(room.code)`，缓存的 world 一起没了** ——
  这时靠的是两人各自的续玩档（两份内容相同，因为都是同一份广播世界 commit 下来的）。

- **`.bat` 里写中文会把脚本切碎**（2026-09-20，用户第一次跑公网版就炸了）：
  `chcp 65001` 写在文件中间时，**cmd 按字节偏移重新读剩下的内容**，多字节的中文让解析器错位，
  表现是某些行被拦腰截断、后半截当命令执行：
  ```
  '；同一个' is not recognized as an internal or external command
  '幽廊' is not recognized as an internal or external command
  ```
  **最坑的是前几行中文显示得好好的**，看着像「只是几句提示报错」，实际上
  `start "幽墟回廊 · 服务器" cmd /k node server.js` 这几行也被切了 ——
  **服务器和隧道根本没起来**，很容易误判成网络问题或 cloudflared 的问题。
  ⚠️ 用户那台是**英文 Windows**（报错是英文的），但控制台**渲染中文没问题** ——
  所以问题只在解析、不在显示，别往「装中文字体 / 换代码页」那个方向修。
  **解法**：bat 的代码部分全 ASCII，中文说明放 `coop-msg-*.txt` 用 `type` 打 ——
  `type` 只是把字节喂给控制台，不过命令解析器。
  ⚠️ 这条**对 `start-coop.bat` 一样成立**，它也一起改了（它原来是 LF + 中文，同样有风险）。

- **strict mode 下，`if(COOP){ function foo(){...} }` 这种写在 block 里的函数声明是块作用域的**，
  外面（比如定义在文件更前面的按钮 click 监听）调不到，会直接 `ReferenceError`。
  `coopToggleMove` / `coopConfirmStair` / `coopMateResume` / `coopUpdateWaitUi` 这几个
  一开始写在 `if(COOP){...}` 里面，被 `$("btnPathfind")`/`$("btnStairGo")` 的 click 监听调用时
  报错——后来挪到了 `if(COOP)` 外面、跟其它函数一样是顶层声明（顶层的 function 声明会整体提升，
  文本位置在调用点之后也没关系）。**以后往这个文件加新的联机专用函数，记住这条**：
  只有 `NET.on(...)` 的注册、`$("btnRoomJoin")` 这类只在 `if(COOP)` 分支里才用得到、
  也只在这个分支里被调用的代码，才能留在 `if(COOP){...}` 里面。
- **自测脚本的 `.relic` 选择器踩过一次坑**：页面上不止 `#relicList` 里有 `.relic`（`#swapNew`
  也是 `class="relic hot"`），用 `page.$(".relic")` 会抓到 DOM 里第一个匹配、但当前隐藏着的
  那个，点了没反应，弹窗永远关不掉，脚本卡死。这是**测试脚本的坑，不是游戏的 bug**，
  写起来记一笔免得下次又踩：以后写自动化测试要用 `#relicList .relic` 这种带上下文的选择器。
- 局域网是 `http://` 不是 `https://`。localStorage 和 WebSocket 都正常
  （混合内容限制只在 https 页面上），但**语音朗读在 iOS Safari 的非加密页面上可能不出声**。
  先做，真出问题再考虑自签证书（代价是手机上要点一次「继续访问」）
- `.github/workflows/deploy.yml` 的 `cp` 用了 `*.js` 通配，新增的 js（`net.js`）会自动上线；
  `coop.html` 是新的 html，**没有被那份 workflow 的 `cp` 捞到**，已经把它加进列表了
  （见下面「上线规矩」那条）。`server.js` 发到 Pages 上无害（没人执行它），不用特意排除。
  **这个 `.md` 文件不上线**，跟 `遗物数据表.md` / `check-words.py` 一样

## 上线规矩

照 `CLAUDE.md` 第 5 条：改完自测过就自动提交 → 推 `main` → 用 Actions API 确认部署 success。
**这次的例外**：如果某一步动了 localStorage 的键或存档格式，按第 5 条的规定**要先问用户**。

---

## 变更记录

**只留结论，过程不留。新的在前。**

- **2026-09-21** · **修「新版本无法正确同步至联机端」** —— 根因是 `server.js` 的静态服务
  一个缓存头都不发，手机拿的是缓存里的旧 `game.js` / `content.js`。现在 `.html` 走 `no-store`、
  其余走 `no-cache` + `ETag`，HTML 里的脚本自动带 `?v=<mtime>`。
  `start-coop.bat` 顺带会打印当前 commit，pull 失败的提示也改醒目了。详见「踩过的坑」第一条。
- **2026-09-21** · 第九批 30 件「跨流派组合」遗物上线，**联机协议一个字没改** ——
  逐件核过，没有一件碰共享世界（全在 `stats()` / `answer()` / `mitigate()` / `healUp()` 里，
  都是各自结算），所以第 4 节那张「碰世界状态的遗物」表仍然只有掘金和水两件。

- **2026-09-21** · **固定地址真机实测通过** —— Tailscale Funnel 这条路正式可用，
  `start-coop-fixed.bat` 是跨网络的唯一入口。
- **2026-09-21** · 组队面板里那段「在电脑上跑 node server.js，两台设备连同一个 WiFi……」
  的 `<p class="note">` **整块删了**（用户要求，跟「别堆提示文字」那条一致）。
  标题「两个人，一个房间号」+ 两个按钮已经说清楚了，**别加回来**。
- **2026-09-21** · 修「地址出来了但手机打不开」：serve 和 funnel 的域名一模一样，
  脚本现在查 `Funnel on` 才认，没开就报警并打印排查步骤（`coop-msg-funnel-off.txt`）。
- **2026-09-20** · 入口收敛成两个：**删掉 `start-coop-online.bat` / `coop-msg-url.txt` /
  `coop-msg-install.txt`**（cloudflared 快速隧道那一整套，用户嫌入口多）。
- **2026-09-20** · 固定地址入口 `start-coop-fixed.bat`（Tailscale Funnel）+ 两份中文提示
  `coop-msg-tailscale.txt` / `coop-msg-fixed.txt`。快速隧道那个地址每次重启都变、存档跟着换，
  这条路把地址钉死成 `https://xxx.ts.net`。**代码一个字没改，都是 bat 的事。**
- **2026-09-20** · 跨网络真机实测通过（用户和朋友用 cloudflared 打完一整层，零问题）。
  同一天修了「退出后回不到原来那一趟」：根因是**自动重连只认 URL 上的 `?room=`，
  而房主的地址栏里从来没有它**，刷新后房主成了「有存档、没房间」。
  改法是 `afterJoined()` 里 `history.replaceState` 把房间号写进地址栏、离队时摘掉。
- **2026-09-20** · 第二期「双血条战斗」完成：怪的两条独立血条、打空自己那条转去帮队友砍、
  倒地不出局、队友清层原地复活。
- **2026-09-20** · 第一期「骨架通」完成：`server.js`（零依赖，自己实现 WebSocket）、
  `coop.html` + `net.js`、`game.js` 里的 `COOP` 开关、世界同步、双棋子、移动锁、下一层双确认。
  `index.html` 一个字没改，单人版零风险。
- **2026-09-20** · 两个 `.bat` 入口（局域网 / 跨网络）+ cloudflared 穿透。
  踩的编码和换行的坑全部记在「踩过的坑」那一节，别再踩。
- **2026-09-19** · 实现设计定稿：服务器不跑游戏逻辑，只维护两条血条 + ready + 倒地并定序；
  世界状态分层为「布局共享、消耗独立」；`m.hp` 始终指向「我眼前这条血条」。
- **2026-09-18** · 需求对齐、方案定稿、玩法规则逐条确认（见上面「玩法规则」一节）。
