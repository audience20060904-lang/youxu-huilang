# 幽墟回廊 · 局域网双人合作模式 · 方案与进度

**这个文件是给 Claude 看的，也是联机这件事唯一的交接文档。**
`CLAUDE.md` 管的是游戏本体，这一份只管联机。做联机的新会话，先读 `CLAUDE.md`，再读这一份。

> ## ⚠️ 维护规矩（最重要的一条）
>
> **只要动了联机相关的任何东西，就必须回来改这个文件。** 包括但不限于：
> - 做完了一步 → 更新下面的「当前进度」，把方块从 `[ ]` 划成 `[x]`，并在「变更记录」里加一行
> - 用户改了规则 → 就地改「玩法规则」那一节，别在别处另记一份
> - 踩了新坑 → 写进「踩过的坑」
> - 加了新文件 / 新的 localStorage 键 / 新的消息类型 → 写进对应小节
>
> 这个文件的目的就是：**任何一个新开的会话，读完它就知道联机现在做到哪了、下一步该干什么、
> 哪些事已经定死不许重新讨论。** 写得不够新，下一个会话就会白做或做反。

---

## 当前进度

**阶段：设计已定稿，代码一行未写。**

- [x] 需求对齐（2026-09-18，用户逐条确认，见下面「玩法规则」）
- [x] 方案定稿并落成这份文档
- [x] 第二轮构思：读过代码后的实现设计（见「实现设计」一节），修正了第一轮对服务器职责的误判
- [ ] **第一期 · 骨架通** ← 下一步就是它
  - [ ] `server.js`（Node + ws）：静态文件服务 + 房间服务
  - [ ] `coop.html` 联机入口 + `net.js` 客户端
  - [ ] `game.js` 里的 `COOP` 开关与钩子
  - [ ] 世界状态同步（复用 `writeRun()` 的序列化）
  - [ ] 双棋子 + 移动锁（两人都点寻路才走）
  - [ ] 下一层双确认
  - [ ] 队友状态条
  - [ ] 单人版回归自测（必须和改动前一模一样）
- [ ] **第二期 · 双血条战斗**
  - [ ] `m.hp` 从单值改成两条独立满血
  - [ ] 打空自己那条后转去帮队友砍
  - [ ] 倒地与救起
  - [ ] 战斗状态同步

---

## 架构：同一仓库，开关隔离

用户拍板过：**不另开仓库**。8300 行里有 2900 多行是纯数据（`words-a1.js` 1765 行、
`content.js` 623 行、`art.js` 584 行），复制一份就会永久分叉——以后加词、调遗物平衡
要做两遍，漏一次两边就对不上了。所以：

- **`index.html` 一个字都不改** —— 线上 GitHub Pages 永远是现在这个单人版，零风险
- 新增 **`coop.html`**：联机入口，加载同样的 `util.js` / `words-a1.js` / `content.js` /
  `art.js` / `style.css`，多加载一个 `net.js`，并在 `game.js` 之前设 `window.__COOP = true`
- `game.js` 里所有联机代码写成 **`if(COOP){...}`** 分支，`COOP` 在单人版恒为 false，
  **单人的执行路径一行都不能变**
- 新增 **`server.js`**：同时当静态文件服务器和房间服务器。
  用户在电脑上 `node server.js` 启动，手机开 `http://<局域网IP>:8080/coop.html`
- ⚠️ **`server.js` 必须零依赖**（2026-09-18 定）：只用 Node 自带的 `http` / `crypto` / `fs` 等内置模块，
  **自己实现 WebSocket 握手和帧解析**（大概 100 行），**不许 `npm install ws`**。
  理由：用户装完 Node 就能直接跑，不用等下载包、不会碰到国内 npm 超时；
  也跟这个项目「没有构建步骤、没有依赖」的一贯风格一致。仓库里**不要出现
  `package.json` / `node_modules`**
- 词库、遗物配置、美术**只有一份**，以后加词/调平衡改一次两边都生效

**线上和联机是两个入口**：`https://audience20060904-lang.github.io/youxu-huilang/`
永远是单人版，不受影响；联机必须开着电脑。

## 同步模型

- ⚠️ **「服务器权威」这个说法 2026-09-19 修正过，别照字面理解**（见下面「实现设计」第 1 节）：
  服务器是 Node，**没有 DOM，跑不了 `game.js`**，所以它不算游戏逻辑。
  它只维护几个权威数字（每只怪的两条血条、两人的 ready 和倒地状态）并负责**定序**；
  世界由**房主客户端**生成后整包广播；伤害仍然在各自客户端算（要用各自的遗物）
- **每台手机只跑自己那份 `P`** —— `game.js` 里 `P.` 出现 357 次、`G.` 240 次、`B.` 147 次，
  **绝不要在一份代码里塞两份角色状态**，那等于重写。每人看自己的第一视角，
  屏幕上多一条「队友状态条」（血、连击、遗物数、准备状态）
- **世界状态同步直接复用 `writeRun()` 那套序列化**（`game.js` 约 3117 行）：
  地图按行压成字符串、怪只存 defId + 状态，整包几 KB，正好当同步载荷
- ⚠️ **不要试图同步随机种子。** `Math.random()` 在 `game.js` 里直接调了 29 处、
  `util.js` 2 处，加上 38 处 `ri()` / `pick()`。两个人答的题不一样，随机序列立刻漂移。
  **地图和怪由权威端单点生成、整包广播给对方**，这是唯一可靠的做法

## 实现设计（2026-09-19 第二轮构思，读过代码之后）

这一节是**动手前必须看完的**。下面每条都对着 `game.js` 的真实代码核过，不是设想。

### 1. 三方分工（修正了第一轮的「服务器权威」）

第一轮写的「伤害、掉落、世界状态由服务器裁决」**做不到**：服务器是 Node，没有 DOM，
而 `game.js` 里到处是 `$()` 和 `render()`，整个文件在 Node 里跑不起来。正确的分工是：

| 谁 | 负责什么 |
| --- | --- |
| **服务器** | 房间、消息转发、**定序**，外加维护极少量权威数字 |
| **房主客户端** | 世界生成：跑 `genFloor()`，把结果整包广播 |
| **各自客户端** | 自己的 `P`、自己的伤害计算（要用自己的遗物）、自己的渲染 |

**服务器要维护的权威状态少得惊人**，一共只有这些：

- 每只怪的两条血条 `hpA` / `hpB`
- 两人的 ready 状态（寻路 ready / 下一层 ready）
- 两人的倒地状态
- 当前层号 + 世界包的版本号

⚠️ **服务器不 `require` 任何游戏文件**，它连 `content.js` 都不用读。
客户端把**算好的伤害数值**发上来，服务器只做减法、判死、广播。
这样 `server.js` 能保持在几百行，也不会跟游戏平衡的改动纠缠在一起。

### 2. 世界状态的分层：**布局共享，消耗独立**

这是整个联机设计的核心原则，想不清楚这条就会到处打补丁：

| 类别 | 例子 | 怎么同步 |
| --- | --- | --- |
| **共享且必须一致** | 地图、怪的位置、金币位置、阶梯位置、怪的两条血条 | 房主生成 → 整包广播；血条走服务器 |
| **每人独立消耗** | 泉、宝箱、祭坛、游商 | **各开各的**，各自从自己的 `G.things` 里删 |
| **天然就一致** | 金币 | 共享移动 → 两人同时踩到 → 各拿一份 → 各自 splice，结果必然相同 |

- 「每人独立消耗」意味着 A 开过的宝箱在 A 的地图上没了、在 B 的地图上还在。
  **这是有意的**，不是 bug —— 两个人本来就各看各的第一视角，而且遗物就是各抽各的
- ⚠️ 这一条**不影响移动限制的判定**：解除条件只看「怪 + 金币」，而这两样恰好都是必然一致的那一类。
  泉/箱/坛/商不进判定，所以两人的 `G.things` 不完全相同也不会让两边对「能不能走了」产生分歧

### 3. 关键技巧：`m.hp` 永远是「我眼前这条血条」

怪要有两条独立血条，最笨的改法是把 `m.hp` 换成 `m.hpA / m.hpB`，然后把 `game.js` 里
每一处读写 `m.hp` 的地方都改一遍。**不要这么做。** 正确的做法是：

> **每个客户端本地的 `m.hp` 始终指向「我现在正在砍的那条」**，另一条存在 `m.hpMate` 上只供显示。

这样 `answer()` 里那行 `m.hp -= dmg + extra`、盲斗的 `m.hp = 0`、回响之厅的 `m.hp -= hall`
**一个字都不用改**。只需要在三个点上挂钩：

1. **砍完之后**：把这次的伤害数值上报服务器（`dmg` 消息），服务器扣权威血条并广播
2. **`m.hp <= 0` 那一处判定**（`game.js` 约 1833 行，全文件**只有这一处**判怪死）：
   联机模式下要先问一句「对方那条还有血吗」——
   有就**切换目标**（`m.hp = m.hpMate`，继续答题，这就是「转去帮队友砍」），
   没有才走 `finishBattle(true)`
3. **收到对方扣血的广播**：更新 `m.hpMate` 并重画血条

⚠️ **唯一有竞态的地方就是「帮打」**：A 帮砍 B 那条时，A 和 B 可能同时把它砍到 0，
两边都以为是自己补的最后一刀。所以**所有伤害都走服务器定序**（自己那条也走，保持一条路），
局域网往返 1~5ms，手感上察觉不到。本地可以先乐观扣血、收到权威值再校正。

### 4. 只有 **3 件**遗物会碰世界状态

把 `genFloor()` / `nextFloor()` 里所有 `hasRelic()` 调用逐个核过了，115 件里只有这三件
会改变**共享的**世界，其余（油灯回血、钱袋给钱、盾誓、厚盾…）全是各自结算，不用管：

| 遗物 | 做什么 | 冲突点 | 处理 |
| --- | --- | --- | --- |
| **掘金 `dig`** | `genFloor()` 里金币堆 +3 | 只有一个人有时算不算 | **任一人有就生效**（合作模式好处共享，金币本来两人各拿一份） |
| **水 `water`** | `drinkAll()` 喝光**这一层所有的泉** | 泉是共享布局 | 按第 2 节：**泉的消耗是每人独立的**，A 的「水」只喝光 A 那份，B 的泉还在 |
| **盲斗 `blind`** | `nextFloor()` 里一口气跳 `BLIND_STEP`(4) 层 | 世界级，会把另一个人一起带走 | ⚠️ **待定，见下面第 8 节** |

⚠️ 以后**新增任何遗物，都要回来问一句「它碰不碰共享世界」** —— 碰的话就得在这张表上加一行。

### 5. `game.js` 的钩子点清单

联机代码全部写成 `if(COOP){...}`，单人路径一行不变。按要改的地方从上到下：

| 函数（行号是 2026-09-19 的） | 要加什么 |
| --- | --- |
| `newRun()` 64 | 房主生成世界；非房主等世界包到了再铺场 |
| `nextFloor()` 140 | 双确认才进；盲斗跳层按第 8 节的规则；`genFloor()` 只有房主跑 |
| `genFloor()` 234 | **完全不碰 DOM，可以原样复用** —— 只有房主跑，跑完把 `G` 整包广播 |
| `autoToggle()` 854 | 不再直接开，改成**上报「我 ready 了」**，等服务器的 `go` |
| `autoTick()` 838 | 移动锁阶段：只有收到 `go` 才走；160ms 轮询本身照旧 |
| `autoPath()` 810 | ⚠️ **只有房主算目标**，算完广播路径；否则两人各算各的会分叉 |
| `tryMove()` 875 / `#map` 的 click | 移动锁阶段 + 寻路开着时禁用手动（用户要求） |
| `onEnter()` 894 | 金币各拿一份（天然一致）；泉/箱/坛/商各自独立消耗 |
| `startBattle()` 971 | 两人同时进；初始化 `m.hp` / `m.hpMate` 两条血条 |
| `answer()` 1490 | 扣完血上报服务器（`m.hp` 本身不用改，见第 3 节） |
| 判怪死 1833 | **全文件唯一一处** `m.hp <= 0` —— 加「切换目标 or 真死」的分岔 |
| `P.hp <= 0` 三处 1179 / 1834 / 1993 | 联机模式下走**倒地**，不是 `gameOver()` |
| `finishBattle()` 1997 | 怪死是共识事件，等服务器的 `dead` 再收 |
| `closeBattleWin()` 2017 | 经验/金币/回血各自独立；`G.stair` 改写位置天然一致（怪死在哪是共识） |
| `takeHit()` 1937 | 各自独立，不用同步；只是血见底时转倒地 |
| `deathSave()` 1844 | 薪火/回魂都用完了 → 倒地而不是结束 |
| `endRun()` 3249 | **两人都倒下**才结算；各算各的宝石 |
| `flee()` | ⚠️ **待定，见第 8 节** |
| `render()` 577 | 画两个棋子（见第 7 节） |
| `renderBattleBars()` 1000 附近 | 画两条怪血条 + 队友状态条 |
| `commit()` 3110 | 各自存各自的，**localStorage 的键一个都不用新增** |

### 6. 消息协议草案（`server.js` ↔ `net.js` 的契约）

JSON 文本帧，每条带一个 `t`（type）。**先照这张表实现，不够了再加，别临时发明字段。**

**客户端 → 服务器**

| `t` | 带什么 | 谁发 | 什么时候 |
| --- | --- | --- | --- |
| `join` | `room`, `name` | 两人 | 进页面时 |
| `world` | `floor`, `pack`（`writeRun()` 那套序列化） | **只有房主** | 每次 `genFloor()` 跑完 |
| `ready` | `what:"move"\|"floor"`, `on:bool` | 两人 | 点寻路 / 点下一层 |
| `path` | `steps:[{x,y}...]` | **只有房主** | `autoPath()` 算出目标之后 |
| `dmg` | `mob`（下标）, `side:"a"\|"b"`, `n` | 两人 | 砍完一刀（`side` 指砍的是谁那条血条） |
| `me` | `hp`, `maxHp`, `combo`, `relics`, `down` | 两人 | 状态一变（给队友状态条用） |
| `used` | `kind`, `x`, `y` | 两人 | 用掉了泉/箱/坛/商（**只是通知对方，不改对方的世界**） |

**服务器 → 客户端**

| `t` | 带什么 | 意思 |
| --- | --- | --- |
| `joined` | `host:bool`, `mate:bool` | 你是不是房主、对方在不在 |
| `world` | 原样转发 | 非房主收到就铺场 |
| `go` | `what:"move"\|"floor"` | **两人都 ready 了**，可以走 / 可以下一层 |
| `path` | 原样转发 | 非房主照着这条路走 |
| `hp` | `mob`, `a`, `b` | **权威血量**，客户端照这个校正 |
| `dead` | `mob` | 这只怪两条血条都空了，收掉它 |
| `mate` | 对方那条 `me` | 刷队友状态条 |
| `pause` | `why:"disconnect"` | 对方掉线，整局暂停 |
| `resume` | — | 对方回来了 |

⚠️ 几条要点：
- **`dmg` 只发「伤害数值」，不发血量** —— 血量是服务器算的，客户端算血量就会各说各话
- **`world` 只有房主发**，服务器不校验内容，只管转发和记版本号
- `used` 是**纯通知**（让对方的日志里能看到「队友开了宝箱」），**收到它不要去改自己的 `G.things`**
  —— 消耗是独立的，见第 2 节
- 断线检测靠 WebSocket 的 close 事件 + 30 秒心跳，两条都要有：
  手机锁屏时连接常常不会干净地关掉

### 7. 两个棋子：**逻辑同格，渲染错开**

用户要「地图上画两个棋子，一前一后走同一条路」。实现上**不要**让两个人真的占不同格子 ——
`autoPath()` 里的 `roomOf(P.x, P.y)` 和 `nearestBy()` 都按位置算，位置一差目标就可能不同，
路线立刻分叉。正确做法：

> **两人逻辑上共享同一个 `P.x / P.y`**，渲染时把自己画在格子偏左、队友画在偏右并缩小一号。
> 纯视觉偏移，不占格子、不进寻路。

这样 `findPath` / `goTo` / `stepWalk` / `onEnter` **一个字都不用改**。

### 8. 还要用户拍板的三件事

- **盲斗跳层**：只有一个人带「盲斗」时，要不要把另一个人也一起带下去 4 层？
  （跳层会跳过整层的怪和金币，对没带的那个人是白丢经验和钱）
- **撤退**：`flee()` 掉一半生命上限。联机里是两人一起撤（跟移动锁一个道理）、各撤各的，
  还是干脆禁用？
- **房主怎么定**：第一个连进房间的人当房主，还是开服务器那台电脑上单开一个「主机页面」？
  （前者更省事，后者在有人掉线时更稳）

## 玩法规则（用户 2026-09-18 逐条确认，别再重新讨论）

**角色完全独立**：遗物、金币、等级、血、连击、心魔、`P.used`、出题全部各是各的。
两人的熟练度不同，各抽各的题。

### 移动限制

- 解除条件 = 这一层的怪**全清完** `且` 地上金币**全捡完**（不只是清怪）
- 解除之前：**必须两人都点了寻路**才开始走，走同一条路、打同一只怪
- **寻路开着的时候禁用手动点地图**（防止一个人乱点把共享路线打乱）
- 任一人再点一次寻路 = 两人一起停下
- 解除之后：两人自由活动，各自决定用不用泉 / 箱 / 坛 / 商，最后双确认下楼
- 所以一层的节奏是三段：**同步清场 → 自由逛 → 双确认下楼**
- 地图上画**两个棋子**，一前一后走同一条路
- **一场战斗打完，寻路状态保留、自动接着走**（一层十几只怪，每只都要两人重新点太烦）
- 每下一层仍然要重新手动开（现有规矩，`nextFloor()` 里的 `autoOff()`）

### 战斗

- **两人同时各答各的题**，互不等待。各自的连击、遗物、暴击、7 秒读条都独立
- 怪的反击各打各的（A 答错 A 掉血）
- **怪有两条独立血条，每条都是原血量的满血**（实际总血量 ×2，但两人同时砍，
  单人体感节奏不变）。`m.hp` 要从单值改成两条
- **A 把自己那条清空后，可以转去帮 B 砍 B 那条**。强的人能带弱的人
- 两条都清空，怪才死

### 倒地

- 血掉光**不出局**：停止答题，怪也不再打他
- 队友把这一层的怪清完，他原地复活（回 1 点血）
- **两人同时倒下才算这趟结束**
- **倒地的人不阻塞移动** —— 视为自动同意，队友一个人点寻路就能走
- ⚠️ **A 倒地后不做任何难度缓解**：怪的两条血条都还在，B 必须独自砍完。
  这是用户明确选的硬核档（已经提醒过后果，用户坚持），**不要自作主张减半或让血条消失**。
  实战下来觉得劝退的话由用户来提，改起来是一行的事

### 局内资源

- 地上一堆金币，**两人各拿一份**（不是抢，合作模式不该为捡钱吵架）
- 怪掉遗物，**两人各自抽各自的三选一**
- 泉 / 宝箱 / 祭坛 / 游商：走到那一格后各自决定用不用，互不阻塞，各花各的金币

### 结算

- 各算各的宝石，存进各自手机的 localStorage。熟练度、图鉴、统计也各存各的

### 练习模式

- 局前由房主统一定，两人一致（它直接改难度平衡，只有一个人开会很怪）

### 断线

- 谁掉线就整局暂停，屏幕上写「等待队友重连」，重连回来接着走
- 最坏情况退回本层开头（续玩档本来就存「刚踏进这一层的样子」，这条规矩不变）

## 用户的运行环境

- **用户用 Windows**（2026-09-18 确认）。装 Node 的步骤已经给过他了：
  nodejs.org 下 LTS 的 .msi，一路 Next，「Tools for Native Modules」那个复选框保持不勾
- 查局域网 IP 用 `ipconfig` 看「IPv4 地址」
- ⚠️ **第一次跑 `node server.js` 会弹 Windows 安全中心警报，必须勾「专用网络」再允许访问**，
  不勾手机连不上 —— 这是最常见的坑，第一期做完要把它写进给用户的启动说明里
- 第一期做完，**把完整启动步骤写成一节放进这个文件**（Windows 为主，Mac 顺带）

## 验收

- **单人版必须完全不变**：用 Playwright（Chromium 在 `/opt/pw-browsers`，
  **别跑 `playwright install`**）打开 `index.html`，跑一遍进洞 → 走路 → 打怪 → 下楼，
  确认和改动前一模一样
- 联机：本机开两个浏览器窗口连同一个房间，走完一整层
- ⚠️ 沙盒访问不了 `*.github.io`，本地自测用 `python3 -m http.server`

## 踩过的坑

（还没开工，暂空。**踩到就往这儿写**，别只写在提交信息里。）

- 局域网是 `http://` 不是 `https://`。localStorage 和 WebSocket 都正常
  （混合内容限制只在 https 页面上），但**语音朗读在 iOS Safari 的非加密页面上可能不出声**。
  先做，真出问题再考虑自签证书（代价是手机上要点一次「继续访问」）
- `.github/workflows/deploy.yml` 的 `cp` 用了 `*.js` 通配，新增的 js 会自动上线。
  `server.js` 发到 Pages 上无害（没人执行它），不用特意排除。
  **这个 `.md` 文件不上线**，跟 `遗物数据表.md` / `check-words.py` 一样

## 上线规矩

照 `CLAUDE.md` 第 5 条：改完自测过就自动提交 → 推 `main` → 用 Actions API 确认部署 success。
**这次的例外**：如果某一步动了 localStorage 的键或存档格式，按第 5 条的规定**要先问用户**。

---

## 变更记录

**写在最上面，新的在前。每做完一步就加一行。**

- **2026-09-19** · 第二轮构思，新增「实现设计」整节。读代码后的几个结论：
  服务器不跑游戏逻辑（Node 没 DOM），只维护两条血条 + ready + 倒地并定序；
  世界状态分层为「布局共享、消耗独立」；`m.hp` 始终指向「我眼前这条血条」，
  `answer()` 里扣血那几行一个字不用改；全文件判怪死只有一处（1833 行）；
  115 件遗物里只有掘金/水/盲斗 3 件碰共享世界；两个棋子逻辑同格、渲染错开。
  留了三件待用户拍板：盲斗跳层、撤退、房主怎么定。

- **2026-09-18** · 定下 `server.js` 零依赖（自己实现 WebSocket，不用 npm 包）；
  确认用户是 Windows，记下装 Node 的步骤和防火墙那个坑。

- **2026-09-18** · 需求对齐 + 方案定稿，落成这份文档。代码一行未写。
  用户确认的关键选择：Node 服务器 / 怪各自一条满血 / 同时各答各的 / 倒地不出局队友救起 /
  同一仓库开关隔离 / 倒地的人不阻塞移动 / 倒地后不缓解难度 / 战后寻路保留。
