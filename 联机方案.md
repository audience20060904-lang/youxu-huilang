# 幽墟回廊 · 局域网双人合作模式 · 方案与进度

**这个文件是给 Claude 看的，也是联机这件事唯一的交接文档。**
`CLAUDE.md` 管的是游戏本体，这一份只管联机。做联机的新会话，先读 `CLAUDE.md`，再读这一份。

> ## ⚠️ 维护规矩（最重要的一条）
>
> **只要动了联机相关的任何东西，就必须回来改这个文件。** 包括但不限于：
> - 做完了一步 → 更新下面的「当前进度」，把方块从 `[ ]` 划成 `[x]`，并在「变更记录」里加一行
> - 用户改了规则 → 就地改「玩法规则」那一节，别在别处另记一份
> - 踩了新坑 → 写进「踩过的坑」
> - 加了新文件 / 新的 localStorage 键 / 新的消息类型 → 写进对应小节
>
> 这个文件的目的就是：**任何一个新开的会话，读完它就知道联机现在做到哪了、下一步该干什么、
> 哪些事已经定死不许重新讨论。** 写得不够新，下一个会话就会白做或做反。

---

## 当前进度

**阶段：第一期「骨架通」+ 第二期「双血条战斗」+ 移动锁改窄/战斗窗队友血条这一续，
代码都写完了、都自测过。真人两台真机实测还没做（都需要用户帮忙这一步）。**

- [x] 需求对齐（2026-09-18，用户逐条确认，见下面「玩法规则」）
- [x] 方案定稿并落成这份文档
- [x] 第二轮构思：读过代码后的实现设计（见「实现设计」一节），修正了第一轮对服务器职责的误判
- [x] 三条待定规则已由用户拍板（盲斗不跳层 / 禁用撤退 / 房主是先连上的手机），并补上了进洞前的同步
- [x] **第一期 · 骨架通**（2026-09-20 做完）
  - [x] `server.js`（Node 内置模块，零依赖）：静态文件服务 + 房间服务，自己实现 WS 握手/帧解析
  - [x] `coop.html` 联机入口 + `net.js` 客户端
  - [x] `game.js` 里的 `COOP` 开关与钩子
  - [x] 世界状态同步（`packWorld()`/`applyCoopWorld()`，复用 `writeRun()` 那套 map/mobs 打包方式）
  - [x] 双棋子（视觉层，见下面「跟原计划的出入」）+ 移动锁（两人都点寻路才走）
  - [x] 下一层双确认
  - [x] 队友状态条
  - [x] 单人版回归自测（Playwright 跑过 index.html 的进洞/走路/寻路/战斗/答题/翻标签页，
        `#errbar` 全程没出现过，`pageerror` 是空数组）
  - [x] 联机联调自测：本机两个 Playwright context 连同一个房间号，验证了房主/队友角色分配、
        房主选路线队友只能看不能点、两人都点寻路才真正开始走、地图和怪的初始状态一致、
        战斗各打各的不报错、队友状态条实时刷新。**没有跑到自动清完一整层**——
        测试脚本是随机点选项（正确率只有约 25%），角色会先掉血倒下，这是测试脚本笨，
        不是游戏的问题；真人玩会正常清层，下一步交给用户在真机上试。
  - [x] **组队改成主城板块**（2026-09-20 续，用户要求）：不再一进页面就拿遮罩挡住整个游戏，
        `coop.html` 打开就是正常主城；洞窟下面新加一块「组队」，点开是「创建房间」/
        「加入房间」，连上之后显示房间号/角色/队友状态
  - [x] **选难度改成"房主选、队友确认"**（2026-09-20 续，用户要求）：房主点一条路线只是提议
        （会高亮，还能反悔重选），队友看到「确认，一起下去」按钮，点了两边才真的进
  - [x] **房间号改数字 + 邀请链接**（2026-09-20 又续，用户要求）：4 位数字房间号（数字键盘）、
        创建房间后有邀请链接一键复制，队友打开链接自动加入
  - [x] **补上了 `used` 消息**（2026-09-20 又续，之前协议表写了但代码里一直没人发）：
        踩到泉/坛/箱/商时通知队友，队友日志里写一句"队友用了 X"
  - [ ] **二维码**（用户问过"更简单的方法"，提了这个选项但没做）：没有摄像头没法实测扫描，
        手搓零依赖二维码编码器风险较高，做之前建议先问用户接不接受引入一个现成的二维码库
        （CDN 或本地 vendor 一份，二选一都要用户点头，因为都不是"零依赖"了）
  - [ ] 真人在两台真机上实测一整层（含下楼双确认）—— 这一步需要用户帮忙，见下面「怎么跑起来」
- [x] **第二期 · 双血条战斗**（2026-09-20 做完，代码写完、自测过；见下面「第二期怎么做的」）
  - [x] `m.hp` 语义不变（永远是「我现在正在砍的那条」），怪身上挂两条独立满血
  - [x] 打空自己那条后转去帮队友砍
  - [x] 倒地与救起
  - [x] 战斗状态同步（伤害上报服务器定序，怪死是服务器广播确认的共识事件）
  - [ ] 真人在两台真机上实测（这一步跟第一期一样，需要用户帮忙）
- [x] **移动锁改窄 + 战斗窗队友血条可视化**（2026-09-20 又续，用户要求，代码写完、自测过）
  - [x] 移动锁解除条件从「怪清完 `且` 金币全捡完」改成**只看怪**（`coopLocked()`）
  - [x] 怪没清完之前，**手动移动整个禁用**（原来只是"寻路开着时禁用"，现在是不管有没有开
        寻路都禁用，只能靠两人都点的共享寻路走）
  - [x] 寻路目标在锁定阶段**只挑怪，不挑金币**（`autoPath()`），金币留到解锁之后各找各的
  - [x] 怪清完那一刻**立刻整体解锁**（`coopUnlockMove()`）：寻路停一下、手动移动恢复、
        队友棋子从"跟着走同一条路"变回"各画各的最后上报坐标"，两人从此各按各的节奏
  - [x] 战斗窗新增两条半透明（50%）队友血条：怪血条下面的 `#foeMateBar`（队友正在砍的那条）
        和自己血条上面的 `#bMateHpBar`（队友本人剩多少血）
  - [x] 自测：两个 Playwright context 连同一个房间，验证了清怪前两边方向键/点地图都不生效
        （日志里看到"这一层的怪还没清完"）、两人都点寻路后才一起走向同一只怪、进入战斗后
        两条队友血条都正确显示且 `getComputedStyle().opacity` 读出来是 `0.5`，全程两边
        `pageerror` 为空。**没有自动测到"清完一层之后真的各自独立走"**——原因跟第一期
        自测记录的一样：脚本乱点选项正确率太低，清不完一层，这一步交给用户在真机上确认
  - [ ] 真人在两台真机上实测（跟前两期一样，需要用户帮忙）

### 跟原计划的出入（第一期做的时候临场改的几处）

写代码之前那两版「实现设计」设想的是**战斗也走服务器定序**（第 3 节：`m.hp` 始终指向
「我眼前这条血条」，帮队友砍那一套）。**这套东西第一期没做** —— 它就是「第二期 · 双血条战斗」
要解决的问题，提前搭等于把两期的活儿混在一起，风险太大。第一期实际做出来的是一个更简单、
但完全立得住的版本：

- **地图/怪的初始状态/物件/楼梯是共享的**（房主生成，广播给队友，跟设计里说的一样）。
- **战斗完全不联网** —— 两人各打各的怪、各算各的伤害、各出各的题，跟单人版一模一样，
  一个字没改 `answer()` / `m.hp` 那一套。两人的怪列表都是从同一份广播出来的初始状态各自
  独立往下打，**没有「一只怪两条血条」，是两份各自独立的怪**。这恰好也满足玩法规则里
  「两人同时各答各的题，互不等待」那条 —— 只是「怪」本身也是各打各的，不是共享一份血条。
  ⚠️ 这意味着**现在还没有「打空自己那条转去帮队友砍」**，也没有「倒地不阻塞移动」——
  这些是第二期要补的。
- **移动锁靠"房主算路 + 广播 + 队友照抄"实现**：只有房主的 `autoPath()` 真的跑 BFS，
  算完一段路（去下一个最近的怪或金币）就广播给队友；队友收到 `path` 消息后原样走
  （`stepWalk()` 复用得一个字没改）。**房主会等队友"不忙"（没在战斗/没在弹层）才敢算下一段**
  （`coopMateBusy`，靠 500ms 一次的状态心跳判断），免得队友还在打上一只怪，房主已经跑没影了。
  这不是像设计稿说的"两人逻辑上共享同一个 P.x/P.y"那么严格的同格锁步，
  是**走同一条路线、但各自按自己的节奏推进**，两人的格子坐标经常会有一两步的差 ——
  实测下来这个差距是可接受的，视觉上「队友棋子」用的就是队友最后一次上报的坐标
  （`coopMateX/Y`，见渲染那条）。
- **协议表加了几条原来没写的消息**（都是"先照着表实现，不够了再加"的那种情况，
  下面这些是**实际实现的**，跟上面那张 2026-09-19 写的草案表已经对不上了，
  以这里为准）：
  - `route {id}`（客户端→服务器→队友，只有房主能发）：房主在洞窟里点一条路线时发，
    但**只是提议，不会自动进**（2026-09-20 续改的——一开始是收到就自动
    `enterRoute(id, true)`，用户要求改成两人都确认才进）。队友收到后存成
    `coopProposedRoute`，洞窟弹层上高亮那一条、露出「确认，一起下去」按钮。
  - `enterConfirm {id}`（客户端→服务器→房主，只有队友能发，2026-09-20 续加）：
    队友点确认时发，房主收到才真的 `enterRoute()`；队友自己点确认的同时也在本地
    `enterRoute(id, true)`，不用等服务器回头再告诉它一遍。
    ⚠️ **踩过的坑**：这条消息第一次实现时只写了客户端两边的收发，**忘了在
    `server.js` 的 `onMessage` 里加转发它的 `case`**——服务器遇到不认识的 `t`
    会在 `switch` 里直接跳过，不转发也不报错，表现就是"队友点了确认，房主那边
    毫无反应，两边控制台还都干净"。**以后新增任何客户端间要靠服务器转发的消息
    类型，写完客户端逻辑一定要回来检查 `server.js` 的 `switch(msg.t)` 里加没加
    对应的 `case`**，这类"消息类型没转发"的坑比报错更难查，因为它就是安安静静
    地什么都不做。
  - `practice {on}`（客户端→服务器→队友，只有房主能发）：练习模式的开关，房主定、
    队友跟着变。
  - `go` 消息加了一个 `on` 字段（`{t:"go", what, on}`）：原表只写了"两人都 ready 了才发 go"，
    但"寻路"是常驻开关，需要"任一人再点一次就都停下"，所以服务器在 `ready{on:false}`
    时也会广播一条 `go{on:false}`，两边收到就一起停。`floor`/`enter` 这类一次性事件的
    ready 状态在触发后会自动复位（`server.js` 的 `onMessage` 里）。
- **联机方案里"5. game.js 的钩子点清单"那张表**列的是全量设计（含第二期的战斗联网），
  第一期实际只碰了表里跟"世界生成/移动/下楼"相关的几行：`newRun` 没改（COOP 下走的还是
  它原来的路，只是内部 `nextFloor()` 分了叉）、`nextFloor`、`genFloor`（原样复用，未改一个字）、
  `autoToggle`（现在是 `coopToggleMove`）、`autoTick`、`autoPath`（原样复用，只是队友不跑它）、
  `tryMove`（没改——联机里移动锁生效时队友的手动点击本来就走不到 goTo，因为寻路开关一开
  队友端根本没有自己的 autoPath/walkPath 循环，是被动接收 `path`）、`render`、`commit`（没改，
  各存各的续玩档）、`enterRoute`。`answer` / `finishBattle` / `closeBattleWin` / `takeHit` /
  `deathSave` / `flee`（新增了"联机里禁用"这一条）/ `endRun` 这几行留给第二期。

### 跟原计划的出入（第二期做的时候临场改的几处，2026-09-20）

- **怪的身份用 `cid`，不是数组下标**（设计稿第 6 节协议表写的是「`mob`（下标）」，
  没写清楚是哪种下标）。踩过一步就想明白了：数组下标会因为 `splice` 移动 ——
  A 那边刚杀掉第 3 只，后面的怪全部往前挪一位，B 那边可能还没收到这条 `dead` 广播，
  两边的「下标 3」这时候已经不是同一只怪了。所以每只怪在这一层生成/铺场时
  （`coopPrepMobs()`）就挂一个**固定不变**的 `m.cid`（就是它在广播那份世界包里的原始序号），
  `dmg`/`hp`/`dead` 三条消息全部按 `cid` 找怪（`mobByCid()`），不管数组这时候长什么样。
  ⚠️ **服务器那边 `room.mobs` 也是拿 `cid` 当 key**（`{cid: {a,b,dead}}`），
  每次收到 `world` 就整个重建一遍（新的一层，血条全部满血）。
- **`m.hp` 的语义完全照设计稿第 3 节实现了，一个字没改**：它永远是「我现在正在砍的那条」，
  `answer()` 里原来那几处 `m.hp -= dmg` 全部换成 `coopDealDamage(m, n)`——
  这个函数只做两件事：本地先乐观减一下（手感不等网络），COOP 时再把 `n` 和当前打的是
  哪条（`m.curSide`）发给服务器。服务器算出的权威值靠 `hp` 广播校正回来
  （`NET.on("hp", ...)`，不分是不是自己打的这一下都会收到，被动也能保持 `m.hpMate` 新鲜）。
  `m.hpMate` 是**另一条**的缓存值，纯显示 + 判断能不能切换目标，不参与伤害计算。
- **判怪死那一处**（全文件唯一一处 `m.hp <= 0`）分成三条路，照设计稿第 3/6 节：
  ① 我这条空了、队友那条还有血 → `coopTrySwitchTarget()` 直接把 `m.hp` 换成 `m.hpMate`、
  `m.curSide` 翻过去，战斗不结束，继续答题（这就是「转去帮队友砍」）；
  ② 两条这边看着都空了 → **不在本地直接判定**，锁住战斗窗、等服务器广播的 `dead`
  （避免设计稿第 3 节说的那个竞态：两人同时把同一条血条砍成负数，都以为是自己补的那一刀）；
  ③ 单人版（`COOP` 恒 false）完全没变，还是原来那句 `setTimeout(finishBattle, 760)`。
  `dead` 到了之后走 `coopHandleDead(cid)`：如果这只怪正是我在打的那只，等 300ms（让人看清
  最后一题）再走**跟单人版一模一样的** `finishBattle(true)`；如果我压根没在打它（还没走到 /
  队友一个人带走了），直接从 `G.mobs` 摘掉，不结算经验金币——奖励只给「当时真的在这场战斗里」
  的那一边，这跟"经验/金币各自独立"是一个规矩。
- **倒地不是新状态机，是 `finishBattle(false)` 的一个分支**：单人版原来 `!win` 直接
  `gameOver()`；COOP 里改成 `coopGoDown()`——`P.down=true`、`P.hp=0`，不碰 `G.over`
  （这一趟还没结束，队友可能还站着）。两人都倒下才真正结算：每次「我」或「队友」的倒地状态
  变化（自己触发，或收到对方的 `me`/`mate` 心跳）都会检查一遍 `coopCheckBothDown()`，
  两边都 `down` 才各自走 `gameOver()`——**这是各自独立的**，跟"各算各的宝石"一致，
  不是由服务器统一喊停。
- **倒地状态没有走服务器**（设计稿第 1 节列的服务器权威状态里有"两人的倒地状态"，
  实际没做）：图省事直接复用了已经存在的 500ms 心跳（`me`/`mate` 消息）多带一个 `down`
  字段，两边各自本地判断。代价是最多 500ms 的感知延迟（比如队友已经倒了，我这边还要等下一次
  心跳才会显示"队友倒地了"），可以接受——这跟血条不一样，血条必须服务器定序是因为有真实的
  竞态（两人同时打同一条），倒地判断没有这种"谁先谁后决定结果"的竞态，纯本地判断就够。
- **房主倒下会连寻路一起停住**（设计稿没写到这么细）：`autoPath()`/`goTo()` 算路永远是
  照着**房主自己的坐标**算的（`findPath` 用的是模块级的 `P.x/P.y`），队友是照抄这条**绝对坐标**
  的路走。如果房主倒下之后人还固定不动，但寻路的算路循环（`autoTick`）继续跑，就会不断从
  房主这个冻结的坐标重新算路、重新广播——队友会收到一堆从"房主所在地"出发但队友人已经不在
  那儿的路径，越走越错位。所以 `coopGoDown()` 里**房主倒下会额外 `autoOff()` 并广播
  `ready:move on:false`**，两边的寻路一起停，队友只能手动点地图清完剩下的怪
  （手动移动 `tryMove()`/`goTo()` 不看房主是死是活，一直都能用）。**队友倒下没有这个问题**——
  房主还醒着，继续正常算路、正常走，队友这边只是自己不挪（`stepWalk()` 里 `P.down` 直接
  返回，不落地那一步）。
  ⚠️ **这是一个已知的不对称限制**：倒下的如果是房主，寻路团灭；倒下的如果是队友，寻路完全不受影响。
  没有再往下做"房主倒下时把算路的活儿动态交给队友"——那需要队友那边也能独立跑一份
  `autoPath()`/`findPath()`（现在只有房主在跑），改动面已经超出这次的范围，先记在这儿，
  真被用户吐槽再说。
- **战斗窗新增一行「队友那条」**（`#foeMateBar`，`coop.html` 独有，`index.html` 没有这个元素，
  `renderBattleBars()` 里 `$()` 拿到 `null` 会自动跳过）——纯显示，数字来自 `m.hpMate`。
- **联机联调自测**（2026-09-20，Playwright 两个 context 连同一个房间号）：房主正常答对清怪、
  队友故意连续答错，验证了：战斗窗里「队友那条」确实显示出来了（`dualBarSeen`）、
  队友血掉光之后触发倒地、房主日志里看到「队友倒下了」、房主清完那一层之后队友原地复活、
  两边日志都看到了正确的提示文案，全程两边控制台 `pageerror` 为空。单人版
  （`index.html`）用同样的方式回归测试过，`#errbar` 全程隐藏，无报错。

---

## 架构：同一仓库，开关隔离

用户拍板过：**不另开仓库**。8300 行里有 2900 多行是纯数据（`words-a1.js` 1765 行、
`content.js` 623 行、`art.js` 584 行），复制一份就会永久分叉——以后加词、调遗物平衡
要做两遍，漏一次两边就对不上了。所以：

- **`index.html` 一个字都不改** —— 线上 GitHub Pages 永远是现在这个单人版，零风险
- 新增 **`coop.html`**：联机入口，加载同样的 `util.js` / `words-a1.js` / `content.js` /
  `art.js` / `style.css`，多加载一个 `net.js`，并在 `game.js` 之前设 `window.__COOP = true`
- `game.js` 里所有联机代码写成 **`if(COOP){...}`** 分支，`COOP` 在单人版恒为 false，
  **单人的执行路径一行都不能变**
- 新增 **`server.js`**：同时当静态文件服务器和房间服务器。
  用户在电脑上 `node server.js` 启动，手机开 `http://<局域网IP>:8080/coop.html`
- ⚠️ **`server.js` 必须零依赖**（2026-09-18 定）：只用 Node 自带的 `http` / `crypto` / `fs` 等内置模块，
  **自己实现 WebSocket 握手和帧解析**（大概 100 行），**不许 `npm install ws`**。
  理由：用户装完 Node 就能直接跑，不用等下载包、不会碰到国内 npm 超时；
  也跟这个项目「没有构建步骤、没有依赖」的一贯风格一致。仓库里**不要出现
  `package.json` / `node_modules`**
- 词库、遗物配置、美术**只有一份**，以后加词/调平衡改一次两边都生效

**线上和联机是两个入口**：`https://audience20060904-lang.github.io/youxu-huilang/`
永远是单人版，不受影响；联机必须开着电脑。

## 同步模型

- ⚠️ **「服务器权威」这个说法 2026-09-19 修正过，别照字面理解**（见下面「实现设计」第 1 节）：
  服务器是 Node，**没有 DOM，跑不了 `game.js`**，所以它不算游戏逻辑。
  它只维护几个权威数字（每只怪的两条血条、两人的 ready 和倒地状态）并负责**定序**；
  世界由**房主客户端**生成后整包广播；伤害仍然在各自客户端算（要用各自的遗物）
- **每台手机只跑自己那份 `P`** —— `game.js` 里 `P.` 出现 357 次、`G.` 240 次、`B.` 147 次，
  **绝不要在一份代码里塞两份角色状态**，那等于重写。每人看自己的第一视角，
  屏幕上多一条「队友状态条」（血、连击、遗物数、准备状态）
- **世界状态同步直接复用 `writeRun()` 那套序列化**（`game.js` 约 3117 行）：
  地图按行压成字符串、怪只存 defId + 状态，整包几 KB，正好当同步载荷
- ⚠️ **不要试图同步随机种子。** `Math.random()` 在 `game.js` 里直接调了 29 处、
  `util.js` 2 处，加上 38 处 `ri()` / `pick()`。两个人答的题不一样，随机序列立刻漂移。
  **地图和怪由权威端单点生成、整包广播给对方**，这是唯一可靠的做法

## 实现设计（2026-09-19 第二轮构思，读过代码之后）

这一节是**动手前必须看完的**。下面每条都对着 `game.js` 的真实代码核过，不是设想。

### 1. 三方分工（修正了第一轮的「服务器权威」）

第一轮写的「伤害、掉落、世界状态由服务器裁决」**做不到**：服务器是 Node，没有 DOM，
而 `game.js` 里到处是 `$()` 和 `render()`，整个文件在 Node 里跑不起来。正确的分工是：

| 谁 | 负责什么 |
| --- | --- |
| **服务器** | 房间、消息转发、**定序**，外加维护极少量权威数字 |
| **房主客户端** | 世界生成：跑 `genFloor()`，把结果整包广播 |
| **各自客户端** | 自己的 `P`、自己的伤害计算（要用自己的遗物）、自己的渲染 |

**服务器要维护的权威状态少得惊人**，一共只有这些：

- 每只怪的两条血条 `hpA` / `hpB`
- 两人的 ready 状态（寻路 ready / 下一层 ready）
- 两人的倒地状态
- 当前层号 + 世界包的版本号

⚠️ **服务器不 `require` 任何游戏文件**，它连 `content.js` 都不用读。
客户端把**算好的伤害数值**发上来，服务器只做减法、判死、广播。
这样 `server.js` 能保持在几百行，也不会跟游戏平衡的改动纠缠在一起。

### 2. 世界状态的分层：**布局共享，消耗独立**

这是整个联机设计的核心原则，想不清楚这条就会到处打补丁：

| 类别 | 例子 | 怎么同步 |
| --- | --- | --- |
| **共享且必须一致** | 地图、怪的位置、金币位置、阶梯位置、怪的两条血条 | 房主生成 → 整包广播；血条走服务器 |
| **每人独立消耗** | 泉、宝箱、祭坛、游商 | **各开各的**，各自从自己的 `G.things` 里删 |
| **天然就一致** | 金币 | 共享移动 → 两人同时踩到 → 各拿一份 → 各自 splice，结果必然相同 |

- 「每人独立消耗」意味着 A 开过的宝箱在 A 的地图上没了、在 B 的地图上还在。
  **这是有意的**，不是 bug —— 两个人本来就各看各的第一视角，而且遗物就是各抽各的
- ⚠️ 这一条**不影响移动限制的判定**：解除条件只看「怪 + 金币」，而这两样恰好都是必然一致的那一类。
  泉/箱/坛/商不进判定，所以两人的 `G.things` 不完全相同也不会让两边对「能不能走了」产生分歧

### 3. 关键技巧：`m.hp` 永远是「我眼前这条血条」

怪要有两条独立血条，最笨的改法是把 `m.hp` 换成 `m.hpA / m.hpB`，然后把 `game.js` 里
每一处读写 `m.hp` 的地方都改一遍。**不要这么做。** 正确的做法是：

> **每个客户端本地的 `m.hp` 始终指向「我现在正在砍的那条」**，另一条存在 `m.hpMate` 上只供显示。

这样 `answer()` 里那行 `m.hp -= dmg + extra`、盲斗的 `m.hp = 0`、回响之厅的 `m.hp -= hall`
**一个字都不用改**。只需要在三个点上挂钩：

1. **砍完之后**：把这次的伤害数值上报服务器（`dmg` 消息），服务器扣权威血条并广播
2. **`m.hp <= 0` 那一处判定**（`game.js` 约 1833 行，全文件**只有这一处**判怪死）：
   联机模式下要先问一句「对方那条还有血吗」——
   有就**切换目标**（`m.hp = m.hpMate`，继续答题，这就是「转去帮队友砍」），
   没有才走 `finishBattle(true)`
3. **收到对方扣血的广播**：更新 `m.hpMate` 并重画血条

⚠️ **唯一有竞态的地方就是「帮打」**：A 帮砍 B 那条时，A 和 B 可能同时把它砍到 0，
两边都以为是自己补的最后一刀。所以**所有伤害都走服务器定序**（自己那条也走，保持一条路），
局域网往返 1~5ms，手感上察觉不到。本地可以先乐观扣血、收到权威值再校正。

### 4. 只有 **3 件**遗物会碰世界状态

把 `genFloor()` / `nextFloor()` 里所有 `hasRelic()` 调用逐个核过了，115 件里只有这三件
会改变**共享的**世界，其余（油灯回血、钱袋给钱、盾誓、厚盾…）全是各自结算，不用管：

| 遗物 | 做什么 | 冲突点 | 处理 |
| --- | --- | --- | --- |
| **掘金 `dig`** | `genFloor()` 里金币堆 +3 | 只有一个人有时算不算 | **任一人有就生效**（合作模式好处共享，金币本来两人各拿一份） |
| **水 `water`** | `drinkAll()` 喝光**这一层所有的泉** | 泉是共享布局 | 按第 2 节：**泉的消耗是每人独立的**，A 的「水」只喝光 A 那份，B 的泉还在 |
| **盲斗 `blind`** | `nextFloor()` 里一口气跳 `BLIND_STEP`(4) 层 | 世界级，会把另一个人一起带走 | **联机里不跳层**，只保留「一击必杀」（见第 8 节） |

⚠️ 以后**新增任何遗物，都要回来问一句「它碰不碰共享世界」** —— 碰的话就得在这张表上加一行。

### 5. `game.js` 的钩子点清单

联机代码全部写成 `if(COOP){...}`，单人路径一行不变。按要改的地方从上到下：

| 函数（行号是 2026-09-19 的） | 要加什么 |
| --- | --- |
| `newRun()` 64 | 房主生成世界；非房主等世界包到了再铺场 |
| `nextFloor()` 140 | 双确认才进；盲斗在联机里不跳层（第 8 节）；`genFloor()` 只有房主跑 |
| `genFloor()` 234 | **完全不碰 DOM，可以原样复用** —— 只有房主跑，跑完把 `G` 整包广播 |
| `autoToggle()` 854 | 不再直接开，改成**上报「我 ready 了」**，等服务器的 `go` |
| `autoTick()` 838 | 移动锁阶段：只有收到 `go` 才走；160ms 轮询本身照旧 |
| `autoPath()` 810 | ⚠️ **只有房主算目标**，算完广播路径；否则两人各算各的会分叉 |
| `tryMove()` 875 / `#map` 的 click | 移动锁阶段 + 寻路开着时禁用手动（用户要求） |
| `onEnter()` 894 | 金币各拿一份（天然一致）；泉/箱/坛/商各自独立消耗 |
| `startBattle()` 971 | 两人同时进；初始化 `m.hp` / `m.hpMate` 两条血条 |
| `answer()` 1490 | 扣完血上报服务器（`m.hp` 本身不用改，见第 3 节） |
| 判怪死 1833 | **全文件唯一一处** `m.hp <= 0` —— 加「切换目标 or 真死」的分岔 |
| `P.hp <= 0` 三处 1179 / 1834 / 1993 | 联机模式下走**倒地**，不是 `gameOver()` |
| `finishBattle()` 1997 | 怪死是共识事件，等服务器的 `dead` 再收 |
| `closeBattleWin()` 2017 | 经验/金币/回血各自独立；`G.stair` 改写位置天然一致（怪死在哪是共识） |
| `takeHit()` 1937 | 各自独立，不用同步；只是血见底时转倒地 |
| `deathSave()` 1844 | 薪火/回魂都用完了 → 倒地而不是结束 |
| `endRun()` 3249 | **两人都倒下**才结算；各算各的宝石 |
| `flee()` 2068 | **联机里整个禁用**，战斗窗的「撤退」按钮隐藏（见第 8 节） |
| `render()` 577 | 画两个棋子（见第 7 节） |
| `renderBattleBars()` 1000 附近 | 画两条怪血条 + 队友状态条 |
| `commit()` 3110 | 各自存各自的，**localStorage 的键一个都不用新增**；重连时以房主广播的世界为准（第 10 节） |
| `enterRoute()` 3071 | 选路线由房主定，两人到齐才一起进（第 10 节） |
| `offerRelics()` 2792 + 宝箱/祭坛/游商 | 联机里把「脱壳」滤出掉落池（第 9 节） |

### 6. 消息协议草案（`server.js` ↔ `net.js` 的契约）

JSON 文本帧，每条带一个 `t`（type）。**先照这张表实现，不够了再加，别临时发明字段。**

**客户端 → 服务器**

| `t` | 带什么 | 谁发 | 什么时候 |
| --- | --- | --- | --- |
| `join` | `room`, `name` | 两人 | 进页面时 |
| `world` | `floor`, `pack`（`writeRun()` 那套序列化） | **只有房主** | 每次 `genFloor()` 跑完 |
| `ready` | `what:"move"\|"floor"`, `on:bool` | 两人 | 点寻路 / 点下一层 |
| `path` | `steps:[{x,y}...]` | **只有房主** | `autoPath()` 算出目标之后 |
| `dmg` | `mob`（**cid**，不是数组下标）, `side:"a"\|"b"`, `n` | 两人 | 砍完一刀（`side` 指砍的是谁那条血条） |
| `me` | `hp`, `maxHp`, `combo`, `relics`, `down` | 两人 | 状态一变（给队友状态条用） |
| `used` | `kind`, `x`, `y` | 两人 | 用掉了泉/箱/坛/商（**只是通知对方，不改对方的世界**） |

**服务器 → 客户端**

| `t` | 带什么 | 意思 |
| --- | --- | --- |
| `joined` | `host:bool`, `mate:bool` | 你是不是房主、对方在不在 |
| `world` | 原样转发 | 非房主收到就铺场 |
| `go` | `what:"move"\|"floor"` | **两人都 ready 了**，可以走 / 可以下一层 |
| `path` | 原样转发 | 非房主照着这条路走 |
| `hp` | `mob`（cid）, `a`, `b` | **权威血量**，客户端照这个校正 |
| `dead` | `mob`（cid） | 这只怪两条血条都空了，收掉它 |
| `mate` | 对方那条 `me` | 刷队友状态条 |
| `pause` | `why:"disconnect"` | 对方掉线，整局暂停 |
| `resume` | — | 对方回来了 |

⚠️ 几条要点：
- **`dmg` 只发「伤害数值」，不发血量** —— 血量是服务器算的，客户端算血量就会各说各话
- **`world` 只有房主发**，服务器不校验内容，只管转发和记版本号
- `used` 是**纯通知**（让对方的日志里能看到「队友开了宝箱」），**收到它不要去改自己的 `G.things`**
  —— 消耗是独立的，见第 2 节
- 断线检测靠 WebSocket 的 close 事件 + 30 秒心跳，两条都要有：
  手机锁屏时连接常常不会干净地关掉

### 7. 两个棋子：**逻辑同格，渲染错开**

用户要「地图上画两个棋子，一前一后走同一条路」。实现上**不要**让两个人真的占不同格子 ——
`autoPath()` 里的 `roomOf(P.x, P.y)` 和 `nearestBy()` 都按位置算，位置一差目标就可能不同，
路线立刻分叉。正确做法：

> **两人逻辑上共享同一个 `P.x / P.y`**，渲染时把自己画在格子偏左、队友画在偏右并缩小一号。
> 纯视觉偏移，不占格子、不进寻路。

这样 `findPath` / `goTo` / `stepWalk` / `onEnter` **一个字都不用改**。

### 8. 第二轮定下的三条规则（用户 2026-09-19 确认）

- **盲斗在联机里不跳层**：只保留「答对一击必杀」，去掉 `BLIND_STEP`(4) 层的跳跃。
  跳层会把整层的怪和金币一起跳过，在共享世界里怎么分都不公平；一击必杀本身已经够强。
  实现：`nextFloor()` 开头那句 `hasRelic("blind") ? BLIND_STEP : 1` 加一个 `!COOP &&` 的条件即可
- **联机里禁用撤退**：碰上了就必须打完。`flee()` 整个走不到，战斗窗里的「撤退」按钮直接隐藏
- **房主 = 先连上的那台手机**：电脑只跑服务器、不开游戏页面。
  ⚠️ **房主掉线时服务器要让另一个人自动接管**（发一条 `joined` 更新 `host:true`），
  否则世界就没人生成了，整局卡死

### 9. 禁用撤退的连带影响：**「脱壳」要从掉落池里剔除**

把所有跟 `flee()` 相关的遗物核了一遍，受影响的**只有一件**：

- **脱壳 `shed`**（史诗，「每层第一次撤退不掉血」）—— 撤退都没了，它是**纯废牌**。
  联机模式下要把它从掉落池里滤掉：三选一 `offerRelics()`、宝箱、祭坛、游商货架，
  四个出货口都要过滤，别让玩家白高兴一次抽到史诗
- ⚠️ 这是一条**通用规矩**：以后凡是在联机里改动或禁用某个系统，都要回头问一句
  「有没有遗物是专门吃这个系统的」。`content.js` 里按 `pw`（效果文案）全文搜一遍最快

### 10. 进洞之前也要同步（第二轮发现的遗漏）

前两轮的设计全在讲「进了地牢之后怎么办」，**漏了进洞这一步**。`enterRoute(id)`
（`game.js` 约 3071 行）是进洞的唯一入口，它 `setChapter(r.ch)` 决定这一趟是哪一章
—— 两个人要是选了不同的路，世界从第一秒就是两个，后面全白搭。所以：

- **选哪条路（哪一章）由房主定**，另一台手机上的洞窟卡片只显示、不可点，
  写一行「等房主选路线」
- **练习模式也由房主定**（第一轮就定了「两人一致」，这里补上实现位置）：
  `practiceOn` 是模块级变量，非房主那台要禁用 `#btnPractice` 并跟着房主的值刷新
- **`enterRoute()` 要等两人都到齐**再一起进，不能房主点了就自己先走

**续玩档怎么办**：照常各写各的（`commit()` 一个字不用改），但
⚠️ **重连或重开页面时，一律由房主重新广播一次世界包，非房主用它覆盖自己的 `G`**。
不要试图让两边的续玩档「自己对上」—— 只要有一次不一致，后面全是错位，
而重新广播一次只花几 KB。

## 玩法规则（用户 2026-09-18 逐条确认，别再重新讨论）

**角色完全独立**：遗物、金币、等级、血、连击、心魔、`P.used`、出题全部各是各的。
两人的熟练度不同，各抽各的题。

### 移动限制

⚠️ **2026-09-20 又续改窄了一次**：解除条件从「怪清完 `且` 金币全捡完」改成**只看怪**——
金币是两人各自生成、各自捡的（`onEnter()` 里各自 `splice` 自己的 `G.things`，不共享一份计数），
拿它当解锁条件的话，一个人手快先把地上那几堆都捡了，另一个人还没碰到，这一层反而先解锁了，
条件本身就说不清楚。现在是：

- **解除条件 = 这一层的怪清完，不再管金币**（`coopLocked()` = `COOP && G.mobs.length > 0`）
- **解除之前：手动移动整个禁用**（不是"建议用寻路"，是方向键 / 点地图**直接不生效**，
  `tryMove()`/`#map` 的 click 里挡死，只提示一句「这一层的怪还没清完」，1.5 秒节流别刷屏）——
  唯一能动的办法就是**两人都点寻路**，走同一条共享路线、打同一只怪
- **寻路只认怪，不认金币**（`autoPath()` 里 `coopLocked()` 时目标只挑 `G.mobs`，
  金币过滤掉）——顺路踩过的金币照样白捡（`onEnter()` 没变），只是不会特地为了一堆金币绕路，
  也不会在还有怪的时候把两人的路带岔
- 任一人再点一次寻路 = 两人一起停下（没变）
- **怪一清完，移动锁立刻整个解除**（`coopUnlockMove()`，两边各自检测到"我的 G.mobs 空了"
  都会调用一次）：寻路自动停一下、还给玩家自己决定接下来干嘛，manual 移动同时解禁
- **解除之后两人完全独立**，不再有"一前一后走同一条路"这回事——各自决定要不要用寻路
  （找的是**各自的**金币，因为地上剩下的金币两人捡去的份数早就不一样了）、要不要手动逛、
  要不要用泉 / 箱 / 坛 / 商，最后双确认下楼
- 所以一层的节奏还是三段：**同步清怪（手动锁死，只能走共享寻路）→ 各自独立行动 → 双确认下楼**
- **清怪阶段**地图上画**两个棋子**，一前一后走同一条路；**解锁之后**队友棋子就是它本来的样子——
  按队友最后一次上报的坐标画（`coopMateX/Y`），不再跟自己捆在同一条路上
- **一场战斗打完、这一层还没清完的话，寻路状态保留、自动接着走下一只怪**
  （一层十几只怪，每只都要两人重新点太烦）——这条只在**清怪阶段内部**有效，跟"清完之后
  还继不继续走"是两回事
- 每下一层仍然要重新手动开（现有规矩，`nextFloor()` 里的 `autoOff()`；新一层怪没清完，
  `coopLocked()` 又是 true，移动锁自动重新扣上）
- ⚠️ **`coopUnlockMove()` 里那句 `NET.send({t:"ready",what:"move",on:false})` 是幂等地发**，
  不管 `autoOn` 现在是不是已经是 `false` 都发一次——`server.js` 的 `room.ready.move` 是
  常驻状态、不会自动复位（"floor"/"enter" 这类一次性事件触发后会自动清空，但"move"不会，
  见"实现设计"第 6 节的协议要点）。踩过一次：如果只有一边真的发了这条 off，下一层"两人都点
  寻路才走"的判定会被这一层残留的 `[true,true]` 提前凑成 `go`，变成"其中一个人一点寻路，
  两人就直接一起走了，另一个人根本没点"。两边各自检测到清完都调用一次 `coopUnlockMove()`，
  这条才能真的清干净

### 战斗

- **两人同时各答各的题**，互不等待。各自的连击、遗物、暴击、7 秒读条都独立
- 怪的反击各打各的（A 答错 A 掉血）
- **怪有两条独立血条，每条都是原血量的满血**（实际总血量 ×2，但两人同时砍，
  单人体感节奏不变）。`m.hp` 要从单值改成两条
- **A 把自己那条清空后，可以转去帮 B 砍 B 那条**。强的人能带弱的人
- 两条都清空，怪才死
- **联机里禁用撤退**（用户 2026-09-19）：碰上了就必须打完，战斗窗里没有「撤退」按钮
- **战斗窗里两条队友血条，都是半透明 50%**（用户 2026-09-20）：
  ① 怪血条 `.fbar` 下面再画一条 `#foeMateBar`——队友正在打的那条（`m.hpMate`）；
  ② 自己血条 `#bHpBar` 上面再画一条 `#bMateHpBar`——队友本人剩多少血（数据是
  `coopMateInfo.hp/maxHp`，跟顶栏那条「队友状态条」`#mateRow` 同一份心跳，最多 500ms 延迟）。
  两条都只在 `coop.html` 里有对应元素（`index.html` 没有，`$()` 拿 `null` 就跳过），
  纯显示，不参与伤害计算。⚠️ `#bMateHpBar` **不走 `paintHp()`**——那个函数会顺手把
  `P.shield`（自己的护盾）画上去，套在队友那条血条上会显示错的盾数，所以专门写了
  `paintMateHp()`，没有护盾层、没有掉血/回血的闪光动效。半透明是在 CSS 里用 id 选择器
  （`#bMateHpBar`/`.fbar.mate`）写 `opacity:.5`，不是加一个新 class——`paintHp()` 里
  `bar.className = "hpbar" + ...` 是整块重写，加在 class 上的自定义类每次刷新都会被冲掉。

### 倒地

- 血掉光**不出局**：停止答题，怪也不再打他
- 队友把这一层的怪清完，他原地复活（回 1 点血）
- **两人同时倒下才算这趟结束**
- **倒地的人不阻塞移动** —— 视为自动同意，队友一个人点寻路就能走
- ⚠️ **A 倒地后不做任何难度缓解**：怪的两条血条都还在，B 必须独自砍完。
  这是用户明确选的硬核档（已经提醒过后果，用户坚持），**不要自作主张减半或让血条消失**。
  实战下来觉得劝退的话由用户来提，改起来是一行的事

### 局内资源

- 地上一堆金币，**两人各拿一份**（不是抢，合作模式不该为捡钱吵架）
- 怪掉遗物，**两人各自抽各自的三选一**
- 泉 / 宝箱 / 祭坛 / 游商：走到那一格后各自决定用不用，互不阻塞，各花各的金币

### 结算

- 各算各的宝石，存进各自手机的 localStorage。熟练度、图鉴、统计也各存各的

### 练习模式

- 局前由房主统一定，两人一致（它直接改难度平衡，只有一个人开会很怪）

### 断线

- 谁掉线就整局暂停，屏幕上写「等待队友重连」，重连回来接着走
- 最坏情况退回本层开头（续玩档本来就存「刚踏进这一层的样子」，这条规矩不变）

## 用户的运行环境

- **用户用 Windows**（2026-09-18 确认）。装 Node 的步骤已经给过他了：
  nodejs.org 下 LTS 的 .msi，一路 Next，「Tools for Native Modules」那个复选框保持不勾
- 查局域网 IP 用 `ipconfig` 看「IPv4 地址」
- ⚠️ **第一次跑 `node server.js` 会弹 Windows 安全中心警报，必须勾「专用网络」再允许访问**，
  不勾手机连不上 —— 这是最常见的坑
- **用户电脑上那份代码最早是用网页上的「Download ZIP」下载的**（2026-09-20 问出来的），
  跟仓库没有关联，Claude 这边改完推到 `main`，用户电脑上那份不会跟着变，
  每次都要重新整个下载很麻烦。**已经引导用户改成 `git clone`**，见下面「怎么自动更新」。

### 怎么自动更新到用户电脑（2026-09-20 加）

- **单人版网站不用管**：推到 `main` 之后 GitHub Pages 自动重新部署，用户打开网页看到的
  永远是最新版，这条路径本来就是自动的。
- **联机版是另一回事**：`node server.js` 跑的是**用户电脑本地磁盘上**的一份代码，
  跟 GitHub 仓库是两份独立的东西 —— 仓库更新了，本地这份不会自动跟着变。
  真正做到"自动"的前提是**用户电脑上那份必须是 `git clone` 下来的**（不能是解压的 ZIP，
  ZIP 没法 `git pull`）。
- **新增了 `start-coop.bat`**（仓库根目录，2026-09-20 加，不上线 —— 只在用户本地跑，
  跟 `server.js`/`check-words.py` 一个待遇）：双击就会先 `git pull` 拉一遍最新代码，
  再 `node server.js` 启动服务器；`git pull` 失败（没装 Git、没联网、这个文件夹不是
  git clone 建的）就打印一句提示，然后照样用本地现有代码启动，不会卡死。
  ⚠️ **用了 `chcp 65001` 切到 UTF-8 控制台**，不然中文提示在默认 GBK 代码页下会变成乱码。
  ⚠️ 末尾有 `pause` —— `node server.js` 万一直接崩了，窗口不会一闪而过看不清错误。
- **以后用户要联机，标准流程是**：进这个 git clone 出来的文件夹，双击 `start-coop.bat`。
  它会自动确保是最新代码，不用再手动 `git pull` 或重新下载 ZIP。
- ⚠️ 这条只解决"联机服务器代码是不是最新"，**不会自动帮用户重新触发那两步防火墙授权**——
  防火墙那个警报只在 Windows 认为"这是一个新版本的程序"时才可能再弹一次，正常改代码不动
  `server.js` 监听的端口/协议的话，一般不会重新弹。

### 怎么跑起来（第一期，2026-09-20）

**电脑上**（只有电脑跑服务器，手机不用装任何东西）：

1. 打开这个仓库所在的文件夹（用文件资源管理器地址栏输入 `cmd` 回车，或者用 PowerShell
   `cd` 过去），确认目录里能看到 `server.js`
2. **双击 `start-coop.bat`**（推荐，会自动先拉最新代码再启动 —— 前提是这个文件夹是
   `git clone` 出来的，见上面「怎么自动更新」），或者手动跑 `node server.js`。
   黑窗口里会打出两行地址，形如：
   ```
   电脑上打开： http://localhost:8080/coop.html
   手机（同一个 WiFi）打开：
     http://192.168.x.x:8080/coop.html
   ```
   第一次跑，Windows 安全中心会弹一个「Windows Defender 防火墙已阻止此应用的某些功能」——
   **勾上「专用网络」，点「允许访问」**（公用网络不用勾，勾了也没坏处）。不允许的话手机连不上。
3. 这个黑窗口**不能关**，关了服务器就没了（两人都会掉线）。想停止就在窗口里按 `Ctrl+C`。

**两台手机（或一台手机一台电脑）**（2026-09-20 又续：房间号改数字 + 邀请链接）：

1. 确认两台设备跟**跑服务器的那台电脑**连的是**同一个 WiFi**（手机流量、访客网络不行）
2. 浏览器打开黑窗口里打出的那个 `http://192.168.x.x:8080/coop.html` 地址（照抄，不要打 https）
   —— 打开就是正常的主城画面，**不用先组队**，图鉴、设置都能先逛
3. 一个人点主城里的**「组队」**（在「洞窟」下面），点**「创建房间」**——会自动给一个
   **4 位数字房间号**（比如 `6148`），界面上会显示出来
4. 另一个人有两种加入方式：
   - **报数字**：也点「组队」→「加入房间」，把那 4 位数字输进去（手机上会自动弹数字键盘）
   - **发链接**（更省事）：创建房间那边点**「复制邀请链接」**，把链接发给对方（微信、短信都行），
     对方**直接打开那个链接就自动加入了**，不用再点任何按钮、也不用手输数字
5. 两边都连上之后，「组队」那一块的副标题会变成「房间 6148 · 房主/队友 · 已连接」
6. **先创建房间的是房主**：点「洞窟」能选去哪一章；队友点「洞窟」只能看，写着「等待房主选路线」
7. **房主选一条路线**（点一下），这时候还**不会**进去——卡片会高亮、下面写「等队友确认」。
   房主反悔了可以重新点别的路线，会覆盖掉上一次的选择
8. **队友点「确认，一起下去」**，两边才真的一起进洞
9. 清怪捡金阶段要**两人都点「寻路」**才会一起走；下楼前要**两人都点「下去」**才会真的下去
10. 如果中途有人掉线（切后台太久、信号不好），屏幕上会显示「等待队友…」，
    服务器还开着的话重新打开页面、走一遍第 3~4 步（用同一个房间号，或者再点一次那条邀请链接）
    就能接回来

**没有公网、不能跨 WiFi** —— 这是局域网方案的性质决定的，不是 bug。真要跨网络玩，
以后可能要考虑内网穿透之类的东西，但**用户没提这个需求，先不做**。

## 验收

- **单人版必须完全不变**：用 Playwright（Chromium 在 `/opt/pw-browsers`，
  **别跑 `playwright install`**）打开 `index.html`，跑一遍进洞 → 走路 → 打怪 → 下楼，
  确认和改动前一模一样。**2026-09-20 跑过**：进洞/寻路/战斗/答题/翻标签页/练习模式开关，
  `#errbar` 全程没出现、`pageerror` 全程是空数组
- 联机：本机开两个浏览器窗口连同一个房间，走完一整层。**2026-09-20 跑过部分**：
  两个 Playwright context 连同一个房间号，验证了房主/队友分配、路线选择的房主独占、
  移动锁的双人 ready、世界同步、独立战斗不报错、队友状态条。
  **没有自动验证到"清完一层→下楼双确认→进第二层"这一段**（自动化脚本乱点选项，
  正确率只有随机水平，角色会先倒下）—— 这一步交给用户在真机上实际玩一遍确认
- ⚠️ 沙盒访问不了 `*.github.io`，本地自测用 `python3 -m http.server`（联机的静态文件服务
  现在由 `server.js` 自己提供，不用再单独起 `http.server`）

## 踩过的坑

- **strict mode 下，`if(COOP){ function foo(){...} }` 这种写在 block 里的函数声明是块作用域的**，
  外面（比如定义在文件更前面的按钮 click 监听）调不到，会直接 `ReferenceError`。
  `coopToggleMove` / `coopConfirmStair` / `coopMateResume` / `coopUpdateWaitUi` 这几个
  一开始写在 `if(COOP){...}` 里面，被 `$("btnPathfind")`/`$("btnStairGo")` 的 click 监听调用时
  报错——后来挪到了 `if(COOP)` 外面、跟其它函数一样是顶层声明（顶层的 function 声明会整体提升，
  文本位置在调用点之后也没关系）。**以后往这个文件加新的联机专用函数，记住这条**：
  只有 `NET.on(...)` 的注册、`$("btnRoomJoin")` 这类只在 `if(COOP)` 分支里才用得到、
  也只在这个分支里被调用的代码，才能留在 `if(COOP){...}` 里面。
- **自测脚本的 `.relic` 选择器踩过一次坑**：页面上不止 `#relicList` 里有 `.relic`（`#swapNew`
  也是 `class="relic hot"`），用 `page.$(".relic")` 会抓到 DOM 里第一个匹配、但当前隐藏着的
  那个，点了没反应，弹窗永远关不掉，脚本卡死。这是**测试脚本的坑，不是游戏的 bug**，
  写起来记一笔免得下次又踩：以后写自动化测试要用 `#relicList .relic` 这种带上下文的选择器。
- 局域网是 `http://` 不是 `https://`。localStorage 和 WebSocket 都正常
  （混合内容限制只在 https 页面上），但**语音朗读在 iOS Safari 的非加密页面上可能不出声**。
  先做，真出问题再考虑自签证书（代价是手机上要点一次「继续访问」）
- `.github/workflows/deploy.yml` 的 `cp` 用了 `*.js` 通配，新增的 js（`net.js`）会自动上线；
  `coop.html` 是新的 html，**没有被那份 workflow 的 `cp` 捞到**，已经把它加进列表了
  （见下面「上线规矩」那条）。`server.js` 发到 Pages 上无害（没人执行它），不用特意排除。
  **这个 `.md` 文件不上线**，跟 `遗物数据表.md` / `check-words.py` 一样

## 上线规矩

照 `CLAUDE.md` 第 5 条：改完自测过就自动提交 → 推 `main` → 用 Actions API 确认部署 success。
**这次的例外**：如果某一步动了 localStorage 的键或存档格式，按第 5 条的规定**要先问用户**。

---

## 变更记录

**写在最上面，新的在前。每做完一步就加一行。**

- **2026-09-20（再续）** · 用户提了两条：① 战斗窗里加两条半透明的队友血条（自己血条上方一条、
  怪血条下方一条，都是 50% 透明度）；② 移动锁改窄——寻路阶段只认怪，不理会金币（两人金币
  各自生成各自捡，混进去当目标/当解锁条件都说不清楚），怪没清完之前双方**手动移动整个禁用**，
  怪一清完**立刻解除所有限制**，包括"队友棋子跟着走同一条路"这件事也一起结束——
  从此各自独立找各自的金币，不再互相跟随。做了这几件事：
  1. **`coopLocked()`**（`COOP && G.mobs.length > 0`）成了移动锁的唯一判据，取代了原来
     "怪 `且` 金币都清完"的双重条件。
  2. **`tryMove()`** 和 `#map` 的 click 监听都在最前面挡了一句 `if(coopLocked()){ coopNoteLocked(); return; }`——
     以前手动移动只是"点了就关掉寻路，两件事各走各的"，现在是清怪阶段**真的点不动**，
     `coopNoteLocked()` 节流 1.5 秒提示一句「这一层的怪还没清完」，别让人以为按键失灵。
  3. **`autoPath()`** 的目标列表在 `coopLocked()` 时只有 `G.mobs`，金币被过滤掉——避免清怪阶段
     的共享路线被顺路的一堆金币带偏，也避免两人因为"金币各自的剩余不一样"而路线分叉。
  4. **`autoTick()`** 里原来写死的 `COOP && !coopIsHost()`／`COOP && coopMateBusy` 两个门槛
     全部换成 `coopLocked() && ...`——**怪清完之后，队友也能自己跑 `autoTick()`**（原来这个
     循环从头到尾只有房主在跑，队友只会照抄广播的 `path`）。`goTo()` 里那句广播 `path`
     消息也加了同一个 `coopLocked()` 条件，解锁之后各自的路不再发给对方。
  5. **新增 `coopUnlockMove()`**：在 `closeBattleWin()` 和 `coopHandleDead()` 判定
     `G.mobs.length === 0` 的两处各调用一次（两边独立检测到"我的怪清完了"都会调），
     负责把这一层的移动锁彻底收尾——寻路停一下、`ready:{move,off}` 发给服务器、手动移动的
     禁用跟着解除。⚠️ 这条**必须两边都发一次 off**，因为 `server.js` 的 `room.ready.move`
     是常驻状态、不会像"floor"/"enter"那类一次性事件一样自动复位——只有一边发的话，
     下一层"两人都点寻路才走"的判定会被这一层残留的 `[true,true]` 提前凑成 `go`，
     变成"随便一个人点一下，两人就一起走了"，详见「移动限制」那节的踩坑记录。
  6. **`coopToggleMove()`** 分岔：`coopLocked()` 时走原来那套 ready/go 握手；解锁之后
     直接调 `autoToggle()`（跟单人版一模一样），不用再等队友、不用走服务器。
  7. **战斗窗新增两条队友血条**：怪血条下面的 `#foeMateBar`（从纯文字改成了真的 `.fbar` 条，
     配 `#foeMateFill`）显示队友正在砍的那条 `m.hpMate`；自己血条上面新增 `#bMateHpBar`
     显示队友本人的血（数据来自 `coopMateInfo`，也就是顶栏「队友状态条」用的那份心跳）。
     两条都是 CSS 里用 id 选择器写 `opacity:.5`（`.fbar.mate` 那条例外，是给以后万一有别的
     地方也想用同一个视觉效果留的类）。⚠️ **新写了 `paintMateHp()`**，`#bMateHpBar`
     **没有走 `paintHp()`**——那个函数会顺手把 `P.shield`（自己的护盾）画到传进去的血条上，
     队友那条要是也走它就会显示我自己的盾数，是错的；也因为 `paintHp()` 里
     `bar.className = "hpbar" + ...` 整块重写 className，如果借用它，"mate" 这个类每次
     刷新都会被冲掉，透明度就会跳回 100%（这个坑在写的时候发现了，改成 CSS 用 id 选择器
     而不是叠 class 解决的，没有真的踩进去）。`NET.on("mate", ...)` 里也加了一句：
     战斗窗开着就顺带 `renderBattleBars()`，不然队友那条血条只能等你自己下次答题才刷新。
  Playwright 两个 context 测过：清怪前两边的方向键和点地图都被挡住（日志出现"还没清完"提示）、
  两人都点寻路后一起走向同一只怪并同时进入战斗、两条队友血条都正确显示且透明度读出来正好是
  `0.5`，全程 `pageerror` 为空。单人版（`index.html`）重新跑过回归（进洞/寻路），无报错——
  这次的改动全部包在 `if(COOP)`/`coopLocked()` 判断里，`index.html` 的执行路径没有一行变化。
  **没有自动测到"清完一层之后两人真的各走各的"**——原因跟之前几次一样：自动化脚本乱点选项，
  正确率太低清不完一整层的怪，这一步照旧交给用户真机实测确认。

- **2026-09-20（又续）** · 用户问"怎么自动更新到我电脑"——一问才发现用户电脑上那份代码是
  网页上「Download ZIP」下的，跟仓库没关联，Claude 这边改完推到 `main`，用户那份不会跟着变。
  引导用户改用 `git clone`，并新增 `start-coop.bat`（不上线）：双击自动 `git pull` 再
  `node server.js`，以后不用手动敲命令。这条经验记一笔：**联机是跑在用户本地的东西，
  跟"改完推 main 就自动上线"那套单人版的假设不一样** —— 早点问清楚用户本地代码怎么来的，
  别假设它跟仓库天然同步。

- **2026-09-20（第二期）** · **双血条战斗代码写完了**：`m.hp` 语义照设计稿第 3 节实现，
  一个字没改（永远是"我现在正在砍的那条"）；怪身上新增 `cid`（固定身份，见下面"跟原计划的
  出入"）、`hpMate`（队友那条的缓存）、`curSide`；`answer()` 里所有 `m.hp -=` 换成
  `coopDealDamage()`（本地乐观扣血 + 上报服务器）；判怪死那一处分三路（转去帮砍 / 等服务器
  `dead` 确认 / 单人版不变）；`finishBattle(false)` 在 COOP 里走 `coopGoDown()`（倒地，不结束
  这一趟）；两人都倒下才 `gameOver()`；队友清完层自动复活。`server.js` 新增 `room.mobs`
  （cid → {a,b,dead}，每次收到 `world` 重建）和 `dmg` 消息的处理（服务器定序、判两条都空、
  广播 `hp`/`dead`）。`coop.html` 战斗窗新增「队友那条」一行显示。
  `index.html` 一个字没改。单人版和联机版都用 Playwright 回归测试过（联机版两个 context
  连同一个房间，房主正常打、队友故意连续答错触发倒地→复活整个链路），全程两边控制台无报错，
  细节和踩过的坑见上面"跟原计划的出入（第二期）"那一段。
  **还没做的**：真人在两台真机上实际联机测试（跟第一期一样，需要用户帮忙）；
  房主倒下时寻路团灭这个已知限制（见"跟原计划的出入"）没有进一步处理。

- **2026-09-20（又续）** · 用户又提了几点：房间号改数字、能不能开两个窗口本机测、
  有没有更简单的联机方式、还有什么没做完。做了三件事：
  1. **房间号改成纯数字**（`randRoomCode()`，4 位，1000~9999，不带前导 0）——
     以前是字母+数字混的（去掉 0/O/1/I 那种容易看混的），现在纯数字，手机上弹数字键盘更好按，
     报数字给队友念也方便。`#roomCode` 输入框加了 `inputmode="numeric" pattern="[0-9]*"`。
  2. **加了邀请链接**：创建房间之后界面上会显示 `coop.html?room=6148` 这样一个链接
     （`coopInviteLink()`），旁边有个「复制邀请链接」按钮。队友**直接打开这个链接就自动加入**
     （见文件末尾的 `autoJoinFromUrl()`：页面一加载检查 URL 的 `room` 参数，有就直接
     `NET.connect()`），不用再手输房间号。
     ⚠️ **复制按钮做了降级**：局域网 `http://` 不是"安全上下文"，`navigator.clipboard`
     在有的浏览器里根本不存在（不是权限问题，是 API 压根拿不到）——`#teamLinkText` 用一个
     只读输入框显示链接，点击会自动全选，复制失败时按钮文字提示"已选中，手动复制"，
     跟设置页「复制导出码」那个按钮是同一个降级套路。
  3. **补上了协议表里早就写了但没人真发的 `used` 消息**：`onEnter()` 踩到泉/坛/箱/商时会
     `coopNoteUsed("泉"|"坛"|"箱"|"商")`，队友那边收到 `NET.on("used",...)` 会在日志里写一句
     「队友用了泉」。⚠️ 字段名跟最早那张协议草案表不完全一样（草案写的是 `kind`，实际实现用的
     是 `what`，值也从英文 kind 改成了直接给中文短词）——**协议表那几行是 2026-09-19 定的草案，
     没有跟着每次实现更新，读的时候留意"这是最初设计"还是"这是最终实现"**（第一期完工那次的
     变更记录里已经提过一次同样的事，这次索性再强调一遍：草案表本身不会再逐字改了，
     以变更记录 + 代码为准）。
  两个"能不能" ——**本机开两个窗口测试可以**，但同一浏览器的两个普通窗口会共用同一份
  `localStorage`（词汇熟练度/宝石/续玩档都会互相覆盖），建议用「一个普通窗口 + 一个无痕窗口」
  或者两个不同的浏览器；**"更简单的方法"** 目前就是上面这个邀请链接，二维码没做——
  手搓一个不依赖任何库的二维码编码器有风险（Reed-Solomon 纠错码那部分很容易写错，
  而这个沙盒环境没有摄像头，生成出来的码**没法实测扫描**），真要加的话建议要么让用户确认
  接受「链接可能要手动分享，不扫码」，要么接受引入一个现成的二维码库（CDN 或者本地 vendor
  一份），目前没做是因为两者都还没问过用户，**先记在待办里**。
  Playwright 测过：数字房间号生成正确、邀请链接直接打开能自动加入且双方状态互相同步、
  复制按钮不报错、`used` 消息端到端能送到（用 `NET.send` 直接触发验证的，真实走到泉/坛/箱/商
  格子那条路径没有在自动化里触发到——地图生成随机，脚本没恰好走到那种格子，逻辑本身
  跟已经验证过的 `me`/`route`/`practice` 走的是同一条转发代码路径，判定为可靠）。
  单人版回归照常跑过，无报错。
  顺手把 `CLAUDE.md` 里「联机」那条待办的过期描述（还写着"设计已定稿，代码一行未写"）改成了
  跟这份文档同步的状态。

- **2026-09-20（续）** · 用户提了两条改进，都做完了：
  1. **组队从"一进页面就挡住"改成主城里的一个板块**：`coop.html` 不再有一层强制的
     入房间遮罩，打开页面直接是主城（能逛图鉴、设置，逛都不用组队）。洞窟下面新加一块
     「组队」，点开是 `#veilTeam`：「创建房间」自动生成一个 4 位房间号
     （`randRoomCode()`，去掉了 0/O/1/I 这种容易看混的字符）并立刻连接、「加入房间」
     弹出房间号输入框。连上之后这个面板变成状态视图：房间号 / 自己是房主还是队友 /
     队友连没连。「组队」板块本身的副标题（`#teamHint`）也会跟着刷新。
  2. **选难度从"房主一点就两人一起进"改成"房主选、队友确认"**：房主在洞窟里点一条路线
     现在只是**提议**（`coopProposeRoute()`），不会立刻进 —— 卡片会高亮、下面写
     "等队友确认"，房主还能反悔重选（重选会覆盖上一次提议，队友要重新确认）。
     队友那边看到的是"房主选了这条路"+ 一个「确认，一起下去」按钮
     （`#btnCaveConfirm`），点了才会发 `enterConfirm` 消息、两边才真的 `enterRoute()`。
  协议加了一条 `enterConfirm {id}`（队友 → 服务器 → 房主）。⚠️ **踩了一个坑**：
  一开始只在客户端两边写了发送/处理逻辑，**忘了在 `server.js` 的 `onMessage` 里加转发
  这个消息类型的 `case`**——服务器不认识的消息类型会直接在 `switch` 里被吞掉，
  队友点确认后房主那边**什么反应都没有，也不报错**（两边控制台都干净，因为压根没人
  抛异常，只是消息在服务器那一跳就没了）。这类"服务器该转发的新消息类型"以后加了
  客户端逻辑，**一定要回去检查 `server.js` 的 `switch(msg.t)` 里是不是也加了一条**，
  不然表现就是"点了没反应"，比报错更难查。
  Playwright 测过完整链路：创建房间 → 拿到房间号 → 另一个窗口加入 → 房主选路线 →
  队友确认 → 两边一起进洞 → 两边独立寻路/战斗，全程两边控制台都没有报错。
  单人版（`index.html`）也重新跑过回归，没受影响。
  ⚠️ 这一轮还顺手补了一个潜在的崩溃点：`#btnCaveConfirm` 这个按钮只有 `coop.html`
  才有，最早写的时候监听器是**不分单人/联机都挂**的，`index.html` 上 `$()`
  会拿到 `null`，`null.addEventListener` 当场报错、整个 `game.js` 都会跑不起来
  （这种全局脚本一旦顶层抛异常，后面所有事件绑定都不会发生）。已经改成
  `if(COOP) $("btnCaveConfirm").addEventListener(...)`。**以后往 coop.html
  独有的元素上挂监听，记得这条规矩**——凡是 `index.html` 里没有的元素 id，
  监听器必须包在 `if(COOP){...}` 里面，或者判断 `$()` 的返回值非空，不能既不在
  `if(COOP)` 里、也不在某个只有联机才会调用的函数里。

- **2026-09-20** · **第一期「骨架通」代码写完了**：新增 `server.js`（零依赖 Node，自己写
  WebSocket 握手/帧解析 + 静态文件服务 + 房间转发）、`net.js`（客户端）、`coop.html`（联机入口，
  照抄 `index.html` 结构，加了进房间弹层和队友状态条）。`game.js` 加了 `COOP` 开关和一批
  `if(COOP)` 分支：世界同步（`packWorld`/`applyCoopWorld`，房主生成广播、队友照单铺场）、
  移动锁（`coopToggleMove` + 服务器 ready/go 握手，房主算路广播、队友照抄）、下楼双确认
  （`coopConfirmStair`）、队友状态条（`coopSendMe`/`renderMate`，500ms 心跳）、房主选路线
  队友只能看（`enterRoute` 加了 `fromNet` 参数）、联机里禁用撤退并把「脱壳」从掉落池滤掉、
  盲斗联机里不跳层。**战斗本身没有联网**——两人各打各的怪、互不影响，这是跟"实现设计"那两节
  的出入，详见上面"跟原计划的出入"那一段，双血条战斗留给第二期。
  `index.html` 一个字没改。单人版用 Playwright 回归测试过（进洞/寻路/战斗/答题/翻标签页），
  没有报错；联机用两个 Playwright 浏览器 context 连同一个房间测过房主/队友分配、
  路线选择、移动锁、世界同步、独立战斗、状态条，也没有报错，但没有自动测到"清完一层
  →下楼→进第二层"（测试脚本乱点，角色会先倒下）——这一步留给用户在真机上确认。
  顺手把 `coop.html` 加进了 `deploy.yml` 的 `cp` 列表、`CLAUDE.md` 的文件结构表也同步了。

- **2026-09-19（续）** · 三条待定规则拍板：盲斗在联机里不跳层、联机里禁用撤退、
  房主是先连上的那台手机（掉线要让另一个自动接管）。连带补了两节：
  禁用撤退让「脱壳」变废牌，要从四个出货口滤掉；以及第二轮发现的遗漏 ——
  进洞之前（选哪一章、练习模式）也得由房主定，重连时一律由房主重广播世界包。

- **2026-09-19** · 第二轮构思，新增「实现设计」整节。读代码后的几个结论：
  服务器不跑游戏逻辑（Node 没 DOM），只维护两条血条 + ready + 倒地并定序；
  世界状态分层为「布局共享、消耗独立」；`m.hp` 始终指向「我眼前这条血条」，
  `answer()` 里扣血那几行一个字不用改；全文件判怪死只有一处（1833 行）；
  115 件遗物里只有掘金/水/盲斗 3 件碰共享世界；两个棋子逻辑同格、渲染错开。
  留了三件待用户拍板：盲斗跳层、撤退、房主怎么定。

- **2026-09-18** · 定下 `server.js` 零依赖（自己实现 WebSocket，不用 npm 包）；
  确认用户是 Windows，记下装 Node 的步骤和防火墙那个坑。

- **2026-09-18** · 需求对齐 + 方案定稿，落成这份文档。代码一行未写。
  用户确认的关键选择：Node 服务器 / 怪各自一条满血 / 同时各答各的 / 倒地不出局队友救起 /
  同一仓库开关隔离 / 倒地的人不阻塞移动 / 倒地后不缓解难度 / 战后寻路保留。
