# 战场模式 · 设计文档

**不上线**（跟 `遗物数据表.md` 一个待遇，不进 workflow 的 cp 列表）。
这份是战场模式的唯一说明书：用户在这上面划改，Claude 照着改 `battle.js`。
**改了数值就回来同步这份文档。**

---

## 〇 这是什么

一个**跟背单词完全无关**的幸存者类模式（土豆兄弟 / 吸血鬼幸存者那一路）。
玩家在一张**无限大**的地图上跑，怪一波一波涌上来，玩家**自动挥刀**做范围伤害，
升级时从 4 件遗物里挑 1 件（**50 级前每 2 级、51~80 级每 5 级、80 级后每 10 级**），倒下就结束。
**第 40 波是终点**（打完那一波就算通了这一层），往后是 1–40 波的怪混着出、数值一直往上堆。

⚠️ **2026-09-22 又加了一层塔防**：开局和每 5 波会进一次**部署阶段**，
照云顶之弈那套用金币升级 / 刷新 / 买塔、三张同名合成高一星，把塔、泉、商摆成一座营地。
**40 座塔**（含回血 3 / 经济 3；第四批 9 座是 2026-09-23 加的），**升星给的是效果不是数值，三星是质变**。
**每座塔有自己的特效签名**（`TW_FX`：颜色 + 弹丸造型 + 命中样子 + 光环动画，见 12.10）。
**每一波存一次档**（第九节），**部署阶段整个战斗层都不画**（12.1）。
**全部细节在第十二节，碰塔防之前先读那一份。**

跟地牢那边的关系：

| | 地牢（五章） | 战场 |
| --- | --- | --- |
| 核心循环 | 答题 → 砍怪 | 走位 → 自动挥刀 |
| 地图 | 33×25 格、房间图 | **无限**、连续坐标 |
| 推进单位 | 层（50 层一章） | **波**（每波 30 秒；**40 波是终点**，往后混战无限）|
| 遗物获取 | 清层五选一 | **升级四选一**（≤50 每 2 级 / ≤80 每 5 级 / 之后每 10 级）|
| 入口文件 | `index.html` / `coop.html` | **`battle.html`**（独立） |
| 逻辑文件 | `game.js` | **`battle.js`**（独立） |
| 共用 | `util.js` / `art.js` / `content.js` 的 `RELICS`、`RAR_*`、`FUSE_*` | |

⚠️ **`game.js` 一个字都没改，`index.html` 只多了一个入口按钮。**
战场模式炸了也影响不到地牢那边。

⚠️ **美术零新增**：怪的形象全部复用 `art.js` 的 `MOB_ART`（地图小图，轮廓 + 眼睛，
正好适合战场里二三十像素的单位），Boss 用同一套放大。
`MOB_ART` 的 SVG 主体是 `currentColor`，所以每只怪在 `battle.js` 里配一个颜色就够了。

---

## 〇·五 在主城的两个入口

- **主城「战场」按钮**（`index.html` 的 `#btnBattle`，一个 `<a href="battle.html">`），
  副标题就一句 **「能站多久？」**（用户 2026-09-22 定的，别写回「跟背单词无关…」那一长串）。
- **图鉴里多了一页「战场遗物」**（`data-tab="bf"`）：同一批 209 件，
  词条走 `bfWord()` 显示成战场里的说法，顶上的统计换成 `youxu.bf.v1` 的
  「最深第 N 波 / 击杀 / 下场几次」。
  ⚠️ **这一页没有「拿过几次」** —— 战场模式不写 `CODEX`，蹭地牢那份计数会看混。
  ⚠️ 照例**两份 HTML 都要加**（`index.html` / `coop.html`）。

## 一 操作与画面

- **虚拟摇杆 + 自动挥刀**（用户定的）。
  - 触屏：**按住屏幕任意位置**就地长出摇杆，拖动方向 = 移动方向，松手停下。
    摇杆死区 12px，推到 40px 就是满速（中间按比例给速度）。
  - 键盘（桌面自测用）：WASD / 方向键。
- **刀是自动的**：每 `1/aspd` 秒挥一次，打出一个 `arc` 度、半径 `range` 的扇形，
  扇形里的敌人全部吃一次伤害。
- **挥刀特效是「扫过去」的**（用户 2026-09-23 要求优化，`fxSwing()` + `drawFx()` 的 `sw` 分支）：
  刀锋按时间从 `dir−ha` 扫到 `dir+ha`，后面拖 40% 弧长的尾巴，刀尖上有一点白；
  **命中越多刀身越粗**，狂刃那一刀转成金色再粗一档。
  ⚠️ **持续时间跟攻速走**（`0.62 / aspd`，夹在 0.09~0.2 秒）——
  攻速 2 刀/秒时上一道还没消失下一道就来了，会叠成一片白。
  ⚠️ 照旧**只用线和弧**（12.10 那条规矩），别往里加渐变或粒子系统。
- ⚠️ **刀气（破空的冲击波）飞多快，跟攻速走**（用户 2026-09-23，`waveSpeed()`）：
  基准攻速 `BF.base.aspd` 对应 `SP_WAVE_SPD`(420)，攻速翻倍就飞两倍快，
  **封在 2.4 倍** —— 再快就只剩一道残影，看不出是从哪儿飞出去的。
  画法也从「整圆」改成了**朝着飞行方向的一段弧 + 一道拖尾**，一眼看得出往哪儿走
  （怒潮那种原地扩散的圈仍然画整圈）。
- ⚠️ **刀自动瞄准最近的敌人**（`aimDir()`，搜索半径 `range × 2.4`），只有附近一个人都没有时
  才用移动方向。**别改回「只朝移动方向」** —— 实测那样绕圈跑 100 秒只砍到 2 只：
  怪永远在你背后，自动挥刀等于没有。幸存者类的自动武器都是自动瞄准的。
- **相机永远居中**（跟地牢那条规矩一致），地图无边界。地板是程序化画的：
  56px 的格线 + 按坐标哈希撒的石纹，**不存任何地图数据**。
- **顶栏**：波数 / 本波剩余秒数 / 等级 / 金币 / 击杀数 + **血条**，**右上角是「暂停」**
  （用户 2026-09-22 从底部挪上来的，`.pausebtn`；顶栏右边留了 76px 给它，别收掉）。
  ⚠️ **血条和经验条 2026-09-22 互换了位置**（用户要求）：**血条在顶栏**（护盾照旧只画上半条）、
  **经验条在底部**那三个按钮上面。血是要一直盯着的，经验只是进度。
  两个类（`.hpbar` / `.xpbar`）一个字没改，只在 `battle.css` 里各补了一条位置微调
  （`.hud .hpbar` / `.foot .xpbar`）。**别换回去。**
- **暴击和护盾的显示**（用户 2026-09-22 要求优化）：
  暴击的伤害数字 **20px 金色 + 深色描边 + 前 1/3 段放大回弹**，另外命中处甩一个金色冲击圈；
  护盾除了血条上那一段（加了渐变和右侧亮边、涨了会闪一下），**玩家身上还套一圈会呼吸的蓝罩子**，
  粗细跟着护盾量走。两样都吃 `REDUCE_MOTION`。
- ⚠️ **遗物页 / 特殊页 / 结算页 / 游商的按钮钉在列表下方**（`.box.col` + `.scroll` + `.rowb.pin`，
  用户 2026-09-22）：带满 15 件、或者货架摆 5 件时列表很长，按钮跟着滚下去就找不着了。
  **别把按钮塞回 `.scroll` 里面。** 三档屏幕量过（390×667 / 430×900 / 1280×720）都在视口内。
  遗物页钉的是**三个**：选择材料 / 合成 / 继续；游商钉的是**刷新货架 / 走了**。
- **底部三个按钮**：**信息 / 遗物 / 特殊**。
  「信息」是面板数值（用户 2026-09-22 从遗物页里拆出来单开一个按钮的），
  「遗物」是那 15 件 + 合成面板，「特殊」是 Boss 掉的那些。
- ⚠️ **Boss 波多一条 Boss 血条**（顶栏经验条下面），并且 Boss 跑出取景框时
  在屏幕边上画一个指向它的血色三角。**别把这两样删了** ——
  Boss 移速比玩家慢，追着你跑的时候经常整个在框外，没有这两样就完全不知道打到哪儿了。

---

## 二 核心属性与公式

### 玩家面板（战场自己一套，跟 `CHAPTER.playerBase` 无关）

| 属性 | 基础值 | 说明 |
| --- | --- | --- |
| `maxHp` | 100 | |
| `atk` | 12 | 一刀的基础伤害 |
| `aspd` | 1.25 | 每秒挥几刀（= 0.8 秒一刀） |
| `range` | 78 | 刀程（px） |
| `arc` | 120 | 刀的张角（度） |
| `spd` | 155 | 移速（px/秒）—— **战场模式新开的属性** |
| `armor` | 0 | 每点减免 1 点伤害 |
| `crit` | 10 | 暴击率 % |
| `critMult` | 2.0 | 暴击倍率 |
| `pickup` | 75 | 拾取半径（px） |
| `knock` | 14 | 命中时的击退（px） |

**每升一级**：`maxHp +9`、`atk +1.5`（并回满 8% 最大生命）。
⚠️ 这两个数 2026-09-22 **+50%**（原来 6 / 1，用户要求）—— 跟「经验 ×3.6」「两级一件遗物」是一组的：
等级给得少了，所以每一级要更值钱。`atk` 是小数，`bstats()` 末尾统一 round 一次，**别在表里先取整**。
成长主要靠遗物 —— 这是幸存者类的规矩，等级只是发遗物的计时器。

### 伤害公式（**照抄地牢那套，一个乘区都没加**）

```
伤害 =（atk + base + extra）×（1 + pct/100）+ flat，再 ×暴击倍率，最后 −敌人护甲，最低 1
```

- ① `base` 基础点伤、② `pct` 百分比（**全部相加只乘一次**）、③ `flat` 点伤、④ `extra` 额外伤害。
- **全局只有「×(1+pct)」和「×暴击倍率」两个乘区**，别再加第三个。
- 暴击率超过 100 的部分，每 5 点转 +10% 暴击伤害（跟地牢一致）。

### 护盾

- **护盾封顶 = 生命上限 × `BF.shieldMaxX`(2)**（用户 2026-09-22：「护盾不能超过血量上限的两倍」）。
  收口在 **`addShield()`** 里（`shieldCap()`），**所有给盾的路都从那儿过** —— 别在别处再写一遍。
  ⚠️ 凝盾自己那个 `AEGIS_MAX` 仍然照吃，两个上限取小的那个。
  ⚠️ 为什么要封：护盾不吃减伤链、不进结算、每波还有一堆「进波给盾」的遗物在产，
  堆到四五百就等于「这一趟不会死了」，整条挨打的线全废。

### 挨打这一侧

```
挨打 = takeHit(dmg) → 先扣护盾，剩下的才扣血
mitigate(dmg) = floor(dmg × (100 − cut) / 100) − armor，最低 1
```

- `cut` 是常驻减伤合计，**封在 75%**（`BF.cutMax`，跟地牢的 `MIT_CUT_MAX` 同值）。
- **先乘后除**（`floor(out * (100-cut) / 100)`），别写成 `out * (1-cut/100)` —— 浮点会白掉 1 点。
- **接触伤害有冷却**：同一只怪碰到你之后 0.65 秒内不会再打第二下（`BF.touchCd`）。

### 连击

**连击 = 连续挥刀命中、期间没受过伤的次数**（对应地牢的「连续答对」）。
- 挥一刀**至少命中 1 只**：`combo += 1`
- **受到伤害**：`combo = 0`（遗物「长链」改成减半、「铁胆」「断链」「惯性」「归位」照旧生效）
- 连击加成 = `floor(combo / comboStep) × 2%`，`comboStep` 基础 5（遗物「火星」减到 2）。

---

## 三 波次系统（定时制）

- **每波 30 秒**（`BF.waveSec`），时间到**立刻进下一波，场上的怪不清**。
- 怪从**屏幕外一圈**（相机半对角线 +60px 的环上）随机角度生成。
- **每波三个旋钮**：

```
生成速率 rate(w) = 0.75 + 0.22 × (w − 1)      只 / 秒
同屏上限 cap(w)  = min(70, 22 + 4 × w)        只
血量倍率 hpMul(w)  = 1 + 0.20 × (w − 1)
伤害倍率 dmgMul(w) = 1 + 0.09 × (w − 1)
移速倍率 spdMul(w) = min(1.35, 1 + 0.015 × (w − 1))
经验倍率 xpMul(w)  = 1 + 0.12 × (w − 1)
```

⚠️ **移速倍率必须封顶**（1.35）：怪跑得比玩家快，走位这个玩法就没了。
⚠️ **血量倍率涨得比伤害快**是故意的：幸存者类的压力来自「清不干净」，不是「一下打死你」。

### 第 40 波是终点（用户 2026-09-22）

原话：「**40 波为游戏终点，后面关卡是 1–40 的怪物混出，不断增加数值**」。

- **第 40 波的「墟心冕者」打完就算通了这一层**，`nextWave()` 在踏进第 41 波的那一下
  **当场把这一层写进存档**（`m.tb[层号]`，见第八节）—— 不写的话「打过 40 波但没死就关了页面」
  会白打一趟。⚠️ **一整趟只写这一次**（`w === BF.lateFrom + 1`），别改成每波都写。
- **第 41 波往后不再有新怪**：`waveDef()` 分岔到 **`lateWaveDef()`** ——
  池子是**从 `BF_WAVES` 现算**的（**没有新表**），每只怪按「第几波才第一次登场」给权重
  6 / 10 / 14 / 18（1–9 / 10–19 / 20–29 / 30–39 波），所以深波数仍然以难怪为主、
  早期那些也一直混在里面；精英固定刷 `gate` / `gate2` / `gate3` / `gate4` 各一只。
  ⚠️ Boss 波那几行（`pool` 里只有一个 `rat` 的占位）要跳掉，别算进去。
- **数值那一头交给 `lateRamp(w)`**（`BF.lateFrom`(40) / `lateEvery`(10) / `lateRamp`(0.10)）：
  第 40 波往下每 10 波给**血量和伤害**各再乘一档 10%，在 `hpMul` / `dmgMul` 里乘。
  实测一只灰鼠：第 40 波 106 血 → 第 100 波 442 血（×4.2）。
  ⚠️ **只乘血量和伤害**，别去乘移速和密度 —— 那两样一涨就把走位玩法关掉了
  （跟地牢 `ENDLESS_RAMP` 那条注释是同一件事）。
- 结算标题在 40 波之后写成「第 N 波 · 已通关」。

- **每 10 波是 Boss 波**（`BF.bossEvery`）：**不刷普通怪**，一只 Boss + 它自己召的随从，
  **杀掉 Boss 才结束这一波**（Boss 波不看秒数 —— 用户选的「混合」口径里唯一的清光波）。
- 怪的血量随波数成长、**不随玩家等级成长**（跟地牢一样，压力全交给波次）。

---

## 四 1–10 波的怪物设计

### 4.1 怪物基础数值（第 1 波的值，之后乘上面那几个倍率）

`美术` 一栏是 `MOB_ART` 的键，**11 只全部是现成的，一张新图都没画**。

| id | 名字 | 美术 | 类型 | hp | dmg | spd | armor | xp | 半径 | 首现 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `rat` | 廊道灰鼠 | rat | 近战·快脆 | 12 | 5 | 96 | 0 | 2 | 11 | 1 |
| `slime` | 食橱泥怪 | slime | 近战·慢肉 | 34 | 8 | 52 | 0 | 5 | 15 | 2 |
| `spider` | 洞穴长足蛛 | spider | 近战·冲刺 | 20 | 7 | 72 | 0 | 4 | 12 | 3 |
| `bone` | 残骨兵 | bone | 近战·带甲 | 30 | 9 | 82 | 2 | 6 | 13 | 4 |
| `ghost` | 低语幽魂 | ghost | 近战·穿透 | 18 | 11 | 112 | 0 | 6 | 12 | 5 |
| `prism` | 碎色棱 | prism | **远程**·直射 | 26 | 6 | 62 | 1 | 8 | 12 | 6 |
| `statue` | 守门石像 | statue | 近战·重甲 | 70 | 14 | 34 | 4 | 12 | 17 | 7 |
| `clock` | 锈钟怪 | clock | **远程**·减速 | 44 | 9 | 50 | 1 | 10 | 14 | 8 |
| `warden2` | 回廊游影 | warden2 | 近战·瞬移 | 32 | 12 | 100 | 0 | 10 | 13 | 9 |
| `dread` | 吞惧者 | dread | **远程**·散射 | 40 | 7 | 54 | 1 | 12 | 15 | 10 |
| `gate` | 层间守者 | gate | **精英** | 150 | 16 | 66 | 3 | 45 | 20 | 5 |

**行为**（`battle.js` 里每只怪的 `ai` 字段）：

- `rat` —— 直线冲向玩家。最基础的那一只，数量堆出来的压力。
- `slime` —— 直线，很慢很肉。**用来堵路**：它挡在你和出口之间就得绕。
- `spider` —— 平时 72 速，每 3.2 秒朝玩家**冲刺 0.55 秒（速度 ×2.6）**，冲完僵直 0.4 秒。
  冲刺前 0.25 秒身上有一圈白边预警。
- `bone` —— 直线 + 2 点护甲。**低攻速高单伤的构筑会被它卡住**，逼玩家吃攻速或破甲。
- `ghost` —— **不跟其它怪做碰撞排开**（可以互相重叠，也能穿过泥怪），走正弦飘移（幅度 ±22px）。
  它是「挡不住」的那一类，专门破「躲在怪墙后面」的打法。
- `prism` —— 保持 **260px** 距离（太近就后退），每 **2.2 秒**打一发直线弹（弹速 190，半径 6）。
  ⚠️ **三只远程怪开火前都有前摇，前摇期间站着不动**（用户 2026-09-22，`shot.warn`）：
  碎色棱 0.45 秒 / 锈钟怪 0.6 秒 / 吞惧者 0.55 秒，身上画一圈往里收的预警环。
  **别把前摇去掉** —— 边跑边打的远程是躲不掉的，那不是难度是噪音。
- `statue` —— 34 速 + 4 甲 + 70 血。**它几乎追不上你**，它的作用是**占地方**和吃伤害。
- `clock` —— 保持 **300px**，每 **3.0 秒**打一发大慢弹（弹速 125，半径 11），
  命中后玩家**移速 −35% 持续 2 秒**。这是全场最难受的那一只，从第 8 波起开始咬人。
- `warden2` —— 每 **5 秒瞬移**到玩家身后 90~140px 处（瞬移前 0.35 秒原地留一团残影预警）。
  它破的是「一直往一个方向跑」。
- `dread` —— 保持 **240px**，每 **3.5 秒**打 **3 发扇形弹**（±20°，弹速 165）。
  第 10 波才出，作为 Boss 的常驻压力。
- `gate` —— **精英**：体型 ×1.4、**免疫击退**、血条上带一圈金边。
  **第 5 波起每波固定刷 1 只**，第 9 波起 2 只。经验给得很高（45），是提前升级的机会。

### 4.2 每一波的刷怪表

`速率` / `上限` 由三节的公式算出，这里列的是算好的值。
`怪池` 是权重（相对值），Boss 波例外。

| 波 | 时长 | 速率(只/s) | 上限 | 怪池（权重） | 固定 |
| --- | --- | --- | --- | --- | --- |
| 1 | 30s | 0.75 | 26 | rat 100 | — |
| 2 | 30s | 0.97 | 30 | rat 70 · slime 30 | — |
| 3 | 30s | 1.19 | 34 | rat 55 · slime 25 · spider 20 | — |
| 4 | 30s | 1.41 | 38 | rat 40 · slime 22 · spider 22 · bone 16 | — |
| 5 | 30s | 1.63 | 42 | rat 30 · slime 20 · spider 20 · bone 20 · ghost 10 | **gate ×1** |
| 6 | 30s | 1.85 | 46 | rat 22 · slime 16 · spider 18 · bone 18 · ghost 14 · prism 12 | gate ×1 |
| 7 | 30s | 2.07 | 50 | rat 16 · slime 14 · spider 16 · bone 16 · ghost 14 · prism 14 · statue 10 | gate ×1 |
| 8 | 30s | 2.29 | 54 | rat 12 · slime 12 · spider 14 · bone 14 · ghost 14 · prism 14 · statue 10 · clock 10 | gate ×1 |
| 9 | 30s | 2.51 | 58 | rat 10 · slime 10 · spider 12 · bone 12 · ghost 14 · prism 12 · statue 10 · clock 10 · warden2 10 | **gate ×2** |
| 10 | **Boss** | — | — | **石廊守卫** + dread ×2 常驻 + Boss 自己召的灰鼠 | — |

**设计意图（一句话一波）**：

1. **只有灰鼠** —— 学会「贴着怪群边缘绕圈，让刀一次扫到一排」。
2. **加泥怪** —— 第一次遇到「打不动、只能绕」。
3. **加长足蛛** —— 第一次需要**看预警**。
4. **加残骨兵** —— 第一次遇到护甲，纯堆攻速的构筑开始卡。
5. **加幽魂 + 第一只精英** —— 「怪墙挡不住」+ 一个提前升级的大经验包。
6. **加碎色棱（第一只远程）** —— 视线里第一次有**弹幕**，不能再无脑贴脸。
7. **加石像** —— 场上开始有「清不掉的肉」，路被占住。
8. **加锈钟怪（减速弹）** —— 移速第一次被剥夺，是全模式的第一个真正难关。
9. **加游影 + 双精英** —— 背后会突然冒人，怪最多的一波。
10. **Boss** —— 见下。

### 4.3 第 10 波 Boss ·「石廊守卫」`warden`

复用 `MOB_ART.warden`（战斗立绘 `ART.warden` 用在 Boss 出场那一屏）。
**它自己一套写死的数值，不吃波次倍率**（跟地牢的章末 Boss `fixed:true` 一个规矩），
但**吃难度层倍率**。

| | 值 |
| --- | --- |
| hp | 1400 |
| 接触 dmg | 18 |
| spd | 58 |
| armor | 4 |
| xp | 260 |
| 掉落金币 | 400 |
| 半径 | 30（体型是普通怪的 2.5 倍） |
| 击退 | 免疫 |

**三个技能**（各自独立 CD，同时只放一个，放技能时停下不走）：

| 技能 | CD | 前摇 | 效果 |
| --- | --- | --- | --- |
| **横扫** | 6.0s | 0.8s | 朝玩家方向 200°、半径 150 的扇形，**伤害 26**。前摇期间地上画出那块扇形。 |
| **震地** | 9.0s | 1.2s | 在**玩家当下的位置**标一个半径 95 的圈，1.2 秒后砸下，**伤害 34**。圈是走位能躲开的。 |
| **召唤** | 血量掉到 75% / 50% / 25% 各触发一次 | 0.6s | 以自己为心，半径 150 的圆上均匀放 **8 只灰鼠**（吃第 10 波的倍率）。 |

**狂暴**：血量低于 30% 时，**移速 +30%、三个技能 CD 各 −30%**，身上的描边转成血色。

**Boss 波的规矩**：
- 不按秒数推进，**打死它才进第 11 波**。
- 场上常驻 **2 只吞惧者**（死了 8 秒后补一只），保证玩家不能站着对刀。
- Boss 死的那一下**全场清屏**（剩下的怪一起死，经验照给），掉 400 金，并**必掉一个泉 + 一个游商**。

**实测时长**（12 级、8 件中档遗物、机器人乱绕的走位）：**约 70 秒**，玩家从 166 血掉到 39 血，
中途被召唤的灰鼠打断两轮。真人会贴得更紧、更少空刀，估计 **35~50 秒**。
这是对的量级 —— Boss 该比一整波普通怪更久，但也不该磨到无聊。
**嫌长就先砍 `hp`（1400），别动技能** —— 技能是这一波唯一的「玩法」。

---

## 四·五 11–19 波的怪物设计（用户 2026-09-22）

**设计口径：1–10 波考走位，11–19 波考取舍。**
每一只新怪都逼玩家先决定**先打谁**，而不是无脑贴着怪群转圈 ——
拒马给别人减伤、缝合者给别人回血、缚锁者封你的塔、裂壳虫越打越多。

⚠️ **照旧一张新图都没画**：全部复用 `MOB_ART` 里现成的 11 张，靠**颜色 + 体型 + 行为**区分。

### 4.5.1 新怪数值（第 1 波的值，之后照样乘三旋钮）

| id | 名字 | 美术 | hp | dmg | spd | armor | xp | 半径 | 首现 | 干什么 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `swarm` | 蚀影群 | ghost | 22 | 9 | **142** | 0 | 7 | 9 | 11 | 穿透（不跟别的怪排开）+ 全场最快，血很低。纯数量压制 |
| `splitter` | 裂壳虫 | slime | 70 | 11 | 56 | 1 | 14 | 17 | 12 | **倒下时裂成 2 只「碎壳」** |
| `shard` | 碎壳 | slime | 22 | 8 | 92 | 0 | 4 | 10 | — | 裂壳虫的产物，体型 0.6，**不会再裂** |
| `mender` | 缝合者 | prism | 56 | 6 | 66 | 1 | 18 | 13 | 13 | **不打人**；保持 300 距离，每 2.6 秒给身边 210 内伤得最重的一只**回 15% 最大生命** |
| `bulwark` | 拒马 | statue | 130 | 16 | 30 | 6 | 24 | 19 | 14 | **光环：身边 150 内的怪受到的伤害 −25%**（不保护自己）|
| `warder` | 缚锁者 | gate | 90 | 13 | 58 | 2 | 22 | 14 | 15 | **每 5 秒封掉射程 300 内最近的一座塔 3 秒** |
| `bomber` | 爆囊 | dread | 48 | 9 | 88 | 0 | 16 | 14 | 16 | 贴到 76 内点火，**1.0 秒后自爆**：半径 100、伤害 28，自己也没了 |
| `siege` | 攻城眼 | clock | 110 | 11 | 40 | 2 | 26 | 16 | 18 | 超远程：保持 320，每 4.0 秒一发大慢弹（抬手 0.8 秒）|
| `gate2` | 深廊守者 | gate | 280 | 24 | 70 | 5 | 90 | 22 | 15 | **精英二号**：体型 ×1.6、免疫击退 |

**几条写死的规矩**：

- ⚠️ **拒马的减伤走 `guarded()`，所有打怪的路都必须过它** ——
  `hurtFoe` / `splash` / `aoe` / `zap` / `towerHurt` / 破空的冲击波，漏一条拒马就等于没摆。
  每帧先在 `updateFoes()` 里数一遍 `G.guardN`，没有拒马就整段跳过。
- ⚠️ **拒马不保护自己** —— 不然它就是块纯肉，玩家少了「先点掉它」这个解法。
- ⚠️ **缚锁者封的是塔不是人**：塔被封期间 `t.silT > 0`，**完全停手，光环塔也一起失效**
  （`refreshTowerStats` / `towerSlowAt` / `buildCut` 三处都要跳过它）。
  所以塔摆得散一点就不会被一锅端 —— 这是塔防层第一个真正的「摆位有代价」。
- ⚠️ **爆囊的引信期间站着不动**，身上画一圈往里收的预警。155 移速 1 秒跑 155 > 半径 100，
  **走开就躲得掉**。别把引信缩短到躲不掉。
- ⚠️ **缝合者 / 爆囊都不做接触伤害**（跟远程怪一样），判定在 `updateFoes()` 末尾那句
  `(!def.shot && !def.heal && !def.boom) || f.boss`。
- ⚠️ **裂壳虫的分裂在 `killFoe()` 里 push 新怪**，而 `aoe()` 是对快照迭代的 ——
  新裂出来的不会被同一发范围伤害二次命中。这是对的，别去"修"。

### 4.5.2 11–19 波的刷怪表

`BF_WAVES` 的**下标 = 波数 − 1**，所以第 10 行（Boss 波）必须占个位、永远读不到。
**别删那一行**，少了它第 11 波往后全会错一格。第 20 波往后（21、22…）一直吃第 19 波那一行。

| 波 | 新加的 | 固定（精英）|
| --- | --- | --- |
| 11 | **蚀影群** | gate ×2 |
| 12 | **裂壳虫**（灰鼠退场）| gate ×2 |
| 13 | **缝合者** | gate ×2 |
| 14 | **拒马** | gate ×2 |
| 15 | **缚锁者** | gate ×1 + **gate2 ×1** |
| 16 | **爆囊** | gate ×1 + gate2 ×1 |
| 17 | — （权重继续往后压）| gate2 ×2 |
| 18 | **攻城眼** | gate2 ×2 |
| 19 | 全员，权重压满 | gate ×2 + gate2 ×2 |
| 20 | **Boss** | — |

### 4.5.3 第 20 波 Boss ·「锈庭主事」`steward`

立绘复用 `MOB_ART.steward`（它本来就是守卫那张的别名），换个锈色 `#7A4A2A` 就认得出来。

| | 值 |
| --- | --- |
| hp | 3000 |
| 接触 dmg | 30 |
| spd | 62 |
| armor | 6 |
| xp / 金币 | 560 / 820 |
| 半径 | 32（体型 ×2.6）|
| 击退 | 免疫 |

**技能**（⚠️ **跟守卫一条都不重**：守卫是「扇形 + 一个圈」，考的是别贴脸；
主事是「一片钉子 + 一条长鞭」，考的是**别站在原地、也别站成一条线**）：

| 技能 | CD | 前摇 | 效果 |
| --- | --- | --- | --- |
| **锈钉雨** `nails` | 7.0s | 1.1s | 在玩家周围标 **5 个半径 62 的圈**（第一个钉在脚下），一起落下，各 26 伤害 |
| **回廊链** `chain` | 8.5s | 0.7s | 朝玩家方向甩一条 **长 420 / 宽 46** 的带子：30 伤害 + **移速 −50% 持续 2.5 秒** |
| **唤锈** `call` | 血量 70% / 40% 各一次 | 0.6s | 半径 170 的圆上放 **6 只裂壳虫**（它们死了还会各裂成两只）|

- **狂暴**：血量低于 30% 时移速 +25%、技能 CD −30%，描边转血色。
- **常驻随从**：场上保持 **2 只爆囊**（死了 7 秒后补），逼玩家不能站着对刀。

### 4.5.4 Boss 的技能引擎（2026-09-22 重写）

以前 `updateBoss()` 把「横扫/震地」写死在函数里。现在是**数据驱动**的：

```
BOSS_SKILLS = ["sweep","quake","nails","chain"]   ← 有哪个键就有哪个技能
bossAim(f, def, k)   抬手那一下把落点/朝向**锁死**（地上画的预警就是按这个画的）
bossFire(f, def)     前摇走完真的结算
```

- **加新技能要同时动四处**：`BOSS_SKILLS`、`bossAim()`、`bossFire()`、`draw()` 里的地面预警。
- **加新 Boss**：`BF_BOSS` 抄一条 + `BF_BOSS_ORDER` 里排个位，立绘走 `buildArt()` 自动生成
  （按 `f.bkey` 取 `IMG["b_" + key]`），不用再动画面代码。
- ⚠️ **顺手修了一个老 bug**：守卫的横扫以前**只看距离不看角度** ——
  地上画一个 200° 的扇形，实际四面八方都打得到。现在按**抬手时锁定的方向**判，跟画面一致。

---

## 四·六 21–39 波的怪物设计（用户 2026-09-22「继续做到 40」）

接着「四·五」往下。**每十波换一个考点**：

| 波段 | 考什么 |
| --- | --- |
| 1–10 | **走位**（看预警、别贴脸）|
| 11–19 | **取舍**（先打谁）|
| **21–29** | **拆营地**（别守死一个点）|
| **31–39** | **破构筑**（你那套打法被专门克制）|

⚠️ 照旧**一张新图都没画**，全部复用 `MOB_ART` 的那 11 张 + 三张 Boss 图。

### 4.6.1 21–29 波：拆营地

| id | 名字 | 美术 | hp | dmg | spd | armor | xp | 半径 | 首现 | 干什么 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `leech` | 噬盾者 | dread | 90 | 12 | 74 | 1 | 28 | 14 | 21 | 站在你 200 内**每秒吸走 8% 最大生命值那么多的护盾**，自己回一半 |
| `phaser` | 相位兽 | ghost | 80 | 14 | 96 | 0 | 30 | 13 | 22 | 每 5 秒**无敌 1.4 秒**（穿透、飘移）|
| `hive` | 母巢 | slime | 220 | 10 | 26 | 3 | 45 | 21 | 24 | 每 4 秒产 2 只巢虫，**场上封 10 只** |
| `spawnling` | 巢虫 | rat | 16 | 7 | 120 | 0 | 3 | 8 | — | 母巢的产物 |
| `sniper` | 穿刺手 | prism | 95 | 16 | 52 | 1 | 32 | 13 | 26 | 抬手 1.0 秒画一条**瞄准线**，然后射一发弹速 460 的快弹 |
| `gate3` | 烬渊守者 | gate | 420 | 30 | 72 | 6 | 140 | 24 | 21 | **精英三号**（体型 ×1.8）|

- ⚠️ **噬盾者只吃护盾不吃血** —— 它是专门来破「叠盾流」的。改成扣血就跟别的近战没区别了。
- ⚠️ **相位兽的无敌收在 `guarded()` 里**（`f.immuneT > 0` 直接返回 0），所以**六条打怪的路全都认它**。
  它破的是「攒一个大招一次性砸死」，逼你分开出手。
- ⚠️ **母巢的 `hatch.max` 不许删** —— 不封的话巢虫会指数爆炸，一分钟就把同屏上限占满。
- ⚠️ **穿刺手在抬手那一下就把方向锁死**（`shot.lock` → `f.aimDir`，`shoot()` 读它）。
  弹速 460 是躲不掉的，**能躲的是那 1 秒**。别改成开火时再瞄 —— 那样地上画的瞄准线就是骗人的。

### 4.6.2 31–39 波：破构筑

| id | 名字 | 美术 | hp | dmg | spd | armor | xp | 半径 | 首现 | 干什么 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `juggernaut` | 碾压者 | statue | 380 | 34 | 46 | 8 | 70 | 24 | 31 | **免疫击退和一切减速**（`noSlow`），体型 ×1.35 |
| `revenant` | 不朽者 | bone | 150 | 22 | 86 | 3 | 55 | 15 | 32 | **倒下一次原地回 50% 血站起来**，复活过的头上打叉 |
| `stormcaller` | 唤雷者 | clock | 120 | 14 | 46 | 2 | 50 | 15 | 34 | 每 4.5 秒在你**脚下**标一个半径 85 的圈，1.2 秒后劈下（伤害 16）|
| `sapper` | 掘垒者 | spider | 130 | 18 | 92 | 2 | 48 | 13 | 36 | 活着的时候，身边 200 内的**塔攻击间隔 ×2** |
| `voidling` | 蚀空 | ghost | 110 | 16 | 104 | 0 | 45 | 13 | 38 | 倒下时**原地留一片 3 秒的蚀空**（半径 90，每 0.6 秒扣 9）|
| `gate4` | 墟心守者 | gate | 620 | 42 | 74 | 8 | 220 | 26 | 31 | **精英四号**（体型 ×2.0，也免疫减速）|

- ⚠️ **`noSlow` 的怪免疫一切减速** —— 霜灯、冰晶塔、荆棘园、霜环对它们一概无效。
  它们就是来破「控场流」的，**别在 `updateFoes()` 里给它们开后门**。
- ⚠️ **不朽者的复活必须排在 `killFoe()` 里 `f.dead = true` 之前** —— 这是它跟「裂壳虫」的根本区别
  （裂壳虫是死了再裂，不朽者是压根没死）。
- ⚠️ **掘垒者（一直拖慢）跟缚锁者（短暂封停）是两回事**，别合并成一个机制：
  一个是「先点掉它」，一个是「塔别挤在一起」。掘垒者的倍率在 `sapMul()` 里、并进 `refreshTowerStats()`。
- ⚠️ **唤雷者和蚀空的伤害都砍过一轮**（实测：唤雷 24 在第 34 波是一发 95、蚀空 14/4 秒站满是 183，
  而那会儿玩家血上限才三百上下）。**改这两个数之前先按 `dmgMul(波数)` 算一遍。**

### 4.6.3 地面危险区 `E.zones`（新系统）

唤雷者的落雷、蚀空的尸块、祭司的火场**共用这一套**，别再各写一份：

```
addZone(x, y, r, warn, life, dmg, col)
warn > 0 的那段只画预警（虚线圈）不结算；之后每 ZONE_TICK(0.6) 秒咬一口，life 到就消失
```

- ⚠️ **它也算「怪打你」，所以必须走 `takeHit()`** —— 否则护盾会被整个绕过去。
- ⚠️ **进部署阶段要连它一起清**（`openDeploy()` 里跟 `shots` / `tshots` / `pwaves` 一起清空）。

### 4.6.4 第 30 波 Boss ·「烬渊祭司」`priest`

立绘 `MOB_ART.priest` 是现成的（地牢第三章那只）。hp 6800 / 接触 48 / 移速 64 / 护甲 8 /
经验 1100 / 金币 1600 / 半径 34。

| 技能 | CD | 前摇 | 效果 |
| --- | --- | --- | --- |
| **炎环** `ring` | 8.0s | 1.1s | 一个**甜甜圈**：半径 95~250 之间才吃 52 伤害，**贴着它站或者跑到 250 外都安全** |
| **火场** `pyre` | 11.0s | 0.6s | 在你脚下和周围放 **3 片持续 6 秒**的火（半径 95，每 0.6 秒 16）|
| **唤巢** `call` | 血量 70% / 40% | 0.6s | 召 **2 只母巢**（它们还会一直产巢虫）|

常驻随从 **2 只穿刺手**（死了 8 秒补），狂暴线 30%。

⚠️ **炎环是它的身份技**：逼你要么贴脸要么远遁，**不许在「舒适距离」上磨**。
地上的预警用 `fill("evenodd")` 把中间挖空，一眼看得出贴脸是安全的。

### 4.6.5 第 40 波 Boss ·「墟心冕者」`crown`

立绘 `MOB_ART.crown`。hp 13000 / 接触 72 / 移速 68 / 护甲 10 / 经验 2200 / 金币 3200 / 半径 36。

| 技能 | CD | 前摇 | 效果 |
| --- | --- | --- | --- |
| **崩心** `nova` | 12.0s | **2.0s** | 半径 **380** 的一整圈，95 伤害 —— 唯一的解法是**往外跑** |
| **冕刃** `shards` | 6.5s | 0.6s | 向四周均匀射 **10 发**弹（弹速 220，各 34）|
| **位移** `warp` | 9.0s | 0.5s | 瞬移到你刚才站的地方旁边 —— **甩不掉它** |
| **唤魂** `call` | 75% / 50% / 25% | 0.6s | 召 **4 只不朽者**（每只要杀两遍）|

常驻随从 **1 只碾压者**（死了 10 秒补），狂暴线 30%。

⚠️ **第 50 波往后继续按冕者加压**（每多一轮 hp/经验/金 ×1.9、伤害 ×1.35）。
再往上要新 Boss 的话：`BF_BOSS` 抄一条 + `BF_BOSS_ORDER` 排个位，立绘走 `buildArt()` 自动生成。

### 4.6.6 五个新的 Boss 技能

`BOSS_SKILLS` 现在是 **`sweep` / `quake` / `nails` / `chain` / `ring` / `pyre` / `nova` / `shards` / `warp`**。
**加新技能照旧要同时动四处**：这张表、`bossAim()`（抬手锁落点）、`bossFire()`（结算）、
`draw()` 里的地面预警。

---

## 五 升级与遗物四选一

- **经验**：击杀直接给（不掉经验球），`xp × xpMul(w) × (1 + 求知%)`。
- **升级所需**：`need(lv) = ⌈(8 + 6 × (lv − 1)) × 3.6⌉`（1→2 要 **29**，9→10 要 **202**），
  **40 级往上再 ×3**（用户 2026-09-22；39→40 要 850，40→41 要 **2616**）。
  遗物「速成」照旧 −20%。
  ⚠️ 深波数的经验本来就滚雪球（一波几百只），不在 40 级刹一下的话 60 级之后一波能升七八级。
  **别去砍怪的 `xp` 来代替它。**
  ⚠️ 那个 **×3.6 是两次叠出来的**：用户 2026-09-22 先要「升级难度增加 100%」（×2），
  同一天又要「升等级所需经验加 80%」（再 ×1.8）。
  **别去砍怪的 `xp` 来达到同样效果** —— 那会连带把金币曲线和 Boss 的经验补偿一起拖下水。
- ⚠️ **哪几级给 4 选一走 `BF.pickSteps` + `pickAtLevel(L)`**（用户 2026-09-23 改）：
  **50 级以内每 2 级**（2 / 4 / … / 50）、**51~80 级每 5 级**（55 / 60 / … / 80）、
  **80 级往后每 10 级**（90 / 100 / …，**不再封顶**）。
  按「到达的那一级」逐级判，一次升好几级就把中间每一级都数一遍，不会漏。
  ⚠️ 原来那套「`P.lvlSince` 攒零头 + 超过 `pickLvlMax`(80) 级就不再发」**已经删了**，别加回来
  （老存档里的 `lvlSince` 字段留着也没人读，无害）。
  升级本身照旧**一级一算**（面板、`onLevelRelics()` 都按级走）。
- 一次升多级就连着弹几次。
- ⚠️ **遗物页里的遗物按品质从「普通」排到「神圣」**（用户 2026-09-22，`relicsByRar()`）——
  遗物页 / 取舍窗 / 结算页三处都走它，同品质内保持 `RELICS` 的原顺序。
  它只是**显示顺序**，`P.relics` 本身没动。
- 候选来自 `content.js` 的 `RELICS`，**按地牢那张掉率表的同一个形状**，横坐标换成波数：

| 波 | 普通 | 稀有 | 史诗 | 传奇 |
| --- | --- | --- | --- | --- |
| 1–4 | 87.5 | 12.5 | — | — |
| 5–8 | 75 | 25 | — | — |
| 9–12 | 60 | 35 | 5 | — |
| 13–16 | 50 | 40 | 10 | — |
| 17+ | 35 | 45 | 15 | 5 |

- **神圣恒为 0，不掉落** —— 只能靠合成、游商和石箱（石箱抽的是 `波数 + 6` 那一档，也吃这张表）。
- **遗物上限 15 件**（`relicCap()`，「行囊」+3）。带满了还捡就弹取舍窗，
  换下来的那件当场分解成金币（跟地牢的 `doSwap()` 一个规矩）。
  ⚠️ **只剩 1 格时底部「遗物」按钮上挂一个红点**（`.fb.warn`，用户 2026-09-22）——
  再捡一件就要做取舍了，提前给个信号。
  ⚠️ **刚好装满那一下，遗物页会自己弹出来**（`maybeFullBag()`，用户 2026-09-22）：
  下一件就要做取舍，先让玩家看一眼手里有什么、顺手合成掉几件。
  只在**从没满到满**的那一下弹一次（`P.bagAlerted`，掉到满以下才复位），
  而且挂在 `step()` 里 —— 暂停时不跑，所以永远不会去抢别的弹层。
- **每次升级给一次「换一批」**（`BF.rerollN` = 1，遗物「集齐」再 +1）：
  点一下整批候选重掷，**用完按钮变灰写「已经换过了」，不是消失**（用户 2026-09-22）——
  按钮突然不见会让下面的「都不要」跳位置。下一次升级重新给。
  ⚠️ 它是**基础功能**，不是遗物效果 —— 以前那套「每波一次、只有集齐才有」已经改掉了，别改回去。
- 还有个「都不要」，跳过这一次升级的选择（带满了又不想换的时候用）。

---

## 五·五 特殊遗物（Boss 掉落，2026-09-22）

用户定的规格：**打倒 Boss 后从特殊遗物池里三选一，不占那 15 个遗物位，
效果要强、要有割草的爽感**（用户原话：「现在的遗物数值都太保守了」）。

- 表在 `battle.js` 的 **`BF_SPECIAL`**（**30 件** = 第一批 18 + **第二批 12**，2026-09-23），
  id 全部带 `sp_` 前缀。
  ⚠️ **跟那 209 件是两套东西**：存在 `P.special` / `P.sset` 上，判定走 **`hasSp()`** 不走 `has()`。
  **别把它们混进 `RELICS`**，也别让它们进合成池或分解。
- **每 10 波一只 Boss，所以一趟拿得到的件数 = 波数 ÷ 10** —— 第 30 波才 3 件。
  单件必须够猛，这就是它们能这么强的理由。
- ⚠️ **最多同时带 5 件**（`BF.specialMax`，用户 2026-09-22）。
  满 5 件之后再打 Boss，挑完新的会弹一个**取舍窗**（`veilSpSwap`）让你换掉一件，
  或者点「不要了」放弃 —— 跟遗物带满 15 件是同一个套路。
  **换下来的那件当场失效**（`P.sset` 里删掉），特殊遗物不折金币。
- ⚠️ **设计口径：一件都不许是「伤害 +N%」那种。**
  每一件都得改变「一刀能打到多少东西」—— 张角、刀程、攻速、补刀、爆炸、连锁、
  冲击波、绕转刀、践踏、光环。加新的照这个来，别加纯数值件。

| id | 名字 | 效果 | 实现在哪儿 |
| --- | --- | --- | --- |
| `sp_whirl` | 回旋 | 刀挥出一整圈（张角 360°） | `bstats()` |
| `sp_reach` | 长臂 | 刀程 +60%，击退翻倍 | `bstats()` |
| `sp_haste` | 疾风 | 攻速 +60% | `bstats()` |
| `sp_second` | 二段 | 每一刀之后补一刀（70% 伤害） | `swing()` + `me.second` 倒计时 |
| `sp_burst` | 爆裂 | 击杀时原地炸开：范围 80，伤害 = 那只怪最大生命的 60%，**能连锁** | `killFoe()` → `aoe()` |
| `sp_chain` | 雷链 | 每次命中，闪电跳到最近 3 个敌人，各受 40% 伤害 | `swing()` → `zap()` |
| `sp_wave` | 破空 | 每一刀射出一道穿透冲击波（70% 伤害） | `E.pwaves` |
| `sp_orbit` | 悬刃 | 两把刀绕着你转，碰到的敌人每 0.4 秒受 60% 伤害 | `updateSpecial()` |
| `sp_trample` | 践踏 | 移动时每 0.3 秒对身边的敌人造成 35% 伤害 | `updateSpecial()` |
| `sp_thorn` | 荆棘 | 每秒对身边 130 范围内所有敌人造成 50% 伤害 | `updateSpecial()` |
| `sp_exec` | 处决 | 敌人生命低于 25% 时，命中直接击杀（**Boss 除外**） | `swing()` |
| `sp_vortex` | 漩涡 | 每 3 秒把周围的敌人拽到身边 | `updateSpecial()` |
| `sp_frost` | 霜环 | 身边 220 范围内的敌人移速 −40% | `updateFoes()` |
| `sp_horde` | 人海 | 身边每有一个敌人，伤害 +3%（**不封顶**） | `swing()` 的 ② 层桶 |
| `sp_rampage` | 狂暴 | 每次击杀攻速 +5%，持续 4 秒，最多叠 15 层 | `killFoe()` + `bstats()` |
| `sp_skull` | 裂颅 | 暴击时以被砍中的怪为心炸开：范围 100，130% 伤害（**一刀最多炸 3 次**，`SP_SKULL_MAX`） | `swing()` → `aoe()` |
| `sp_magnet` | 磁石 | 拾取范围 ×4；每捡一枚金币，下一刀伤害 +3%（挥刀后清零） | `bstats()` + `updateDrops()` |
| `sp_feast` | 盛宴 | **拿到之后**每次击杀回复 1% 最大生命，并且最大生命 +1（最多 +400）—— 计数在 `P.feastN` | `killFoe()` + `bstats()` |

**第二批 12 件（用户 2026-09-23：「加多一批特殊效果的遗物」）。**
前 18 件已经把「张角 / 刀程 / 攻速 / 补刀 / 爆炸 / 连锁 / 冲击波 / 绕转 / 践踏 / 光环 /
处决 / 牵引 / 减速」占满了，所以这一批走的全是**没人用过的口子**：
持续伤害、打回弹丸、分身、天降、瞬移、跟塔联动。

| id | 名字 | 效果 | 实现在哪儿 |
| --- | --- | --- | --- |
| `sp_ember` | 余烬 | 命中的敌人燃烧 3 秒，每秒受 20% 攻击的伤害 | `swing()` 打标记 + `updateFoes()` 结算 |
| `sp_deflect` | 格挡 | 挥刀把刀锋里的敌方弹丸打回去，每颗 120% 攻击 | `deflectShots()` |
| `sp_rain` | 刀雨 | 每 **1.2** 秒随机一个敌人头顶落一道（**150%** 伤害，范围 60） | `updateSpecial()` |
| `sp_clone` | 残影 | 身后 64px 的影子跟你同时挥刀（50% 伤害） | `swing()` 末尾 + `draw()` |
| `sp_spike` | 逆刺 | 受到伤害时，对身边 160 内造成最大生命 8% 的伤害 | `takeHit()` |
| `sp_ice` | 冰裂 | 对被减速的敌人伤害**翻倍**；击杀它们时原地炸开（范围 90） | `swing()` + `isSlowed()` |
| `sp_quake` | 裂地 | 每挥 **4** 刀，以自己为心裂开一圈（范围 210，**120%** 伤害） | `swing()` |
| `sp_flurry` | 狂刃 | 连续命中 12 次后，下一刀**刀程和张角翻倍且必定暴击** | `swing()`（局部变量，**不进 `bstats()`**） |
| `sp_blink` | 虚影步 | 每 **4** 秒：被贴身时瞬移开 200，原地留一发（范围 110，**200%**） | `updateSpecial()` |
| `sp_reso` | 共鸣 | 每挥一刀，**最近的那座塔立刻也开一次火** | `resoFire()` → `fireTower()` |
| `sp_leech` | 饮刃 | 每次命中回复 0.4% 最大生命（每刀最多 6 次） | `swing()`，**攒完一次结算** |
| `sp_tide` | 怒潮 | 每进一波、**之后每 6 秒**，放出一圈横扫全场的刀气（200% 伤害） | `tideRing()`（`onWaveRelics()` + `updateSpecial()` 的 `P.tideCd`）→ `E.pwaves` 的 `ring` 分支 |

⚠️ **饮刃必须攒完再回，不能逐次 `healUp()`** —— 单次是 0.4% 最大生命，
`healUp()` 里的 `Math.round` 会把每一笔都抹成 0，**实测一刀回 0 点**。这个坑踩过一次。
⚠️ **狂刃改的是 `swing()` 里的局部 `range` / `arcDeg`，不是 `s`** ——
写进 `bstats()` 就变成常驻加成了。
⚠️ **冰裂的「被减速」走 `isSlowed(f)`**：既认塔和弹丸打上的 `f.slowT`，
也认「站在霜环 220 范围内」（`sp_frost` 是在 `updateFoes()` 里直接乘速度的，不写 `slowT`）。
⚠️ **共鸣只找会开火的那几种 `kind`**（`shot` / `burst` / `lob` / `aoe` / `chain`），
且要求 `t.tgt` 还活着、没被「缚锁者」封停 —— 光环 / 经济塔按自己的节奏走，
硬塞一发会**把金库的收入算重**。
⚠️ **怒潮复用 `E.pwaves`**，靠 `ring:true` 走另一条更新分支（原地扩散、只打圈经过的那一环）。

**2026-09-23 按「折合每秒几倍攻击」拉了一遍**（用户：「优化遗物效果和数值，检查合理性」）。
基准是玩家自己那把刀：1.25 刀/秒 × 1.0 倍 = **1.25**（还吃全部百分比）。

| 件 | 改前 | 改后 | 说明 |
| --- | --- | --- | --- |
| 怒潮 | 一波一次 = **0.07** | 每 6 秒一次 = **0.33**（全屏）| 原来一波只放一圈，全表最弱 |
| 裂地 | 5 刀 90% = 0.23 | 4 刀 120% = **0.38**（半径 210）| |
| 虚影步 | 6 秒 150% = 0.25 | 4 秒 200% = **0.5** + 位移 | |
| 刀雨 | 2 秒 130% = 0.65（单点）| 1.2 秒 150% = **1.25** | 打的是全场随机一只，不一定在身边 |
| 裂颅 | 每只被砍中的都炸 | **一刀最多炸 3 次** | 回旋 + 高暴击时一刀扫十几只 = 十几倍范围伤害 |
| 盛宴 | 读 `P.kills`（整局击杀）| 读 `P.feastN`（拿到之后才数）| 第 20 波拿到时一下就 +400 血，词条写的是「每次击杀」 |

没动的几件留作参照：悬刃 2 × 60% / 0.4 秒 ≈ 3.0（只打刀经过的点）、践踏 35% / 0.3 秒 ≈ 1.17（只在移动时、半径 45）、
荆棘 0.5（半径 130）、破空 / 二段各 0.875、残影 0.625。

- ⚠️ **`aoe()` 是范围伤害的唯一口子**，而且**必须带递归深度**（`aoeDepth`，封 6 层）——
  爆裂能连锁，一片怪挨着炸会一路递归下去。加新的范围效果一律走它。
- **拿满 30 件的面板**（实测）：张角 360° · 刀程 125 · 攻速 2.0 刀/秒 · 拾取 300 · 生命上限 +400，
  另外还一直在烧、在落刀雨、身后跟着影子、挨打就炸一圈。
  这就是「割草」该有的样子 —— 一刀一圈、一秒两刀、杀一只炸一片。
  ⚠️ **实际只能同时带 5 件**（`BF.specialMax`），所以「全装」只是压力测试，不是真实构筑。

## 六 遗物映射表（答题 → 战场）

用户定的口径：**全量自动映射一套等价词**。
**`content.js` 的 `RELICS` 一个字都没改** —— 另挂了一张
`BFW`（battle wording）表，只**替换显示文案**，数值和 id 全部沿用。

⚠️ **2026-09-22 清过一轮**（用户：「优化目前版本遗物的信息」）：
`BFW_RE` 里「游商 → 整备点商店」和「祭坛献祭 → 整备补给」两条删了（整备点早就没了，
而游商在战场里就是玩家自己摆的那座建筑），「宝箱 → 补给」改成「宝箱 → 石箱」。
另外**全表 209 件过了一遍**，揪出三件在战场里一点效果都没有的（`spare` / `boom` / `deposit`）：
前两件按同品质改写了词条，`deposit`（分解或换掉遗物时得护盾）直接把逻辑补上了
（收口在 `sellRelicGold()`）。**以后再改战场的机制，记得回来跑一遍这个审计** ——
办法就是把 `RELICS` 的 id 跟 `battle.js` 里的 `has("…")` 对一遍，对不上的就是空的。

⚠️ **这张表（`BFW` / `BFW_RE` / `bfWord()`）住在 `content.js` 的末尾，不在 `battle.js` 里** ——
主城的图鉴多了一页「战场遗物」，而 `index.html` 不加载 `battle.js`。**别搬回去。**
`BFW_RE` 是按顺序做正则替换，**顺序有意义**：长的规则必须排在短的前面
（「答对 8 个字母以上的词」要排在「答对」前面）。

### 6.1 频率对齐（这是整张表的地基）

| 地牢 | 一层的量 | 战场 | 一波的量 |
| --- | --- | --- | --- |
| 答一道题 | 40~68 题 | **挥一次刀** | 30s × 1.25 = **37.5 刀** |
| 挨一次打 | 约 10 次 | **受到一次伤害** | 8~14 次 |
| 一层的怪 | 11.5 只 | 一波的怪 | 20~70 只 |

⚠️ **「答对」必须映射成「挥一次刀」，不能映射成「命中一只敌人」** ——
一刀能扫到五只，按命中算的话「答对时 10% 概率回 4 点血」一波能回三百多，直接炸。
**挥刀次数跟答题次数是同一个量级**，这是整套映射能成立的唯一原因。

⚠️ **「击败敌人」那一类不做频率折算**（一波 20~70 只 vs 一层 11.5 只，差 2~6 倍）——
这是故意的：幸存者类里「杀怪回血」本来就该是主力续航。
⚠️ 原来是统一乘 `BF.killScale`(0.45)，但词条上写的还是地牢的数（写 4% 实际 1.8%）——
**2026-09-23 删掉了这个系数，五件都直接写战场的数**（见 6.6 ⑤），词条跟实际一个数。

### 6.2 主映射表

| 地牢里的说法 | 战场里算什么 | 备注 |
| --- | --- | --- |
| 答对（答完一题） | **挥一次刀**（不管命中几只） | 见上 |
| 答错 | **受到一次伤害**（护盾吃掉的也算） | |
| 连击 | **连续挥刀命中、期间没受伤的次数** | 受伤清零 |
| 拼写题答对 | **这一刀暴击** | 拼写 5% vs 暴击 10%，量级最接近 |
| 拼写题出现率 +N% | **暴击率 +N%** | 刻字 +15、默诵 +30、慎笔 +2 |
| 打中弱点 | **命中精英或 Boss** | |
| 超时 | **被远程弹丸命中** | 都是「没躲开」 |
| 读条剩 N 秒 | **连续移动了 N 秒** | 停下清零，见「从容」 |
| 3 秒内答对（速答） | **移动中挥的这一刀** | 站着挥不算 |
| 答对 8 字母以上的词 | **命中大体型敌人**（泥怪/石像/吞惧者/精英/Boss） | |
| 心魔 | **仇敌标记**：打伤过你的那只怪被标记，杀掉它算「答对心魔词」 | 见 6.3 |
| 冒险 | **贴身挥刀**：扇形最内侧 45% 的范围里有敌人 | 见 6.3 |
| 每层 / 进入下一层 | **每波 / 进入下一波** | |
| 清空这一层 | **这一波结束** | |
| 当前层数 | **当前波数** | 断链、踏层、老茧、深潜都按这个 |
| Boss 层 | **Boss 波**（每 10 波） | 候门、叩关 |
| 泉 / 游商 | **部署阶段摆下去的建筑**（见 12.8） | 2026-09-22 从「击杀掉落」改的 |
| 宝箱 | **击杀掉落的石箱**（见 7.3） | 祭坛没做，相关的两件已改写 |
| 升级 | 升级 | 一模一样 |
| 金币 | 金币 | 一模一样 |

### 6.3 两条需要单独说的

**心魔 → 仇敌标记**
打伤过你的那只怪立刻被标记成「仇敌」（身上一圈暗红描边），**最多同时标记 5 只**。
- 杀掉一只仇敌 = 「答对心魔词」（驱邪 / 镇魂 / 无惧 的回血和护盾走这里）
- 被同一只仇敌再打一次 = 「答错心魔词」（心镜的减伤、无惧的免伤走这里）
- 「养魔」按场上仇敌数量算伤害加成，「缚魂」让标记数上限翻倍到 10 只

**冒险 → 贴身**
挥刀时，扇形**最内侧 45% 半径**（`78 × 0.45 ≈ 35px`）里有敌人，这一刀算「冒险」。
- 冒险挥刀命中 = 「冒险答对」：`pct += 100`（跟地牢一模一样）
- ~~冒险状态下受伤 = 「冒险答错」：`dmg × 2`~~ —— **这一条从来没实现过**（接触伤害一直传 `wager:false`），
  2026-09-23 审计时正式删掉：近战接触永远在「贴身」半径里，真做了就是**所有近战伤害 ×2**，
  等于偷偷把难度翻倍。于是挂在它上面的「铁胆」「托底」两件换了战场说法（见 6.6）。
- **它是自动判定的**（相当于地牢里的「锁定冒险」恒开）—— 战场里贴脸本来就有风险，
  不需要再给一个按钮。

### 6.4 改写的 8 件

这几件的触发条件在战场里**根本不存在**（题目辅助、祭坛、泉水），
所以**按同品质、同价值换了一套战场词条**。`content.js` 里的 `pw` 没动，只在战场模式里换显示。

| id | 名字 | 品质 | 地牢原词条 | **战场词条** |
| --- | --- | --- | --- | --- |
| `stroke` | 笔顺 | 稀有 | 拼写题随机一个字母自动填上 | **暴击率 +8%** |
| `clean` | 净写 | 稀有 | 拼写题不出现干扰字母 | **暴击伤害 +40%** |
| `wellread` | 博闻 | 稀有 | 已掌握的词出拼写题时不出干扰字母 | **刀程 +10%** |
| `key` | 钥匙 | 稀有 | 宝箱拼错也能开；拼对时二选一 | **游商多两件货**（游商不能手动刷新了，2026-09-22 改的）|
| `spare` | 祭余 | 稀有 | 祭坛献祭只要 1 点生命 | **塔的伤害 +10%**（2026-09-22 改；战场里合成本来就不要钱，原来那条是空的）|
| `boom` | 破音 | 史诗 | 拼写答对的连击奖励翻倍；拼写答对时伤害 +395% | **暴击伤害 +120%，暴击时连击额外 +1**（2026-09-22 加；原来在战场里一点效果都没有）|
| `atone` | 还愿 | 稀有 | 祭坛献祭优先扣护盾 | **每波 +12 点护盾** |
| `pace` | 稳步 | 史诗 | 这一层没用过泉/坛/箱/商，下一层回 55% | **这一波没受过伤，下一波开局回 55% 最大生命** |
| `ascetic` | 苦行 | 史诗 | 没喝过泉水时，每深入一层答错伤害 −5% | **没在游商买过东西时，每过一波受到的伤害 −5%（最多 −25%）** |

另外三件是**沿用但换了说法**，数值没动：

| id | 名字 | 战场词条 |
| --- | --- | --- |
| `water` | 水 | 每波开场回复 15% 最大生命，溢出的每 2 点转 1 点护盾（2026-09-22 把说法对齐了代码）|
| `lesson` | 课业 | 本波第一次击杀某一种怪时 +10 金币 |
| `tome` | 典藏 | 击杀本局已经杀过 20 只以上的怪种时 +8 金币 |
| `dig` | 掘金 | 每波开场在场上空投 3 堆金币 |

### 6.5 新增的战场专属属性

用户开的口子：**战场模式可以有移速**。下面这几条是**只有战场模式才有**的属性桶，
地牢那边一件都不受影响。

`spd` 移速 · `aspd` 攻速 · `range` 刀程 · `arc` 刀的张角 · `pickup` 拾取半径 · `knock` 击退

- **第一批只做到「已有遗物能改写就改写」**（比如「博闻」→ 刀程 +10%）。
- ⚠️ **战场专属的新遗物这一轮不做** —— 先把 209 件跑通，用户试完手感再决定要不要补一批。

### 6.6 第二轮审计（2026-09-23，用户：「优化战场模式的遗物效果和数值，并检查合理性」）

办法：把 209 件的**战场词条**（`bfWord()` 的输出）逐条对 `battle.js` 里的实现。揪出四类问题，全部改掉了。
**战场词条都写进了 `content.js` 的 `BFW`**（地牢那边的 `pw` 一个字没动），跟实现一字对得上。

**① 词条写了、代码没做（10 件）** —— 这几件在战场里一直只生效了一半：

| id | 名字 | 补上的那半句 |
| --- | --- | --- |
| `carve` | 刻字 | 暴击时伤害 +90% |
| `recite` | 默诵 | 暴击时额外伤害 +35 |
| `bile` | 苦胆 | 常驻减伤每 10% 伤害 +2%（最多 +10%）|
| `armblade` | 甲刃 | 每 2 点护甲暴击率 +3%（最多 +18%）|
| `evervow` | 恒甲 | 减伤每 8% 护甲 +1（最多 +6）—— 排在减伤算完之后，铁誓读到的护甲不含这一笔 |
| `janus` | 双面 | 「伤害每 40% 再 −4% 受到的伤害」（读 `G.lastPct`）；伤害那一档也补上了地牢的 5 档上限 |
| `knock` | 叩关 | Boss 波里受到的伤害 −20% |
| `bind` | 缚魂 | 击杀仇敌回复 2% 最大生命 |
| `warmth` | 余温 | 薪火触发后也给 5 次减半 |
| `fullset` | 集齐 | 真的去查「普通 / 稀有 / 史诗各一件」（以前只要带着就给）|

**② 代码的 bug（5 处）**：

- **反震 `recoil` 从不清零** —— 挨满 3 下就是常驻 +150%。现在打出去那一刀就清（跟地牢一样）。
- **赌骰 `dice` 受伤不清零、也不封顶** —— 一波能叠到暴击率 +600%（超出的还会转暴击伤害）。
  现在受伤清零、封 +150%。
- **仇敌计数 `G.hauntN` 会变负数** —— 每波直接清零，但上一波标记的怪还活着，它死的时候再减一次；
  Boss 波清场也没归零。于是「养魔」会变成负的伤害加成、仇敌上限被撑大。
  现在进波时**按场上现数一遍**，Boss 清场时归零，减的时候兜 0。
- **叠甲 `stack` 每波白加一次** —— `onWaveRelics()` 读的 `G.hurt` 已经是新一波的（恒 false）。
  改成 `newWave()` 里读上一波的 **`G.bled`**（真掉了血才算，护盾吃掉的不算）。
- 回响之厅 `hall` 能连环自己触发 3 层（期望 ×1.875，所有「挥刀时」的遗物跟着翻倍）→ **补出来那一刀不再触发**。

**③ 频率差太多，按同品质重新定数**（战场一波挥 37 刀、杀 20~70 只，地牢一层 40~68 题、11.5 只）：

| id | 名字 | 地牢数值 | 战场数值 | 为什么 |
| --- | --- | --- | --- | --- |
| `whim` | 无常 | +60 金 / 本场 +8% | **+15 金** / 本波 +8%（**封 +80%**）| 一波白给七百多金，部署经济直接作废。实测改后每波多 100~250 金 |
| `midas` | 点金 | +10 金 | **+5 金** | 早期一件稀有顶两倍击杀收入；改后 +30% 左右，跟贪婪（+20%）一档 |
| `chase` | 追猎 | 每次回 10% | **每波三次**回 10% | 带通感就是每刀回 10%，Boss 波等于不死 |
| `fearless` | 无惧 | 心魔答错不掉血 | **每波三次**：被仇敌打中不掉血 | 仇敌 = 打过你一下的怪，贴身的怪 0.65 秒再打一下 → 近战几乎全免 |
| `greet` | 见面礼 | 每场第一次 +130% | **每只敌人第一次被砍中 +100%** | 「每场」= 每只怪；原来实现成「每波前 6 刀」，传奇还不如稀有的开场 |
| `scent` | 嗅迹 | 多出弱点题 + 20% | 命中精英 / Boss **+60%** | 前半句在战场里不存在 |
| `shed` | 脱壳 | −10% + 撤退不掉血 | **−14%** | 战场没有撤退 |
| `twice` | 二见 | 第二次答错同一词 −25% | 同种怪第二下起 **−12%** | 战场里几乎每一下都满足 |
| `familiar` | 面熟 | 每只 −5%（最多 −15%）| 每种 **−3%（最多 −9%）** | 开波几秒就满档，稀有档超标一倍 |
| `nemesis` | 老对手 | 每只 Boss −5%（最多 −45%）| 每只 **−10%（最多 −40%）** | 一趟只见得到 4 只 Boss，原来第 40 波才 −20% |
| `foresight` | 未雨绸缪 | 每 100 金 +3%，不封顶 | **最多 30%** | 战场里金币能攒到上千 |

**④ 挂在「冒险答错 ×2」上的两件**（那条从没实现过，见 6.3）：
「铁胆」后半句改成**每波两次：受伤时连击不清零**（不看贴身）；
「托底」改成**被敌人贴身撞到时受到的伤害 −15%**（`takeHit()` 的接触伤害带 `touch:true`）。

另外只改了说法、数值没动的：长链 / 反刍 / 配方 / 收尾 / 凝盾 / 心镜 / 稳答 / 慎笔 / 破盾余威 /
均富 / 驱邪 / 镇魂 / 刹那 / 叠甲 —— 原来是正则换出来的半句，读不通（「受伤后下一题挥刀」「选择题受伤时」这种）。

**⑤ 第三轮（同一天，用户：「说明读不通的就改效果，那些是 bug」）**—— 这回把 209 件逐条跟实现并排摆出来对，
又揪出几件**效果本身就错**的：

| id | 名字 | 问题 | 改成 |
| --- | --- | --- | --- |
| `offer` | 献身 | 「伤害 +100%」完全没实现，只剩「最大生命减半」—— 一件纯负面的神圣 | 补上 ② 层 +100% |
| `corrode` | 蚀甲 | 「每次受伤护甲 −2」没实现 | `takeHit()` 里补上（最低 0）|
| `recipe` | 配方 | 「金币花费减半」在战场里是空的（合成不要钱）| **合成时多一件候选**（三选一 → 四选一，`fusePick()`）|
| `salve` `reap` `breath` `mend` `reapfull` | 药膏 / 收割 / 喘息 / 归血 / 收势 | 词条写地牢的数，实际偷偷乘了 0.45（写 4% 实际 1.8%）| 删掉 `killScale`，直接写战场的数：2 点 / 1 点 / 1% / 2% / 1.2% |

⚠️ **以后再加战场机制，照这个办法再审一遍**：
`bfWord()` 的输出里不许再出现「题 / 层 / 场 / 拼 / 词 / 答 / 撤退」，
而且**每一个半句都要能在 `battle.js` 里找到对应的一行**。

---

## 七 金币的去处：部署面板 + 游商 + 合成

⚠️ **这一节 2026-09-22 被塔防改版翻过一遍，先读第十二节。**
金币现在有两个去处：**部署阶段**（升级 / 刷新 / 买塔）和**营地里的游商**（买遗物）。
合成照旧不要钱。

### 7.1 金币

- **来源**：每只怪掉 `(ri(1,3) + ⌊波数 ÷ 2⌋) × BF.goldMult` 金，落在地上，走进 `pickup` 半径自动吸。
  遗物「掘金」另外每波空投 3 堆（也吃这个倍率）。
  ⚠️ **`goldMult` 现在是 0.5**（用户 2026-09-22：「降低金币爆率 50%」）。
  **Boss 掉的那一笔也吃**（400 → 200），别单独开小灶。
- **另一份来源是部署阶段的收入**（开局 60 + 每轮 35+15×(轮−1) + 利息，见 12.2）——
  那一份是买塔的预算，击杀掉的那一份主要用来买遗物。
- **去处**（全是局内，**不带回主城**）：**部署阶段**（升级 / 刷新 / 买塔）+ **游商**（5 个货位）。
  ⚠️ 石箱白给、合成也不要钱了（都是用户 2026-09-22 定的）。
- ⚠️ **石箱不要钱**（用户 2026-09-22 改的）：踩上去直接白给一件。
- 实测第 4 波手里约 240 金，一件普通遗物 67~78 金、稀有 150~180 金 ——
  **钱是紧的，这是有意的**。嫌紧就抬 `BF.deploy.incomeBase` / `incomePer`，
  **别去动怪的掉金公式**（会连带把别的曲线拖下水）。

### 7.2 合成（面板搬到了遗物页，**不要金币**）

**随时能开**：点底部的「遗物」按钮，合成面板就在最上面。
`FUSE_N`(3) 件同品质 → 高一档，从 `FUSE_PICK`(3) 件里挑；遗物「配方」减到 2 件。
⚠️ **战场模式的合成不花钱**（用户 2026-09-22，`fuseCost()` 恒返回 0）——
别把 `FUSE_COST` 加回来。地牢那边照旧 500 金，两边是独立的。
⚠️ **材料选满了，「合成」按钮会点亮**（`.btn.hot`，金底 + 一圈脉冲，用户 2026-09-22）——
不然要低头去数选了几件才知道能不能点。取消一件就灭。脉冲吃 `prefers-reduced-motion`。

- 点「选择材料」进挑选状态，**直接点下面那张遗物卡**选中/取消（跟地牢遗物页一个路子）。
- 只能挑同一品质、最多 `fuseN()` 件，**神圣不能当材料**。
- ⚠️ 挑选状态下遗物卡右上角的「分解 +N」会收起来，免得误触。
- **不在挑选状态时，点遗物卡 = 分解**，**两步确认**（第一下只是亮起来写「再点一下 · 分解 +N」，
  第二下才真卖）。卡上本来就写着「分解 +N」，点了没反应更糟；一步到位又太容易误触。
- ⚠️ `veilGot`（合成三选一）**故意没有关闭钮** —— 材料已经砸了，窗一被顺手藏掉那件就没了。

### 7.3 击杀掉落的石箱

⚠️ **泉和商已经不在这儿了**（2026-09-22 的塔防改版）：它们现在是玩家在部署阶段摆下去的
**建筑**，永久存在 —— 规矩全在 **12.8**。这一节只剩箱子，`maybeSite()` 也只剩箱子了。
**别把 `spring` / `shop` 加回 `BF.site`。**

**每击杀一只怪掷一次**（`maybeSite()`，⚠️ 这不是遗物效果，用 `Math.random()` **不走 `luck()`**）：

| 种类 | 概率 | 踩上去 | 效果 |
| --- | --- | --- | --- |
| **箱** `chest` | **1.0%** | **不弹窗** | **直接白给一件遗物，不要金币**（品质按 `波数 + 6` 那一档抽，比普通掉落好一档）。带满了才会弹取舍窗。⚠️ **每 5 波最多掉 3 个**（`max` / `every`） |

⚠️ **`p` 2026-09-22 减半**（用户：「宝箱出现概率减半」），2.0% → **1.0%**。
每波保底那一个还在（`pitySite()`），所以「一整波一个箱都没有」仍然不会发生。

- **精英**（层间守者 / 深廊守者）倒下时额外掷一次，`eliteRate` **25%**。
- **Boss** 倒下时**必掉两个箱**（以前是一泉一商，现在那两样是建筑了）。
- ⚠️ **每波保底一个**（`pitySite()`）：一整波一个箱都没掉的话，进下一波时在玩家边上
  150~230px 处补一个（箱的次数用完就跳过）。**别把保底删了** ——
  实测早期一波只杀 20 来只，不兜底经常整波空手。
- **场上最多 8 个**（`BF.site.max`），超了就删最早的那个。
- ⚠️ 石箱原来是「花钱撬开 → 两件挑一件」的弹层，用户 2026-09-22 改成**白给**了 ——
  `veilChest` 那一整套已经删干净（逻辑就在 `updateSites()` 里那几行），**别加回来**。
- **美术一张新图都没加**：`art.js` 现成的 `CHEST`，脚下画一圈会呼吸的淡光圈。

## 八 难度层

用户定的规格：**一到五层，初始一层，这一轮只做第一层**（局外养成做完再开 2–5 层）。
**只动怪，不动玩家**（跟地牢的「难度等级只动玩家攻击」正好相反 —— 那边是往下放水，这边是往上加压）。

| 层 | 名字 | 怪血量 | 怪伤害 | 生成速率 | 同屏上限 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| **1** | 一层 | ×1.00 | ×1.00 | ×1.00 | ×1.00 | 一开始就开着 |
| 2 | 二层 | ×1.30 | ×1.15 | ×1.20 | ×1.15 | 通过一层第 40 波 |
| 3 | 三层 | ×1.70 | ×1.30 | ×1.45 | ×1.30 | 通过二层第 40 波 |
| 4 | 四层 | ×2.20 | ×1.50 | ×1.75 | ×1.45 | 通过三层第 40 波 |
| 5 | 五层 | ×2.90 | ×1.75 | ×2.10 | ×1.60 | 通过四层第 40 波 |

⚠️ **解锁条件 2026-09-22 改成「打通前一层的第 `BF.tierClear`(40) 波」**（用户要求）——
原来那套 `locked:true` + 「局外养成解锁」**已经删了，别加回来**。判定走 **`tierOpen(i, m)`**，
读的是存档 `youxu.bf.v1` 里的 **`tb`**（每层各自的最深波数，老档没有这个字段，读处 `|| {}`）。
⚠️ 判的是 **`> 40` 不是 `>= 40`** —— 死在第 40 波上不算「通过 40 波」。
`tb` 有两个写入点：`endRun()`（倒下时）和 `nextWave()` 里踏进第 41 波的那一下。

⚠️ **别让怪的移速跟着难度层涨** —— 那会直接把走位玩法关掉。加压只走血量 / 伤害 / 密度。

---

## 九 存档与结算

- **一个新键 `youxu.bf.v1`**：`{best: 最高波数, kills: 累计击杀, runs: 打过几趟,
  tb: {难度层号: 那一层的最深波数}}`。
  ⚠️ **`tb` 是 2026-09-22 加的**（难度层解锁要用，见第八节），老档没有，读处一律 `|| {}` / `|| 0`。
  按 CLAUDE.md 那条规矩，它已经同时进了 `game.js` 的
  `commitPerm()` / `snapshot()` / `overwriteAll()` / `mergeData()` 四处（合并取大值）。
- ⚠️ **祝福系统（偏爱 / 封印）不作用于战场**（用户 2026-09-22 明确定的）：
  `battle.js` 从头到尾不读 `youxu.town.v1`、也没有 `blessPick()`，`relicPool()` 就是纯随机。
  **以后别顺手把地牢的 `blessPick()` 搬过来。**
- ⚠️ **战场模式这一轮不发宝石，也不碰续玩档** —— 宝石要等局外养成系统定了口径再接，
  现在两套代码写同一个 `youxu.town.v1` 是白白冒险。**倒下只记录波数。**
- **每一波存一次档**（用户 2026-09-22，`saveWave()` / `savedRun()` / `resumeSaved()`）：
  - ⚠️ **没有开新的 localStorage 键** —— 整份塞在 `youxu.bf.v1` 的 **`run`** 字段里，
    所以 `game.js` 的 `snapshot()` / `overwriteAll()` 自动带上；`mergeData()` 里单写了一条
    （**本机有 run 就一点不动**，跟地牢的层存档一个规矩；`tb` 按层取大值）。
    ⚠️ 那个对象在 `mergeData()` 里是**整份重拼**的 —— 以后 battle.js 再往里加字段，
    **必须回 game.js 把那一条也接上**，漏了就会「导入一次存档之后静默消失」。
  - **存什么**：难度层 / 波数 / 整个 `P`（遗物、特殊遗物、备战区、牌库、等级、金币…）/
    玩家坐标 / 营地里每座建筑（`k` `tid` `star` `x` `y` `used` `stock`）/ 部署阶段的货架。
    **场上的怪一只都不存** —— 波是定时刷的，读档重刷一遍就行。
  - **存档点只有两个**：`nextWave()`（进新一波）和 `closeDeploy()`（摆完塔开战）；
    部署波另有一次 —— `openDeploy()` **在发完钱、摇完货架之后**再存一遍（`nd:true`）。
    ⚠️ 那一次必须排在发钱之后：存在之前的话，读一次档就是重摇一次货架 = 免费刷新。
    **别再往别处加存档点，尤其别每帧存。**
  - **读档**走开场那一屏的「继续 · 第 N 波」按钮（`#btnResume`，没有存档就 `hidden`）。
    点「下场」会 `clearSavedRun()` 把它丢掉。倒下时 `endRun()` 也会清掉。
  - ⚠️ 存的是「**刚进这一波**的样子」：这一波打的怪、捡的金币、升的级都不算数
    （跟地牢「退回本层开头」同一个口径）。
  - ⚠️ 读档回到部署阶段走的是 **`openDeploy(true)`**（只开面板那一半）——
    `P.round++`、收入、回收、白送的卡、摇货架**一次都不许再走**，否则读一次档多发一轮。
  - 老档兜底：读档时会滤掉已经删掉的遗物 / 塔，**牌库按「总张数 − 手上已有的」整份重算**（新加的塔、改过的张数都自动对上）。
- 倒下弹结算：到达波数 / 击杀数 / 等级 / 存活时间 / 带着的遗物（**按品质排序**），
  两个按钮：再来一次 / 回主城。第 40 波之后标题写成「第 N 波 · 已通关」。

---

## 十 文件与代码结构

| 文件 | 作用 | 上线 |
| --- | --- | --- |
| `battle.html` | 战场模式入口，独立页面 | ✅ |
| `battle.js` | 全部战场逻辑（canvas 渲染 + 实体 + 遗物引擎） | ✅ |
| `battle.css` | 战场专属样式（摇杆、顶栏、弹层） | ✅ |
| `战场模式.md` | 这份文档 | ❌ |

⚠️ **新文件必须加进 `.github/workflows/deploy.yml` 的 cp 列表**（`*.js` 已经包含 `battle.js`，
但 `battle.html` 和 `battle.css` 是显式列举的）—— 已经加了。

`battle.js` 的分节（跟 `game.js` 一个路子）：

```
BF          全部数值配置（上面几节的数都在这儿）
BF_FOES     怪物基础数值表
BF_WAVES    每一波的怪池和固定刷怪
BF_BOSS     Boss 数值和技能
BF_TIERS    难度层
BFW         战场文案替换表（6.4 那几件）
bstats()    现算面板（遗物全部在这里生效，**一点加成都不许攒进 P**）
swing()     挥刀 → 命中判定 → 伤害四层桶
takeHit()   挨打的唯一入口（先扣盾）
mitigate()  减伤链的唯一入口
healUp()    回血的唯一入口
tick(dt)    主循环（requestAnimationFrame）
draw()      canvas 渲染

--- 塔防那一节（2026-09-22 加，全部细节见第十二节）---
BF_TOWERS / TW_ODDS / TW_COST / TW_COPIES / DLVL_COST   40 座塔和整套云顶式经济
TW_FX / twFx() / twc()                                  每座塔的特效签名（12.10）
starEff() / auraOf() / auraRange()                      升星效果（s2 + s3 累积）和光环合并
openDeploy() / closeDeploy() / renderDeploy()           部署阶段（不是 .veil，是底部抽屉）
combineAll() / drawCard() / buyCard() / levelUp()       三合一和抽牌
refreshTowerStats() / fireTower() / updateTowers()      塔的运行时（属性一律现算）
towerHurt() / towerAoe() / towerZap()                   塔造成伤害的唯一口子
drawBuilds() / drawTower()                              建筑的画法（canvas 画的，零新增美术）
```

⚠️ **地牢那条硬规矩在这边照样生效：遗物不许留下永久面板加成。**
所有加成都在 `bstats()` 里现算，`P` 上只许存「发生过几次 / 攒了多少」这种事实。

---

## 十·四 踩过的坑

- **手机浏览器上「弹窗关掉之后第一次按屏幕会弹出放大镜」**（用户 2026-09-22 报）。
  根因是浏览器把 canvas 上的按下拖动当成「选文字」，于是弹出选择放大镜；
  弹层里全是文字，关掉时残留的选区会把下一次触摸直接接上去。
  **四处一起堵才干净，缺一不可**：
  1. `battle.css` 里 `*{user-select:none; -webkit-touch-callout:none}` ——
     **必须铺到每一个元素**，光给 `body` 挡不住 canvas；
  2. `battle.js` 的 `pointerdown` 里 **`e.preventDefault()`**，
     ⚠️ 要放在 `if(PAUSED || OVER) return;` **前面** —— 暂停时按下去一样会弹；
  3. 全局拦掉 `selectstart` / `contextmenu` / `dragstart`（输入框放行）；
  4. `hide()` 里 `clearSel()` 清掉残留选区。
  **别因为「看起来重复」删掉其中任何一条。**

## 十·五 已知的调平衡口子（试完手感先看这几个）

1. **玩家移速 155 vs 最快的怪 112** —— 你能永远跑在前面。实测「大圈绕着跑」可以做到
   一波下来一只都不杀（追兵全在身后 187px 之外，自动瞄准够不着）。
   这是**自我惩罚**的打法（不杀怪就不升级、波次照涨，迟早被淹），所以这一版没堵，
   但真嫌恶心就把 `BF.base.spd` 降到 135，或者把 `rat` / `ghost` 的 `spd` 抬到 130 以上。
2. **升级太快**：机器人乱绕 4 波就到 8 级、7 件遗物。真人会更快。
   嫌快就调 `BF.xpNeed`（现在 `8 + 6×(lv−1)`）的斜率，别去砍怪的 `xp`（那会连带金币曲线）。
3. **Boss 1400 血偏肉**（见 4.3）。
4. 「杀怪回血」那五件（药膏 / 收割 / 喘息 / 归血 / 收势）在 `killFoe()` 里直接写战场的数，
   深波要是奶不死先动它们（`BF.killScale` 2026-09-23 已经删了）。
   **`BF.xpNeed` 已经 ×2、`BF.goldMult` 已经 0.5**（都是用户 2026-09-22 定的）——
   这两条一起把「拿遗物的速度」砍了一大截，是这一版最重的难度调整。
   要回退就改这两个数，**别去动怪的 `xp` / 掉金公式**。
5. **互动点掉率**（`BF.site` 的三个 `p` + `pity`）：现在合起来 4.5%，一波期望 ~1 个，
   加上每波保底，**至少一波一个**。嫌补给太多就先砍保底权重里的 `spring`，
   嫌买不到东西就抬 `shop.p`。
6. **石箱白给、合成免费**（都是用户定的），所以金币**只剩游商一个口子**。
   要是试下来钱明显花不完，先抬 `BF.site.shop.p`（现在 1.1%）让游商更常见，
   别去给箱子或合成加回价钱。
7. **特殊遗物（30 件）故意很强**，见「五·五」。一趟只拿得到 波数÷10 件、最多同时带 5 件，
   所以它们是「打到多深」的奖励曲线，不是常规平衡的一部分。
   第二批那几个数（`SP_BURN_PCT` / `SP_RAIN_CD` / `SP_QUAKE_N` / `SP_QUAKE_PCT` / `SP_FLURRY_N` /
   `SP_BLINK_CD` / `SP_BLINK_PCT` / `SP_LEECH_N` / `SP_TIDE_CD` / `SP_SKULL_MAX`）都拎在 `BF_SPECIAL` 下面，
   要调平衡先动那几个。**2026-09-23 按「折合每秒几倍攻击」拉过一遍**，表在「五·五」末尾。
8. **塔的强度**（2026-09-22 加的那一层）：塔的伤害 = `towerPower() × dmg`，
   所以它整条曲线是**跟着玩家的遗物走**的 —— 嫌塔弱就调 `BF_TOWERS` 里的 `dmg`，
   **别去改 `towerPower()` 的口径**（那会把 40 座塔一起挪）。
   ⚠️ **升星已经不给数值了**（用户 2026-09-22），所以「嫌三星太强」要去改那一座塔自己的
   `s3`，不是去改 `STAR_DMG`（那张表整排是 1）。
9. **部署经济**（`BF.deploy` 的 `startGold` / `incomeBase` / `incomePer` + `DLVL_COST` + `TW_COST`）：
   2026-09-22 **价格整排 ×5、利息取消**，同一天又把**高品质塔（×2/×3/×4/×5）和 4~9 级升级价（×2）**
   各乘了一道：第 1 轮到手 95 金（够「升到 2 级 + 两座普通塔」），一路满级要 **3990 金**（2026-09-23 6~9 级再 ×2 → **7130 金**）——
   **买塔的钱主要来自击杀**，固定收入只是个底。
   ⚠️ 同一天还加了「**部署时把场上的金币全部吸走、下一轮到账**」（`P.bank`），
   所以「跑不过去捡」不再等于白丢，只是晚一轮 —— 这条等于给中后期**多发了一笔钱**，
   调经济曲线时要一起算。经济塔（铸币台 / 拾荒幡 / 金库）是第三个口子。
   嫌塔攒得慢就抬 `incomeBase` / `incomePer`，**别去抬怪的掉金**（那一份也是买遗物的预算）。
   ⚠️ **刷新底价 6 金，但部署每升一级翻一倍**（用户 2026-09-23，`rerollCost()`：1 级 6 → 9 级 1536）。
10. **升级经验 ×3.6**（先 ×2 再 +80%，都是用户 2026-09-22 定的），**40 级往上再 ×3**：
   遗物拿得比以前慢得多，实测机器人打到第 10 波才 2 级（改之前是 4 级）。
   嫌慢就改 `BF.xpNeed` 的系数，**别去砍怪的 `xp`**（会连带把金币曲线和 Boss 的经验补偿一起拖下水）。
   ⚠️ **50 级后每 5 级、80 级后每 10 级才给一次遗物**（`BF.pickSteps`，用户 2026-09-23）。
11. **11–19 波那一批的三个阀门**（2026-09-22 加的）：拒马的 `guard.cut`(25%)、
   缚锁者的 `silence.sec`(3)、爆囊的 `boom.dmg`(28)。
   前两个直接决定「塔流」还能不能站得住，第三个决定「站着不动」的惩罚有多重。
   ⚠️ 爆囊的引信 1.0 秒是**按玩家 155 移速刚好跑出半径 100** 算的，**别缩短**。
12. **21–39 波那一批的四个阀门**：噬盾者 `leech.pct`(8%/秒)、相位兽 `immune.dur`(1.4s)、
   母巢 `hatch.max`(10)、掘垒者 `sap.mult`(×2)。这四个各自克一条流派
   （叠盾 / 爆发 / 清场 / 塔），**要调就一次只动一个**，不然分不清是哪条被砍了。
   ⚠️ 地面危险区那两只（唤雷者 / 蚀空）的伤害已经按 `dmgMul(波数)` 砍过一轮（见 4.6.2）——
   再改之前先乘一遍波次倍率：第 34 波是 ×3.97，第 38 波是 ×4.33。
13. **营地跟着人走**（`relocateCamp()`）—— 每次部署把整座营地平移到玩家脚下。
   要是觉得「随便跑就完事」太轻松，可以考虑把它去掉，但那样第二次部署就得从零重建，
   前面买的塔全白费。**这一版选的是「营地跟人」。**

## 十一 待办

- [ ] **2–5 层难度**：数据已经在 `BF_TIERS` 里，等局外养成系统。
- [ ] **战场专属新遗物**（移速 / 攻速 / 刀程 / 击退 / 弹反）：等用户试完手感再决定。
- [x] ~~**塔的第二批**~~ —— 第三批 7 座（2026-09-22）+ 第四批 9 座（2026-09-23）补完，现在每档
      **11 / 8 / 10 / 7 / 4**。再加的话照 12.6 的口径：**每座塔的功能都得不一样**，别加「伤害更高的箭塔」，
      而且**必须在 `TW_FX` 里配一行特效签名**（12.10）。
- [ ] **塔的星级表现**：现在 2★/3★ 只是数字变大 + 头顶多一个点，没有形象变化。
- [ ] **部署阶段的撤退键**：现在摆错了只能拖回去或者点一下收回，没有「一键收回全部」。
- [ ] **塔的伤害跟玩家遗物的联动只有一件**（「祭余」+10%）。想让「养塔」成为一条流派，
      得再配几件专门喂塔的遗物 —— 口子已经留好了，都并进 `towerRelicPct()` 就行。
- [ ] **宝石结算**：等局外养成定了口径再接 `youxu.town.v1`。
- [x] ~~**11–20 波的怪**~~ —— 2026-09-22 做完：**9 只新怪 + 第 20 波 Boss「锈庭主事」**，
      全表在「四·五」。
- [x] ~~**21–40 波**~~ —— 2026-09-22 做完：**12 只新怪 + 第 30 波「烬渊祭司」+ 第 40 波「墟心冕者」**，
      全表在「四·六」。
- [x] ~~**41 波往后还是老样子**~~ —— 2026-09-22 用户定了口径：**第 40 波就是终点**，
      往后 `lateWaveDef()` 把 1–40 波的怪混着出、`lateRamp()` 一直加数值，
      Boss 照旧是冕者按轮次加压。**不再加新怪**（立绘也确实画不出来了，`MOB_ART` 那 14 张全用满了）。
      见第三节「第 40 波是终点」。
- [ ] **多武器 / 多攻击方式**：现在只有一把刀。土豆兄弟那种「六个武器格」这一轮没做。

---

## 十二 塔防 · 部署阶段（用户 2026-09-22）

用户定的规格，逐条：**每 5 波进一次部署阶段（开局也算一次）；机制照云顶之弈 ——
九个卡槽、金币能升级 / 刷新 / 买塔、三张同名一星合成二星、上限三星、抽牌按等级给概率；
上限 9 级 = 9 人口 = 场上最多 9 座塔；泉和商不占人口；开局先给一笔钱；
塔分普通/稀有/史诗/传奇/神圣五档，每座功能都不一样；
塔之间没有羁绊，但能互相搭配做到 1+1>2；塔的伤害跟人物伤害挂钩。**
（原话里还有「所有建筑除了城墙都没有血量」——**城墙 2026-09-22 整块删了**，见 12.7。）

### 12.1 什么时候进部署

- **第 1 / 6 / 11 / 16 …波之前**（`isDeployWave(w)` = `(w−1) % BF.deploy.every === 0`）。
  开局那一次是在 `btnGo` 里直接 `openDeploy()`，之后由 `nextWave()` 触发。
- ⚠️ **部署波（w%5===1）和 Boss 波（w%10===0）永远撞不上**，不用兜。
- 部署阶段**不是弹层**（`DEPLOY` 这个模块级开关 + `#deploy` 这块底部抽屉）——
  摆塔得看得见画面、点得到画面，所以**别把它改成 `.veil`**。
  `hide()` 里的 `PAUSED = anyVeil() || OVER || DEPLOY` 认的就是这个开关：
  部署途中如果撞上「升级四选一」，选完关掉弹层仍然停在部署里。
- ⚠️ **进部署时整座营地会平移到玩家脚下**（`relocateCamp()`，按所有建筑的重心算位移）：
  阵型一点不变，人跑多远都不会把塔丢在后面。**没有这一步的话第二次部署就得从零重建**，
  前面买的塔全白费 —— 别删。
- ⚠️ **部署时镜头往下推半个面板高**（`camShift`，在 `renderDeploy()` 里量一次）——
  不推的话玩家正好被面板压住，营地只看得见上半圈。
  ⚠️ `camShift` 要**除以 `ZOOM`** 再用 —— 它是屏幕上的半个面板高，换算回世界得除一次。
- ⚠️ **部署阶段能缩放和平移**（用户 2026-09-22）：`ZOOM` 0.55~1.5（手机两指捏合，桌面滚轮），
  ⚠️ **缩放的 ± 按钮同一天删了**（用户：「不用加减按钮」）—— 别加回来；
  `PAN` 是相对玩家的偏移、夹在 `placeR` 之内。**两样都只在部署阶段生效**，
  `closeDeploy()` 会复位 —— 打起来的时候镜头永远居中、1 倍，那是这个模式的手感底线。
  ⚠️ 缩放是在 `draw()` 里用 canvas 的 `transform` 做的（**以屏幕中心为定点**），
  所以 **`sx()` / `sy()` 一个字没改**，全部世界坐标的代码都不用管缩放这回事。
  反过来「屏幕点换世界点」走 **`screenToWorld()`**，那儿要自己除一次 `ZOOM`。
  ⚠️ 缩小之后剔除边界和格线循环都得按 `1/ZOOM` 放开（`padX` / `padY` / `vw` / `vh`），
  **格线的 `lineWidth` 也要 `1/ZOOM`** —— transform 会把它一起缩细到看不见。
- ⚠️ **进部署先把场上的弹幕全清掉**（用户 2026-09-22）：`E.shots` / `E.tshots` / `E.pwaves` /
  `E.zones` 四个数组清空，顺手把远程怪的抬手 `castT` 也打断。
  不清的话「开战」那一下会直接吃一脸停在半空的弹 —— 那是上一波留下的，躲都没法躲。
  ⚠️ **自己塔的也要清**（用户同一天补的「包括自己塔的」）：每座塔的
  `warn`（重炮台的抬手）/ `vortT`（漩涡）/ `beamT` / `tgt` / `cd` / `rep` 全部归零 ——
  不清的话重炮台会把那一发落在早就没人的地方，开战第一秒就是一炮空的。
- ⚠️ **部署时整个「战斗层」都不画**（用户 2026-09-22：「临时屏蔽所有怪物和特效」）：
  `draw()` 开头取一个 **`fight = !DEPLOY`**，怪 / Boss 预警 / 地面危险区 / 两边的弹幕 /
  光束 / 冲击波 / Boss 指示三角 / `drawFx()` **全部挂在它下面**。
  摆塔的时候屏幕上只剩地板、营地和你自己，阵型一眼看得清。
  ⚠️ 这是**只改画面**：怪还在场上、状态一点没变（部署时 `PAUSED`，它们本来也不动），
  **别顺手把 `E.foes` 清了** —— 开战那一下场上该是什么样就得是什么样。
  ⚠️ 不画的那一支要**照样清过期的 `E.fx`**（清理本来挂在 `drawFx()` 末尾）。

### 12.2 钱从哪来

| 项 | 值 |
| --- | --- |
| 开局白给 | **60**（`BF.deploy.startGold`，只在第 1 轮给）|
| 每轮固定收入 | **35 + 15 ×（轮次 − 1）**（`incomeBase` / `incomePer`）|
| 利息 | **没有了** |

- ⚠️ **利息 2026-09-22 取消了**（用户要求）—— 云顶那套「攒钱生钱」在这儿只会让人第一轮什么都不买。
  **别把 `interestPer` / `interestMax` 加回来。**
- ⚠️ **同一天塔和升级的价格整排 ×5**，所以**买塔的钱主要来自击杀**，固定收入只是个底：
  第 1 轮到手 60 + 35 = **95 金**（够「升到 2 级 + 两座普通塔」），之后每轮 50 / 65 / 80…
  而一波击杀大约给 50 金（第 20 波往后两三百）。
- **击杀掉的金币照旧**（`BF.goldMult` 还是 0.5）—— 那一份既买塔也买遗物。
- ⚠️ **进部署会把场上散落的金币全部吸走，但要到下一轮部署才到账**（用户 2026-09-22）：
  存在 **`P.bank`** 上，面板的金币后面写着「（待领 N）」，下一次部署开头才 `addGold()` 发下来。
  **顺序写死：先把上一轮吸走的发下来，再吸这一轮的** —— 反过来就成了当场到账。
  入库存的是**原值**，到账那一步才吃「拾金」那一类的加成。
  它同时解决了一件事：以前满地金币不去捡就白丢，现在跑不过去也不亏，只是**晚一轮**。
- **经济塔那一条线**（用户 2026-09-22 点名要的）：铸币台自己产币、拾荒幡让范围内的击杀多掉钱、
  金库每次部署额外给一笔。全表在 12.6。
  ⚠️ **金库的二三星同一天做厚了一轮**（用户：「加强金库的 2、3 星效果」）：
  二星**收入再 +60 金**、白送 **3 次**商店刷新；三星白送 **2 张**塔牌、再按波数给 **6 × 波数** 金。
  实测一座三星金库在第 20 波那一轮到手 **240 金**（+ 固定收入 50）。
  刷新次数和牌数现在是**数量**不是开关（`vaultReroll` / `vaultCard` 都按数加）。
- ⚠️ 金币现在有**两个去处**：部署面板（升级 / 刷新 / 买塔）和营地里的游商（买遗物）。
  嫌钱紧先抬 `incomeBase` / `incomePer`，**别去动怪的掉金公式**（会连带把别的曲线拖下水）。

### 12.3 等级 · 人口 · 备战区

- **部署等级 `P.dlvl` 1~9**，等级就是**人口上限** = 场上最多几座塔（`popLeft()`）。
  ⚠️ **泉和商不占人口**（用户定的），只有塔占。
- 升级价 `DLVL_COST`（**下标 = 目标等级**，2026-09-22 整排 ×5）：

  | 升到 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9 |
  | --- | --- | --- | --- | --- | --- | --- | --- | --- |
  | 金 | 40 | 70 | **220** | **320** | **920** | **1280** | **1800** | **2480** |

  一路满级一共 **7130 金**。
  ⚠️ **6~9 级 2026-09-23 再 ×2**（用户要求，原来 460 / 640 / 900 / 1240，满级 3990）——
  前五级没动，中期照样能爬到 5 级；后四级是要攒很久的钱，配合「刷新价随等级翻倍」，高等级阵容是真奢侈品。
  ⚠️ **4~9 级那一段 2026-09-22 又翻了一倍**（用户要求）—— 前三级没动，所以开局照旧能顺手升到 3 级，
  之后「爬人口」和「凑三星」才变成真正的取舍。
  ⚠️ **这张表必须有第 9 项** —— 少写一项会在升 9 级那一下把 `P.gold` 变成 `NaN`（踩过，
  `DLVL_COST[10]` 是 `undefined`）。改上限记得跟着补，`levelUp()` 里也兜了一句 `if(!c) return`。
- **备战区 9 格**（`BF.deploy.bench`，用户说的「九个卡槽」）：买来的塔、白给的泉 / 商卡都落这儿。
  备战区满了**还能买**一张能当场合成的牌（`wouldCombine()`），否则买不了。

### 12.4 抽牌（照云顶那套）

- **牌库是共享池**：每种塔按品质各有 **22 / 18 / 14 / 10 / 9** 张（`TW_COPIES`；神圣 2026-09-23 从 6 改成 9，
  凑得出一座三星）。⚠️ **读档时牌库按「总张数 − 备战区和场上已有的」重算**（`resumeSaved()`），
  所以改张数、加新塔都不用管老存档。
  买走扣一张，**卖掉按星级还回去**（1★ 1 张 / 2★ 3 张 / 3★ 9 张，`starCopies()`）。
  ⚠️ **没买的牌不进出池子**（`drawCard()` 只在买的时候 `poolTake`），所以刷新不用还牌。
- 商店 **5 个货位**，刷新 **`rerollCost()` = 6 × 2^(部署等级 − 1)** 金（用户 2026-09-23：「每升一级刷新所需金币 ×2」）：
  1 级 6 / 2 级 12 / 3 级 24 / 4 级 48 / 5 级 96 / 6 级 192 / 7 级 384 / 8 级 768 / 9 级 1536。
  金库给的免费刷新不受影响。
  ⚠️ **商店是个弹窗**（用户 2026-09-22：「塔的商店改为按钮弹窗里面刷新和购买」），
  标题就写「**商店**」、关闭钮写「**返回**」（同一天用户定的措辞，别改回「塔的商店 / 回去摆」）——
  部署面板上只剩一个「商店」按钮，刷新和购买都在 `veilTwShop` 里。
  面板因此矮了一半（34dvh），上面看得见的营地就多了一半。
- 按等级给品质概率（`TW_ODDS`，下标 = 部署等级）：

  | 等级 | 普通 | 稀有 | 史诗 | 传奇 | 神圣 |
  | --- | --- | --- | --- | --- | --- |
  | 1–2 | 100 | — | — | — | — |
  | 3 | 75 | 25 | — | — | — |
  | 4 | 55 | 30 | 15 | — | — |
  | 5 | 45 | 33 | 20 | 2 | — |
  | 6 | 30 | 40 | 25 | 5 | — |
  | 7 | 19 | 30 | 35 | 15 | 1 |
  | 8 | 18 | 25 | 32 | 22 | 3 |
  | 9 | 10 | 20 | 25 | 35 | 10 |

  抽中的那一档如果一张货都没有，**先往低档找，再往高档找**（`drawCard()` 里的 `order`）。
- **买价 `TW_COST` = 20 / 80 / 210 / 480 / 1000**，卖价 = `买价 × 张数`（跟云顶一个口径）。
  ⚠️ 这是**两道乘法叠出来的**：2026-09-22 先整排 ×5，同一天用户又按品质各乘一道
  （**稀有 ×2 / 史诗 ×3 / 传奇 ×4 / 神圣 ×5**，普通没动）。
  于是「开局铺普通塔」仍然便宜，高品质变成需要攒的东西。
  ⚠️ 刷新的底价 6 金没跟着 ×5，但**跟部署等级翻倍**（见上面 12.4 那条）—— 前期随便刷，后期刷一次要掂量。
- **三张同名同星 → 高一星，上限 3 星**（`combineAll()`，备战区和场上一起数）。
  ⚠️ 合成的落点**优先用「场上那三张里的第一张」的位置** —— 合成不会把摆好的阵型打散。
- ⚠️ **升星不再给任何数值加成**（用户 2026-09-22：「塔升星不会有基础数值提升，变成效果，
  比如多攻击一次或分裂、弹射、范围扩大等特殊效果，**特别是三星，提升是质变**」）。
  `STAR_DMG` / `STAR_AURA` 两张表整排留成 **1**（只为不动一片调用点，**别把 1.8 / 3.2 加回来**），
  升星的全部价值写在每座塔自己的 **`s2` / `s3`** 上，`t` 是卡面文案、其余字段是效果键。
  **三星是累积的**（s3 的效果加在 s2 上），别写成「三星替换二星」。
  效果键（`starEff()` 认的，全部在 `refreshTowerStats()` 里进 `t.ef`）：

  | 键 | 意思 | 结算在哪儿 |
  | --- | --- | --- |
  | `rangeX` | 射程 × | `ef.range` |
  | `burstN` | 扇形一次射几枚（跟 `shots` 不是一回事：那个是「打几次」）| `fireTower()` |
  | `mineMax` | 场上最多埋几枚雷 | `updateTowers()` |
  | `shred` | 命中**永久**削几点护甲 | `towerHurt()` |
  | `shots` | 一次打几下（每 0.12 秒补一发，走 `t.rep`）| `updateTowers()` |
  | `pierce` / `bounce` | 弹丸穿透 / 弹射几次 | `updateTShots()` |
  | `splash` | 命中炸开的范围（直射塔也能有）| 弹丸命中 / `towerZap()` |
  | `chain` / `stormChain` | 连锁几个 / 雷落下后再连锁 | `fireTower()` |
  | `markN` / `markPct` / `markBoom` | 标记那一套（`markBoom` 在 `killFoe()` 里炸）| |
  | `slowHit` / `stun` | 命中减速 / 定身几秒（`noSlow` 的怪免疫）| `towerHurt()` |
  | `exec` | 处决线：生命低于这个比例直接死，**Boss 免疫** | `towerHurt()` |
  | `beamN` / `beamRamp` | 光束同时咬几个 / 咬久了越烫 | `fireTower()` |
  | `vortHurt` | 漩涡持续期间每 0.5 秒伤一次 | `updateTowers()` |
  | `freeze` | 光环塔定时冻住场上的怪 | `updateTowers()` 的光环分支 |
  | `auraAll` / `auraAdd` | 光环覆盖全场 / 光环多给一项 | `auraOf()` / `auraRange()` |
  | `healShield` / `healAll` / `healCleanse` / `healSave` / `healOver` | 回血塔那一套 | `fireTower()` |
  | `goldAuto` / `goldDbl` / `mintPerTower` | 经济塔那一套 | `killFoe()` / `fireTower()` |
  | `vaultBonus` / `vaultWave` / `vaultReroll` / `vaultCard` | 金库那一套（后两个是**数量**）| `openDeploy()` |
  | `auraAdd.playerAspd` / `playerRange` | 督战旗那一类作用在**玩家**身上的光环 | `playerAura()` → `bstats()` |

  ⚠️ **处决（`exec`）必须放过 Boss** —— 不然第 40 波的冕者会被一圈荆棘直接抹掉。
  ⚠️ `towerAoe()` **带了递归深度**（`twAoeDepth`，封 6）：窥影镜三星的「倒下就炸」
  和处决能一路连锁，不封会把一整波炸成爆栈。

### 12.5 摆放 —— 拖

用户 2026-09-22：「**塔可以拖动选择位置在放置时**」。三种手势用**一个 `DRAG` 收口**
（`deployDown` / `deployMove` / `deployUp`），别再各写一套：

| 手势 | 做什么 |
| --- | --- |
| 从**备战区那一格**按下去、拖到画面上松手 | 摆下去（松手还在面板上就只是选中，牌还捏在手里）|
| 拖**场上的建筑** | 挪位置；摆不下就弹回原位 |
| **原地点一下**场上的建筑 | 收回备战区 |
| 空白处拖 | 平移镜头（`PAN`）|
| 两根手指（桌面是滚轮）| 缩放（这时 `DRAG` 直接作废）|

- ⚠️ 监听挂在 **`document`** 上，不是画布 —— 「从备战区按下、一路拖到画面上松手」才收得到
  move/up。挂在画布上的话拖出画布就断了。
- ⚠️ 备战区那一格走 **`pointerdown`** 不走 `click`（按下就选中并开始拖），
  并且要 `preventDefault()`，不然手机上拖它会变成选文字。
- ⚠️ **两座建筑至少隔 `BF.deploy.gap`(34)** —— 用户明确说了「**不能重叠**」。
  拖着自己挪位置时 `canPlace(x, y, skip)` 要把自己排除掉，不然永远摆不下。
- ⚠️ **只能摆在离自己 `BF.deploy.placeR`(320) 之内**（有了缩放和平移之后从 260 放宽的）。
- 拖动中画面上有个**落点圈**：绿的能放、红的放不下。
- 游商被收回备战区时**货架跟着牌走**（`{k:"shop", stock:…}`）—— 不然「收起来再放下」
  就是一次免费刷新。

### 12.6 四十座塔

**`kind` 决定行为，`cd` 是攻击间隔（秒），`dmg` 是伤害倍率。**
⚠️ **塔的伤害 = `towerPower() × dmg`**（**星级不再乘**，见 12.4），`towerPower()` 读的是
**玩家最近几刀的实际出手伤害**（在 `swing()` 里平滑记进 `P.power`，空刀也记一笔）。
用户定的：「塔的伤害和人物伤害挂钩」—— 玩家身上每一件加伤害的遗物都会同时喂给全部的塔。
**别改成写死的数值。**

| 品质 | id | 名字 | 间隔 | 射程 | 伤害 | 干什么 | ★★ | ★★★（质变）|
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 普通 | `tw_bolt` | 箭塔 | 1.0 | 150 | 0.60 | 单体直射，最老实的一座 | 一次射两箭 | 箭矢穿透一条线 |
| 普通 | `tw_spike` | 尖刺台 | 0.5 | 78 | 0.28 | 脚下一圈范围伤害 | 范围 ×1.6 | 每次攻击定身 0.4 秒 |
| 普通 | `tw_sling` | 投石机 | 1.8 | 190 | 0.85 | 抛射，落点炸 48 | 一次抛两发 | 炸 120 + 泥泞减速 35% |
| 普通 | `tw_lantern` | 霜灯 | — | 120 | — | 光环：范围内的怪移速 −20% | 减速覆盖全场 | 每 5 秒冻住全场 0.7 秒 |
| 普通 | `tw_drum` | 战鼓 | — | 140 | — | 光环：范围内**其它塔**攻速 +20% | 也给伤害 +15% | 鼓声覆盖全场 |
| 普通 | `tw_well` | **涌泉台** | **8.0** | 150 | — | **回血**：你在圈里就回 2.5% 最大生命 | 顺带 8 点护盾 | 覆盖全场 + 解除减速 + 3 秒 −20% 伤害 |
| 普通 | `tw_fan` | 散花台 | 1.3 | 130 | 0.30 | 扇形射 3 枚碎石，各自结算 | 一次射 5 枚 | 碎石穿透 |
| 普通 | `tw_tack` | 铁蒺藜 | 3.0 | 130 | 0.90 | 埋地刺，踩到炸 55；最多 3 枚 | 最多 6 枚 | 炸 80 + 定身 0.5 秒 |
| 普通 | `tw_coin` | **拾荒幡** | — | 170 | — | **经济**：范围内的敌人倒下多掉 35% 金 | 直接入账不用捡 | 25% 概率再掉一份 |
| 稀有 | `tw_frost` | 冰晶塔 | 1.4 | 165 | 0.55 | 命中减速 40% / 1.5 秒 | 冰弹再弹 2 次 | 命中炸 60 + 定身 0.6 秒 |
| 稀有 | `tw_lure` | 引灵幡 | 2.5 | 220 | 0 | **没有伤害**，把怪拽向自己 90 | 范围 ×1.5 | 拽完定身 0.8 秒 |
| 稀有 | `tw_arc` | 电弧塔 | 1.2 | 135 | 0.42 | 闪电在最近 3 个之间跳 | 改成跳 6 个 | 每一跳都炸 40 |
| 稀有 | `tw_mirror` | 窥影镜 | 1.8 | 200 | 0 | **没有伤害**，标记 3 只 4 秒（伤害 +25%）| 一次标 8 个 | 被标记的倒下时炸 90（40% 最大生命）|
| 稀有 | `tw_mint` | **铸币台** | 10.0 | — | — | **经济**：每 10 秒产「6 + 波数」金 | 直接入账 | 每有一座塔多产 2 金 |
| 稀有 | `tw_sniper` | 长瞄塔 | 2.4 | **340** | 1.50 | 挑射程内**血最多**的那只射 | 连射两发 | 命中后生命 <12% 直接死 |
| 史诗 | `tw_cannon` | 重炮台 | 2.8 | 210 | **2.60** | **抬手 0.8 秒**锁死落点，炸 60 | 连开两炮 | 炸 120 + 定身 0.6 秒 |
| 史诗 | `tw_beam` | 灼光塔 | 0.25 | 175 | 0.20 | 光束咬住一个不放 | 同时咬两个 | 咬越久越烫（每跳 +15%，上限 +150%）|
| 史诗 | `tw_bramble` | 荆棘园 | 1.0 | 105 | 0.50 | 范围持续伤害 + 移速 −25% | 范围 ×1.7 | 圈内生命 <15% 直接死 |
| 史诗 | `tw_horn` | 号角旗 | — | 130 | — | 光环：范围内**其它塔**伤害 +30% | 也给射程 +20% | 号角覆盖全场 |
| 史诗 | `tw_chapel` | **祷堂** | **6.0** | 175 | — | **回血**：你在圈里就回 4% 最大生命 | 顺带 15 点护盾 | 每波一次：掉到 35% 以下立刻回 25% |
| 史诗 | `tw_rend` | 裂甲桩 | 1.6 | 160 | 0.50 | 命中**永久**削 2 点护甲（可叠加）| 改成削 5 点 | 命中炸 80，圈里一起削 |
| 史诗 | `tw_banner` | 督战旗 | — | 150 | — | 光环：**你**在圈里时攻速 +25%、刀程 +15% | 再 −10% 受到的伤害 | 覆盖全场 |
| 史诗 | `tw_vault` | **金库** | — | — | — | **经济**：每次部署额外收入 60 金 | 收入再 +60，白送 3 次刷新 | 白送 2 张牌，再给 6 × 波数 金 |
| 传奇 | `tw_storm` | 雷云柱 | 2.0 | 240 | 2.00 | 劈**最密的那一团**，炸 80 | 一次连劈三道 | 每道雷再连锁 4 个 |
| 传奇 | `tw_gravity` | 塌陷核心 | 4.0 | 260 | 1.20 | 1.5 秒的漩涡一直拽人 | 漩涡期间每 0.5 秒伤一次 | 结束时圈内生命 <20% 直接死 |
| 传奇 | `tw_forge` | 熔炉 | — | 150 | — | 光环：**其它塔**攻速 +35%、伤害 +20% | 间隔再 −15% | 炉火覆盖全场 |
| 传奇 | `tw_grove` | **生息树** | **4.0** | 200 | — | **回血**：你在圈里就回 3% 最大生命 | 顺带 12 点护盾 | 覆盖全场 + 溢出转护盾 |
| 传奇 | `tw_rail` | 贯石弩 | 2.6 | 300 | 1.90 | **自带穿透**，打穿一条线 | 连射两发 | 打中的每只都炸 60 |
| 神圣 | `tw_judge` | 裁决之环 | 1.0 | 190 | 0.70 | 打范围内**全部**；对被控的伤害翻倍 | 范围 ×1.5 | 圈内生命 <25% 直接死 |
| 神圣 | `tw_obelisk` | 归墟碑 | — | 200 | — | 光环：**其它塔**间隔 −25%、射程 +25%；你在圈里 −20% 伤害 | 也给伤害 +25% | 覆盖全场 + 减伤再 −10% |
| 神圣 | `tw_clock` | 停摆钟 | — | 180 | — | 光环：范围内移速 −15%，**每 8 秒定身 1.5 秒** | 改成每 5 秒一次 | 覆盖全场 |
| 普通 | `tw_boomer` | **飞轮台** | 1.6 | 170 | 0.45 | 飞轮飞到射程尽头**再飞回来**，去程回程各打一次 | 一次甩两枚 | 飞轮在尽头原地绞 1 秒 |
| 普通 | `tw_bellows` | **焚风口** | 0.35 | 95 | 0.16 | 朝最近的敌人喷 60° 火舌，扇形里全吃 | 张角 ×2（120°）| 点着 3 秒（每秒一次，伤害 ×2）|
| 稀有 | `tw_hive` | **蜂巢** | 1.8 | 200 | 0.55 | 放 2 只**追踪**毒蜂 | 一次放 4 只 | 蜂蜇完不死，再找下一个（每只 3 次）|
| 稀有 | `tw_prism` | **折光棱** | 1.5 | 180 | 0.75 | 棱光命中后**裂成 3 道**碎光（各 50%）| 裂成 5 道 | 碎光再裂一次 |
| 史诗 | `tw_miasma` | **瘴气罐** | 3.2 | 200 | 0.30 | 抛一罐，落地留**毒雾**（半径 70、4 秒、每 0.5 秒一跳）| 毒雾留 7 秒 | 雾里的敌人吃所有塔的伤害 +30% |
| 史诗 | `tw_quake` | **震地锤** | 2.4 | 230 | 1.60 | 朝目标裂开一道**直线地缝**，线上全吃 + 震开 | 一次三道（扇形）| 地缝定身 0.7 秒 |
| 传奇 | `tw_leech` | **血藤** | 0.3 | 170 | 0.28 | 缠住最近 3 个持续吸血；**你在圈里时吸到的 3% 回给你** | 同时缠 5 个 | 被缠的 −50% 移速，<15% 直接勒死 |
| 传奇 | `tw_lance` | **聚能炮** | 3.6 | 260 | 4.00 | **蓄力 1 秒**，射一道长 420 的贯穿激光 | 激光留 2 秒灼痕 | 一次三道（扇形）|
| 神圣 | `tw_rift` | **虚空裂隙** | 5.0 | 260 | 0.35 | 在最密处撕开裂隙（半径 90、2.5 秒）：−60% 移速 + 每 0.25 秒一跳 | 裂隙追着最近的敌人游走 | 合拢时拽到中心再炸（4 倍）|

**第三批 7 座**（用户 2026-09-22「再新增一批塔楼」）：散花台（扇形齐射 `kind:"burst"`）、
铁蒺藜（埋雷 `kind:"mine"`，上限在 `updateTowers()` 里数场上还剩几枚）、
长瞄塔（`aimStrong` —— **挑血最多的**，专门点精英和 Boss）、
裂甲桩（`shred`，**永久**削护甲，在 `towerHurt()` 里结算）、
督战旗（**唯一一座作用在玩家身上的光环** `playerAspd` / `playerRange`，走 `playerAura()`）、
贯石弩（底表自带 `pierce`）、停摆钟（底表自带 `freeze`，光环塔里唯一会动的那一类）。
⚠️ `pierce` / `burstN` / `mineMax` / `shred` / `freeze` 这几项**底表和升星都能写**，
`refreshTowerStats()` 里一律「升星优先、底表兜底」。
⚠️ `playerAura()` **不许调 `bstats()`**（`bstats()` 自己要调它），跟 `cutStatic()` 那条是同一个坑。

**第四批 9 座**（用户 2026-09-23：「加入一批全新塔楼，每种塔楼的特效要有区别，有识别度」）——
每一座都是**别人没有的打法**，新开了 8 个 `kind`（折光棱走现成的 `shot` + `split`）：

| kind | 塔 | 怎么结算 |
| --- | --- | --- |
| `boomer` | 飞轮台 | `E.tshots` 里的 `disc`：去程（`hitOut`）→ 三星原地绞 → 回程（`hitBack`），每一程同一只只打一次 |
| `flame` | 焚风口 | 瞬时扇形判定（跟玩家挥刀一个算法）；三星借余烬那套 `f.burnT` / `f.burnD` |
| `homing` | 蜂巢 | `bee`：每帧往目标拐最多 7 rad/s，带左右晃；目标没了找最近的 |
| `shot` + `split` | 折光棱 | 直射弹命中后在 `updateTShots()` 里生出 N 道子弹（50% 伤害），三星的子弹再裂一次 |
| `cloud` | 瘴气罐 | 先是一发 `lob`，落地变成 `cloud`（地面物）；三星借窥影镜的 `f.mark` / `f.markPct` 做易伤 |
| `fissure` | 震地锤 | 瞬时线段判定 `foesOnLine()` + 击退；一锤里同一只只算一次 |
| `drain` | 血藤 | 每跳挑最近的 N 只；回血**攒在 `t.drainAcc` 里，满 1 点才 `healUp()`**（小数会被 round 吃掉，跟饮刃同一个坑）|
| `laser` | 聚能炮 | 走重炮那套 `d.warn` 抬手锁方向，再 `foesOnLine()`；二星留 `scorch` 地面物 |
| `rift` | 虚空裂隙 | 地面物 `rift`：减速 + 跳伤；二星每帧往最近的敌人漂 70 px/s |

⚠️ 数值是按「同品质的老塔」对出来的：机器人实测（关掉玩家的刀、只留一座塔、第 12 波、25 秒）
每座新塔的击杀数都落在同品质老塔的区间里（普通 5~46、稀有 1~64、史诗 20~67、传奇 15~91、神圣 51~76）。
⚠️ **飞轮 / 蜂 / 毒雾 / 灼痕 / 裂隙**都挂在 `E.tshots` 上 —— 所以**进部署那一下会被一起清掉**（跟别的弹幕同一条规矩）。

**回血 / 经济这两条线是 2026-09-22 用户点名加的**（「可以做回血类的，经济类的」），
一共 6 座（涌泉台 / 拾荒幡 / 铸币台 / 祷堂 / 金库 / 生息树）。它们不打人：
- ⚠️ **回血塔的冷却 2026-09-22 整排 ×2**（用户要求）：涌泉台 4→**8** 秒、祷堂 3→**6** 秒、
  生息树 2→**4** 秒。它们本来是「站着就不会死」，一座三星生息树的回复量顶得过一整波的挨打。
- **回血塔**（`kind:"heal"`）只在**你站在圈里**时烧 CD（三星的 `healAll` 才不看距离），
  回血一律走 `healUp()`，别直接改 `P.hp`。人不在圈里就把 `t.cd` 压在 0.2 空转，不白烧。
- **经济塔**：铸币台（`kind:"mint"`）自己产币；拾荒幡（`kind:"gold"`）走 **`goldAuraAt()`**
  在 `killFoe()` 里给那一笔（**取最强的一座，不叠加**）；金库（`kind:"vault"`）在 `openDeploy()` 里结算。
- ⚠️ **它们照样占人口** —— 一座回血塔就是少一座输出塔，这是这条线全部的取舍。
- ⚠️ 回血塔和拾荒幡的范围**常驻画一圈淡的**（跟范围塔一个待遇）——
  它们的整套价值就是「你（或者怪）站没站在圈里」。

**搭配（用户要的 1+1>2，没有羁绊，全靠摆在哪儿）**：

- **引灵幡 + 重炮台 / 投石机** —— 用户点名的那一对。重炮抬手 0.8 秒后落点就锁死了，
  单独摆几乎必定打空；旁边放一面幡，怪每 2.5 秒被拽回同一个点，炮就一发一堆。
  幡升到三星还会把人按住 0.8 秒，等于给重炮上了保险。
- **霜灯 / 冰晶塔 / 荆棘园 + 裁决之环** —— 裁决对被减速的敌人伤害翻倍。
- **窥影镜 + 任何输出塔** —— 标记是「受到的伤害 +25%」，作用在 `towerHurt()` 里，全体吃。
- **战鼓 / 号角旗 / 熔炉 / 归墟碑 + 一切** —— 光环**只增益范围内的其它塔**，
  所以「挤在一起」和「铺开」是真的有取舍；**三星一律变成覆盖全场**，这时候才轮到铺开。
- **塌陷核心 + 荆棘园 / 尖刺台** —— 漩涡把人按在范围伤害里 1.5 秒。
- **拾荒幡 + 任何输出塔** —— 幡本身不杀人，它要的是「怪死在它圈里」。
- **涌泉台 / 祷堂 / 生息树 + 你自己的走位** —— 回血圈就是这个模式里唯一的「安全屋」。

⚠️ **光环不叠乘**：`aspd` / `dmg` / `cdCut` / `rangeUp` 各自**相加**再用一次
（`refreshTowerStats()`），跟全局「只有两个乘区」是同一个规矩。
⚠️ **光环塔自己的射程不吃别的光环** —— 否则归墟碑和熔炉会互相放大成死循环。
⚠️ **范围塔（`kind:"aoe"`）不许每一刀闪一个大圈** —— 一秒一次会把画面糊死。
它们的覆盖范围改成在 `drawBuilds()` 里**常驻画一圈很淡的**。

### 12.7 为什么没有城墙

**城墙 2026-09-22 已经整块删了**（用户要求）。于是**所有建筑都没有血量** ——
怪根本不会去理它们，也不会被它们挡住，**走位和塔的射程就是全部的防守**。

⚠️ 别把它加回来：`BF.wall`、`wallMode`、`updateWalls()`、`updateFoes()` 里那段建筑碰撞、
`#btnDWall` 全都删干净了。要找去 git 历史里翻。

### 12.8 泉 / 商 的新规矩

| | 以前（击杀掉落） | 现在 |
| --- | --- | --- |
| 泉 | 2.0% 掉一个，**喝一次就没了** | **建筑，永久存在**；**每一波能喝一口**，回 25% 最大生命 |
| 商 | 1.1% 递减掉一个，5 波收摊，能手动刷新 1 次 | **建筑，永久存在**；**每 5 波（每次部署）换一批货**，**不能手动刷新** |
| 箱 | 1.4% | **没变**，还是击杀掉落（这一节现在只剩它）|

- **开局就有一个商、一个泉**：每次部署白给一张泉卡 + 一张商卡（`giveBuildCard()`），
  备战区满了就替玩家自动摆在身边（`autoSpot()`）。
  ⚠️ **泉和商各只有一个**（`BF.deploy.buildMax` = **1**，用户 2026-09-22 从 4 改的）：
  开局那次部署白给一张泉 + 一张商，之后 `giveBuildCard()` 查到已经有了就不再发。
  所以「这一波的那一口泉水留到什么时候喝」是个真决定。**别改回攒一堆。**
- 泉的「每波回复」= `newWave()` 里把所有泉的 `used` 清成 false。
- 商的「每五波刷新」= `openDeploy()` 里把所有商的 `stock` 清成 null，下次踩上去重新摇。
- ⚠️ **踩上去是「进圈的那一下」才触发**（`b.inside`，`updateBuilds()`）——
  商摊现在是营地里的常驻建筑，站在上面打怪会把弹层刷屏。走开再回来才会再弹，
  关窗另有 `SITE_COOL`(5) 秒冷却 + 把人推开一格。**别改回「在圈里就弹」。**
- ⚠️ 遗物「钥匙」的战场词条跟着改成了「**游商多两件货**」（`BFW` 里）——
  它原来那句「多刷新一次」已经没有对应的东西了。
- ⚠️ `btnShopRe`（刷新货架）那一整套**已经从 `battle.html` 和 `battle.js` 里删干净了**，别加回来。

### 12.9 代码落在哪儿

`battle.js` 的「塔防」一节（在美术那一节之前）：

```
BF.deploy / DLVL_COST             部署阶段的全部数值
BF_TOWERS / TW_MAP                40 座塔（含回血 3 / 经济 3 / 第三批 7 / 第四批 9）
TW_FX / twFx / twc                每座塔的特效签名（颜色 / 弹丸 / 命中 / 光环动画）
foesOnLine / towerBolt            线段取怪（地缝 / 激光 / 灼痕）/ 直射弹的统一出口
updateTShot4                      第四批的弹和地面物（飞轮 / 蜂 / 毒雾 / 灼痕 / 裂隙）
boltHitFx                         直射弹命中的样子（按塔分）
drawTowerGround / drawTowerShots  地面层（毒雾 / 灼痕 / 裂隙 / 地刺 / 抬手预警）和弹丸层（按塔的造型画）
drawTowerAmb                      光环塔常驻的小动画
TW_COST / TW_COPIES / TW_ODDS     买价 / 牌库张数 / 抽牌概率
STAR_DMG / STAR_AURA              **整排 1**，升星不给数值（留着只为不动调用点）
starEff / auraOf / auraRange      升星效果（s2 + s3 累积）/ 光环合并 / 光环够不够得着
starLines / starText              卡面上那两行升星说明（HTML / 纯文本）
initPool / poolTake / poolBack    共享牌库
combineAll / wouldCombine         三合一
drawCard / rollShopCards / buyCard / rerollShop / levelUp / sellBench
canPlace / placeAt / pickUp / giveBuildCard / relocateCamp
openDeploy / closeDeploy / renderDeploy / openTwShop / renderTwShop
deployDown / deployMove / deployUp     ← 拖放 / 平移 / 双指缩放，挂在 document 上
ZOOM / PAN / setZoom / clampPan / screenToWorld
towerPower / towerRelicPct / refreshTowerStats / towerSlowAt / buildCut
towerHurt / towerAoe / towerZap   ← 塔造成伤害的唯一口子（标记、护甲、定身、处决都在这儿算）
goldAuraAt                        拾荒幡那一笔（killFoe 里读）
giveFreeCard                      金库三星白送的那张牌
playerAura                        督战旗那一类作用在玩家身上的光环（bstats 读它）
fireTower / updateTowers / updateTShots / updateBuilds
fxLine / fxSpark / fxBoom / fxDash / fxPlus / fxCoin / fxPillar / fxSwirl / fxPull / fxBracket
                                  塔的打击特效（见 12.10，只用线点扇形）
saveWave / savedRun / resumeSaved / clearSavedRun   每一波一次的存档（见第九节）
drawBuilds / drawTower            画面（形状是 canvas 画的，一张新图都没加）
```

⚠️ **「遗物不许留下永久面板加成」那条硬规矩在这边照样管塔**：
塔的属性全部在 `refreshTowerStats()` 里现算，`P` 上只存「手里有哪些牌、等级几、牌库还剩几张」。

### 12.10 打击特效（用户 2026-09-22：「只有圆圈波很单调」→ 2026-09-23：「每种塔楼的特效要有区别，有识别度」）

**每一座塔一个签名，不是每一类。** 以前特效按「行为」分、颜色按「品质」上 ——
箭塔 / 冰晶塔 / 长瞄塔 / 裂甲桩 / 贯石弩打出来都是「一截曳光 + 一个小方块」，同品质的连图标都是一个颜色。
现在是 `battle.js` 的 **`TW_FX`** 一张表，每座塔一行：

| 字段 | 管什么 |
| --- | --- |
| `c` | **这座塔自己的主色**：图标、弹丸、命中、范围淡圈全用它（**塔座外圈还是品质色 `TW_COL`**，别混）|
| `proj` | 弹丸造型：`arrow` 箭 / `pellet` 碎石 / `crystal` 冰晶菱形 / `streak` 狙击长曳光 / `nail` 楔钉 / `slug` 重弩 + 淡尾 / `shard` 棱光 / `rock` 翻滚的石块 / `ball` 大铁弹 / `flask` 药罐 |
| `hit` | 直射命中：`spark` 火星 / `frost` 六瓣雪花 / `crack` 裂纹 + 甲片 / `cross` 收紧的准星 / `shard` 碎光 |
| `aoe` | 范围塔每一下：`spikes` 地里冒刺（尖刺台）/ `thorns` 抽荆条（荆棘园）/ `judge` 带刻度的金环往里收（裁决之环）|
| `heal` | 回血塔：`drop` 水滴（涌泉台）/ `halo` 头顶光环 + 光线（祷堂）/ `leaf` 落叶（生息树）|
| `amb` | 光环塔**常驻**的小动画（`drawTowerAmb()`，按 `b.t` 现算，不进 `E.fx`）：`beat` 鼓点圈（战鼓）/ `snow` 绕灯的雪（霜灯）/ `horn` 声波（号角旗）/ `ember` 往上飘的火星（熔炉）/ `rune` 绕碑转的符刻（归墟碑）/ `tick` 一秒跳一格的表针（停摆钟）/ `link` **你在圈里时一条虚线连到你身上**（督战旗）/ `glint` 一闪（拾荒幡）|

其余几种是行为本身就独一份的，签名写在 `fireTower()` / `draw()` 里：
电弧（双股抖动的折线）、雷云柱（黄色雷柱）、塌陷核心（螺线）、引灵幡（朝里收的短线）、窥影镜（四个折角）、
灼光塔（抖动粗细的光束 + 目标身上一团光）、铁蒺藜（地上的蒺藜）、
飞轮（转着的四刃圈）、焚风口（抖动的火舌）、蜂巢（黄黑毒蜂带翅）、瘴气罐（鼓动的绿雾 + 冒泡）、
震地锤（一段段裂开的地缝 + 崩起的土块）、血藤（弯的红藤 + 往回流的血珠）、聚能炮（粗光 + 白芯）、虚空裂隙（转着的锯齿椭圆 + 往里吸的短线）。

**抬手预警也画出来了**（以前重炮台的 0.8 秒抬手什么都不画）：
重炮在锁定的落点画一个**收紧的准星**，聚能炮从炮口拉一条**越来越亮的虚线**、炮口的光球越蓄越大（`drawTowerGround()`）。

**画在哪一层**：毒雾 / 灼痕 / 裂隙 / 地刺 / 抬手预警在**怪底下**（`drawTowerGround()`，紧跟 `drawBuilds()`）；
弹丸 / 光束 / 藤蔓在怪上面（`drawTowerShots()`）。

原则没变：**只用线、点、扇形、椭圆和一次性的实心圆**（用户原话「不要太复杂华丽的」）——
不做渐变、不做粒子系统。随机量**在 push 的时候定死**，别在 `drawFx` 里每帧重掷（火舌例外，它就是要抖）。
⚠️ **加新塔必须在 `TW_FX` 里配一行**：漏了不会炸，但会退回品质色 + 默认小方块，识别度就没了。
⚠️ `drawFx2()` 开头把 `k` 夹在 0~1 —— 一帧里跑好几步时 `k` 会掉到 0 以下，拿它算半径就是负数，
`arc()` 当场抛 `IndexSizeError`（新加的特效第一次跑就踩了，跟老的 `r` 分支是同一个坑）。
⚠️ 全部走 `E.fx`，`REDUCE_MOTION` 开着整段跳过；它们是**纯装饰**，一个字的逻辑都不许写在里面。
⚠️ `towerAoe()` 的 `col` 参数现在基本传 `null` —— 炸开那一下由调用方自己 `fxBoom()`，
   免得「一发炸开」画两层圈。
⚠️ 过期的 fx 只在 `drawFx()` 末尾清，所以**部署阶段跳过绘制的那一支要自己清一遍**（见 12.1）。
